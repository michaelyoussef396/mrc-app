]633;E;{ sed -n '1,173p' docs/TODO.md\x3b cat /tmp/p230.txt\x3b cat /tmp/p231.txt\x3b sed -n '179,$p' docs/TODO.md\x3b } > /tmp/todo.new && mv /tmp/todo.new docs/TODO.md;6511fd6c-32b1-4b82-9893-0b1bc4ed1423]633;C# MRC TODO — working tracker

Last updated: 2026-09-11

**Monitoring intake, 11 Sep:** seven automated runs covering 8–11 Sep; PROD/Resend/Slack checks below are Michael's supplied verified observations, accepted as facts. Repository diagnoses cite `222386f` unless a production revision is named; no live service was queried in this intake. Times are copied as supplied. **Live P0: P0-14, P0-A and P0-B.** Other observations are filed for diagnosis/operations, not promoted to live P0. Production remains `bd0e984` (T20 / week plan); Sentry connector visibility is unavailable (T25).

- Detail for every item: `docs/MRC_MASTER_BACKLOG.md` (the 3 September 2026 backlog, archived verbatim). This file holds IDs and tracking state only.
- Pricing rules and the resolved/unresolved conflicts: `docs/PRICING_CANON.md`.
- Bug classes, entry template and the ledger rule: `docs/BUG_LEDGER.md`. **Read its class list (section 1) before starting any investigation.**

## How to use this file

- Sessions reference items by ID, never by description. IDs are stable and never reused.
- The priority structure is the backlog's, unchanged: P0 / P1 Pricing / P1 Workflow / P2 Equipment / P2 Report / P2 CRM / P3 / Marketing / Infrastructure / Blocked / Shipped.
- Every actionable row has four tracking cells, all blank until earned:
  - **Done** — tick only when merged to `main` with `--no-ff`, preview-tested at 375px on a pinned commit URL after a full service-worker reset.
  - **Branch / Session** — the worktree branch and session letter that owns the item.
  - **Verified** — date and method (preview URL, Studio query, `cmp`, screenshot). A chat claim is not verification.
  - **Ledger entry** — the `BUG-n` written in the same commit as the fix, or `n/a` for a feature with no bug behind it.
- Rows marked "folded" or "locked" carry no checkbox. They are kept so the ID keeps resolving.
- Do not re-log anything in the SHIPPED section.

## Session plan — THE FOUR LANES, approved 2026-09-06

**This is the live plan. The 2026-09-05 table below it is the previous block and is history — do not open a session from it.** Worktrees created and baselined 2026-09-06, all four off `2c8087e`, each a fresh lockfile-faithful `npm ci`: TypeScript 5.8.3, **99 tsc error lines**, 71 test files / 1222 tests passing. That 99 is the gate and it is comparable across all four (T14). Merge `origin/main` on open — they were cut before `186d2e1`.

| Lane | TODO IDs | Worktree | Branch | Owns | Must not touch | Type | Parallel with |
|---|---|---|---|---|---|---|---|
| **R** | P0-9 (a)–(e) | `~/mrc-guard-fix` | `fix/guard-hook-bypasses` | `.claude/hooks/block-supabase-prod.sh`, `scripts/test-supabase-guard.sh` | all `src/`, `supabase/`, `api/`, `.claude/settings.json` | build-now | all three |
| **G1 — RE-SCOPE BEFORE OPENING** | P0-0 / P1-14 (P0-10 downgraded to P1, and ruled out as P0-0's cause by a zero-row Studio count on 2026-09-06) | `~/mrc-status-enum` | `fix/orphaned-lead-statuses` | `src/lib/statusFlow.ts`, `src/pages/LeadDetail.tsx`, `src/pages/LeadsManagement.tsx` (statusOptions only) | `api/`, `supabase/`, `src/auth/**`, `src/contexts/AuthContext.tsx`, `src/lib/calculations/pricing.ts` | **investigate-first** | all three |
| **PDF** | T13, then P0-A / P2-18 / MRC-APP-1A | `~/mrc-pdf` | `fix/pdf-hard-save` | `api/render-pdf.ts`, `api/render-job-report-pdf.ts`, `api/_shared/reportHash.ts`, new `tsconfig.api.json`, the `scripts` block of `package.json` | all `src/` (`StalePdfBanner.tsx` read-only), `supabase/functions/` | T13 build-now; the rest **investigate-first** | all three |
| **L** | P0-5 | `~/mrc-send-email` | `fix/send-email-auth` | `supabase/functions/send-email/**`, `src/lib/api/notifications.ts` | every other EF, all other `src/` | **investigate-first** | all three |

**Every one of these four lanes ends with a Codex review, not a diff.** Stop before the PR, hand Michael the `READY FOR CODEX REVIEW` block, and wait — you cannot run the command yourself (`disable-model-invocation: true`). See "CODEX REVIEW IS NOT OPTIONAL ON CODE" in `CLAUDE.md`.

All four are file-disjoint — verified path by path, not assumed. Review type per lane: R and L are security-adjacent so use `/codex:adversarial-review --base main`; G1 and PDF use `/codex:review --base main`. Split anything over ~150 lines before reviewing. G1's full P0-0 collapse will exceed that and must be split.

Not in this block, and why: **Q** (P0-8) is blocked by file overlap — the archive handler lives in both `LeadsManagement.tsx` and `LeadDetail.tsx`, so it waits for G1's successor and H. **J** is not worth a session until the deploy freeze lifts. **H** is gated on Vryan. **I** and all of P1 wait on Clayton's demolition table (B1). **P1-22 offline** is deliberately held.

## Session plan — approved 2026-09-05 (SUPERSEDED, kept as history)

**Superseded by the four-lane table above on 2026-09-06.** Rows O, N and F are done and their worktrees are gone; `~/mrc-lead-view`, `~/mrc-health`, `~/mrc-archive` and `~/mrc-pricing-plan` were never created. Read this table for what was decided on 5 Sep, never to open a session.

One session per worktree, each opened with the Session Handoff Prompt plus its own brief. No two sessions own the same file; the two provisos are J's carve-out of `send-email` for L, and Q's pre-flight.

| Order | Session | Purpose | Worktree | Branch | Owns | Must not touch | Parallel with |
|---|---|---|---|---|---|---|---|
| 1 | O | P1-22 offline: investigation only, written report. Runs first because its findings can move every other priority | `~/mrc-offline` | `docs/offline-investigation` | one new `docs/` report | all code | everything |
| 1 | N | Node 24 pin. Ran as a 1 Oct deadline item; the deadline turned out not to apply — dashboard was already 24.x | `~/mrc-node22` | `chore/node-24` | `package.json` engines, `.nvmrc`, the Node version lines in `docs/DEPLOYMENT.md` and `docs/DEVELOPER-GUIDE.md` | everything else; the Vercel project setting is Michael's | everything |
| 2 | G | One lead view: map every render condition, find why two paths exist. Absorbs P1-14 | `~/mrc-lead-view` | `fix/one-lead-view` | `src/pages/LeadDetail.tsx`, `InspectionDataDisplay.tsx` | `src/pages/LeadsManagement.tsx`, pricing, auth, EFs | H, F, N, J, O |
| 2 | H | Lead list: real pagination, true count, P1-15. Merge gated on Vryan (P0-2) | `~/mrc-lead-list` | `fix/lead-list-pagination` | `src/pages/LeadsManagement.tsx` | `LeadDetail.tsx`, EFs, migrations | G, F, N, J, O |
| 2 | F | Worktree cleanup, guard hook flip, P0-7 MCP pin, T8, T12 | `~/mrc-app-1` main checkout | `chore/guard-hook-flip` | `.claude/hooks/`, `.claude/settings.json`, `scripts/`, `~/.claude.json` | any `src/`, EFs | G, H, N |
| 3 | G2 | Collapse to one lead view | same as G, after G's plan is approved | same as G | same as G | same as G | H, F |
| 3 | J | Health-check P0-A to P0-E: 200-on-failure audit, then fixes | `~/mrc-health` | `fix/ef-fail-loud` | `supabase/functions/**` except `send-email`, `calculate-travel-time`, `send-inspection-reminder`; the attribution manifest | the three excluded EFs, all `src/` | G, H, N, O, L |
| 3 | L | P0-5 send-email relay | `~/mrc-send-email` | `fix/send-email-auth` | `supabase/functions/send-email/**`, `src/lib/api/notifications.ts` | every other EF, all of `src/` **except** `src/lib/api/notifications.ts` | G, H, N, J |
| 4 | Q | P0-8 archive cancels bookings, plus guarded PROD repair | `~/mrc-archive` | `fix/archive-cancels-bookings` | archive handler, located in pre-flight; repair SQL as a Studio pack | unknown until located | not with G or H until the handler's file is known; waits for G2 and H if it lives in their files |
| 5 | I | Pricing rebuild plan only. Last; depends on Clayton's demolition table (B1) | `~/mrc-pricing-plan` | `docs/pricing-rebuild-plan` | one new `docs/` plan | `pricing.ts` (read only), all code | runs alone |
| separate repo | K | OKF setup for `~/okf/projects/mrc/`, after this branch is pushed | the okf repo | n/a | `~/okf/projects/mrc/**` | `~/okf/concepts/`, `~/okf/_pending/` | everything |
| Michael | — | P0-1 Studio apply, off-peak; P0-6 remove the PROD key from DEV Vault | none | none | dashboard / Studio | | |

**Correction to the L row (2026-09-06).** L's Must-not-touch cell read `every other EF, src/` — which excluded the one `src/` file L must change. Adding authentication to `send-email` means changing how it is called, and there is exactly one caller: `supabase.functions.invoke('send-email', …)` at `src/lib/api/notifications.ts:331`. Every other `send-email` reference in `src/` is a toast id, a route query param or a comment, not an invocation. So the single chokepoint is the file L was forbidden to open. Corrected in both cells: `notifications.ts` is now owned by L and carved out of the prohibition. Same defect as the G/H rows below and the same consequence — a session that follows its brief literally either stalls or ships a half-change, and one that ignores it is editing an unowned file.

**Correction to the G and H rows (2026-09-06).** Both rows named two files that do not exist in this repo: `Leads.tsx` and `lib/api/leads.ts`. The lead list is `src/pages/LeadsManagement.tsx` (1,327 lines), and **there is no `src/lib/api/leads.ts`** — `src/lib/api/` holds `inspections.ts`, `invoices.ts`, `jobCompletions.ts`, `jobReportPdf.ts`, `jobReportPipeline.ts`, `reportPipeline.ts`, `apiClient.ts`, `fieldEditLog.ts` and nothing for leads. Lead queries are inline in the page and in `src/hooks/`. Corrected above in both the Owns cell (H) and the Must-not-touch cell (G). This table is what a session reads to learn which files it owns, so a wrong entry here is how two sessions end up in the same file — the exact failure the "no two sessions own the same file" rule exists to prevent.

**Session F — also in scope (logged 2026-09-05, Session P):** F also distributes `AGENTS.override.md`. Only 4 of 21 worktrees carry it (`mrc-lead-list`, `mrc-merge`, `mrc-node22`, `mrc-offline`). The other 17 load `AGENTS.md`, which is 100% GitNexus boilerplate with zero repo facts — so a Codex review run from any of them has no reviewer brief, no 13% cap, no branch model, no Supabase fact. Same 17 worktrees as the dangling guard hook. One sync fixes both.

## ⏰ Hard deadline — 1 October

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| N | Node 24 pin. **Not a deadline item — the 1 October deadline never applied to this project.** The Vercel dashboard was already set to `24.x` (Michael, dashboard read 2026-09-05), and `e2a6b7a` had already recorded the two Chromium PDF functions running on `nodejs24.x` back in May. The backlog line "Vercel drops Node 20 on 1 Oct; `mrc-app` builds fail after that date" was right about Vercel and wrong about us: Vercel does disable Node 20 on 1 Oct for Builds and Functions, erroring new deployments while already-deployed functions keep serving — but we were never on 20. Shipped anyway, because the version was invisible from the repo: `engines.node` now pins `24.x` in git and **overrides** the dashboard, so the major is a reviewable fact instead of a setting nobody can see from a checkout. **`.nvmrc` is inert on Vercel** — Vercel reads only Project Settings and `engines.node`, never `.nvmrc` or `.node-version`; it ships so `nvm use` picks the same major locally, and it is not the fix. Chose 24 over the originally scoped 22: 24 is Vercel's default and the only Active LTS (EOL Apr 2028 vs Apr 2027 for 22, already in maintenance), and zero of 1147 lockfile entries exclude it against four with floors inside 22.x (`@sparticuz/chromium` needs >=22.17.0). Note `production` is still unpinned until `main` is merged into it. | [x] | `chore/node-24` / N | 2026-09-05. Local, Node v24.20.0 / npm 11.19.0: `npm ci` zero EBADENGINE, `npm run build` green, `npx tsc -p tsconfig.app.json --noEmit` 99→99 error lines byte-identical. Vercel Preview deploy on merge `e44ceda` green. **Runtime proven end to end**: INS-2026-0001 regenerated on the preview — 16 pages, "Report generated successfully", zero console errors — so puppeteer-core + @sparticuz/chromium executed on Node 24 in a real request, not just at build time. | n/a |

N is closed and the deadline is cleared: real for Vercel, never real for this project. No live 1 October deadline remains.

Google Maps Platform ToS changes 28 Sep. Noted in the backlog, no action identified.

## 🔴 P0 — Critical / live damage

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P0-0 | Two different lead views exist. Customer A's lead renders 9 sections; Customer B's renders everything. Same status. Not a data problem. Investigate, then collapse to one render path. **THE CAUSE IS UNKNOWN AGAIN (2026-09-06).** P0-10, the orphaned `lead_status` values, was the strongest candidate mechanism and **a Studio count has ruled it out**: zero leads exist in any of the four orphaned statuses, so Customer A's lead cannot be in one and the undefined-`STATUS_FLOW`-lookup mechanism cannot be what produced the 9-section render. Do not re-derive this — the query was run, the answer was zero. What survives from that investigation is only the narrower fact already in the row below: `LeadDetail.tsx:500–543` gates rendering and the nulling of customer financial data on hardcoded positions in `ALL_STATUSES`. That is still a real two-render-path mechanism and is still the place to start, but it has not been shown to produce *this* symptom. **G1 was scoped around the P0-10 hypothesis and must be re-scoped before it opens** — back to the original Session G framing: map every render condition in `LeadDetail.tsx` and find why two paths exist, with no hypothesis presumed. | [ ] | G1 — **needs re-scoping** | | |
| P0-1 | `pdf-assets` / `pdf-templates` anonymously writable on PROD. Fix written and DEV-rehearsed on `fix/pdf-assets-anon-write-rls`. Michael applies it in Studio, off-peak. Not a session. | [ ] | Michael (Studio) | | |
| P0-2 | Lead list count is fake and "Load more" hides leads. Count is derived from loaded rows, not a real `COUNT`. GATE ON MERGE: about 80 uncontacted leads surface at once when this lands. H does not merge until Michael has told Vryan and there is a plan for working them. | [ ] | H | | |
| P0-3 | AI summary review not visible on completed inspections. Folded into P0-0. | — | | | |
| P0-4 | Customer A — no details, can't download report. Folded into P0-0. Reproduced and diagnosed 4 Sep. | — | | | |
| P0-5 | `send-email` EF is an unauthenticated relay with a spoofable `from`. Own scoped session (L). Does not wait for J. | [ ] | L | | |
| P0-6 | DEV Vault holds a PROD-valid `service_role_key` (SF-5). DEV cron received HTTP 200 from PROD. Standalone and first; T5 absorbs the rest of environment separation. | [ ] | | | |
| P0-7 | Supabase MCP has no project ref pinned. Defaults to PROD; already caused a test INSERT on PROD. **Resolved by removal, not by pinning.** There were two unscoped entries, both bare `https://mcp.supabase.com/mcp` with no `project_ref`, i.e. account-scoped and PROD-capable: (1) `~/.claude.json` under project `/Users/michaelyoussef/Mould/mrc-app`, and (2) the `supabase@claude-plugins-official` plugin v0.1.15, which was the one actually live. **Removal beats pinning because entry 2 could not be durably pinned** — its MCP definition lives in `~/.claude/plugins/cache/`, which is vendor-owned and overwritten on upgrade. Removal is the *impossible* tier of `docs/POST_INCIDENT_FRAMEWORK.md` rather than *refused*. **Second finding:** entry 1 was registered against `~/Mould/mrc-app`, the path CLAUDE.md forbids as a working directory — which is why an unpinned PROD-capable MCP never appeared in any worktree's tool list and stayed invisible. **Trade-off accepted knowingly (Michael, 2026-09-05):** disabling the plugin also disables the skills `supabase:supabase` and `supabase:supabase-postgres-best-practices`. An unpinned MCP defaulting to PROD has already caused one unintended production write; schema-lookup convenience does not outweigh that. Reversible with one edit, and re-enabling should be deliberate and per-job rather than always-on. | [x] | F | 2026-09-05. Entry 1: `jq del` on `.projects."/Users/michaelyoussef/Mould/mrc-app".mcpServers.supabase` — servers 8 → 7, all 18 projects preserved, `grep -c mcp.supabase.com` on the file now **0**. Entry 2: `enabledPlugins."supabase@claude-plugins-official"` set to `false` in `~/.claude/settings.json` — 17 top-level keys before and after. Both files backed up to `*.bak-20260906-000301` first, both validated with `jq -e` before being written. | n/a |
| P0-8 | Archived leads still hold technician slots and send reminders. Acceptance is both halves: archiving cancels bookings and reminders from now on, AND existing archived leads on PROD are repaired. The repair SQL is guarded: read-only preview first, Michael approves the row count before anything writes. Session Q. Pre-flight: locate the archive handler first; if it lives in LeadDetail.tsx or Leads.tsx, Q waits for G2 and H. **PRE-FLIGHT ANSWERED 2026-09-06 — Q IS BLOCKED, AND NOT BY A PERSON.** There is no single archive handler: there are two, in both of the files the pre-flight named as blockers. `src/pages/LeadsManagement.tsx:551` (`confirmArchive`) and `src/pages/LeadDetail.tsx:707` (`handleDelete` — misnamed, it archives, it does not delete). Both write `archived_at: new Date().toISOString()` to `leads` and nothing else; neither cancels a booking, and neither touches reminders. They are also not equivalent: only the `LeadsManagement` path writes an `activities` row (`activity_type: 'archived'`), so archiving from the lead detail page leaves no timeline entry. So the pre-flight condition fires on both halves at once and Q waits for G2 **and** H. Do not re-derive this; the handler has been located. Note for whoever picks Q up: the fix is probably one shared `archiveLead()` helper rather than two parallel edits, but extracting it still writes to both files, so it does not dodge the sequencing. | [ ] | Q — blocked on G2 + H (file overlap) | | |
| P0-9 | **The Supabase guard hook has three confirmed HIGH bypasses.** Found by Codex adversarial review of `8984fed` and independently reproduced with synthetic payloads (7/7 bypassed; no CLI run, no database touched). See `docs/codex-review-log.md` round 4. **P0 because a guard that permits an explicit PROD deploy is worse than no guard — it is trusted.** Four fixes, all in `.claude/hooks/block-supabase-prod.sh` unless stated: **(a)** rule 1, the PROD-ref check, must run BEFORE CLI detection, not after. Today it sits downstream of the regex at line 50, so the most important check in the file is gated behind the weakest one — `/opt/homebrew/bin/supabase functions deploy foo --project-ref <PROD>` returns ALLOW, and `supabase` really is at that path on this machine. Also bypassed: `env supabase ...`, and anything where `supabase` follows a newline, because line 34 collapses newlines and destroys the segment boundaries the regex depends on. **(b)** Split the command string and evaluate each `;` `\n` `&&` `||` segment independently. One permitted command must not authorise the rest: `supabase --version; supabase db query --linked -f x.sql` returns ALLOW, as does `migration list; db query`, even though `db query` is the one command deliberately excluded from the allowlist and is exactly what caused incident 1. **(c)** cwd resolution must not prefer `CLAUDE_PROJECT_DIR` over the payload cwd (line 98). The harness always sets it, so payload cwd is effectively dead code; the hook then verifies one worktree's link while the CLI resolves from another. `cd /prod-tree && supabase migration list --linked` bypasses too. **(d)** `scripts/test-supabase-guard.sh` line 87 asserts `"supabase --version; echo x"` → ALLOW. That assertion is wrong and must be **inverted, not deleted** — the suite currently pins bypass (b) as correct behaviour. Add deny regressions for all seven reproduced forms. **(e)** The guard also **over-blocks**: it denied a command whose only match was the literal string `(supabase refs)` inside an `echo`, because the CLI regex treats `(` as a command-segment start. Observed live 2026-09-05 in this session. Benign — it fails closed — but it refuses legitimate work, which is the same class as incident 3, and it is also live proof the distribution works. Fixing (b) by splitting on segment boundaries should fix this too; verify it does rather than assuming. **Context the fix must account for:** there are **at least three variants of this hook in the wild**. `~/mrc-cost-estimate` runs a 78-line copy with no rule 3.5 whose hash (`0a15054`) matches *neither* the current version (`b42aecf`) *nor* the immediately-prior one at `8eedd28^` (`58b75ab`), and nobody knew it existed. Any fix must establish what each worktree is actually running before assuming a single baseline. **Adjacent finding, permission layer rather than the guard (found 2026-09-05 while auditing after P0-7, fixed same day):** `autoMode.allow` in `~/.claude/settings.json` pre-approved `npx supabase functions deploy --project-ref <DEV>`, contradicting CLAUDE.md's unconditional rule that Edge Function deploys are human-applied — CC prepares the command, Michael runs it, with no DEV carve-out. Guard test case 12 permits a DEV-targeted deploy **by design**, so both layers said yes: the guard was working correctly and the permission layer was authorising something the project rule reserves for a human. Auto mode self-enabled six times that night, so this was live config, not hypothetical. Removed from `autoMode.allow`. **Structural point worth carrying:** `autoMode` has no `deny` key at all, only `soft_deny` — nothing in that block is a hard refusal. The two PROD entries that lived there (`functions deploy` and `db dump`) were therefore constrained only while auto mode was on, leaving this guard — the one with three confirmed bypasses — as the sole real control the rest of the time. Two layers that both depend on one flag being set is not defence in depth. Both were moved to `permissions.deny` as hard denies (deny 15 → 17); `Git push to production branch` was left in `soft_deny`, since the push hook covers it separately. | [ ] | R | | |
| P0-10 | **Four `lead_status` values exist in the database enum but in no TypeScript surface.** The DB enum carries 20 values; `LeadStatus`, `STATUS_FLOW` and `ALL_STATUSES` (all `src/lib/statusFlow.ts`) and `statusOptions` (`src/pages/LeadsManagement.tsx:71`) each carry 16. The four orphans are `hipages_lead`, `contacted`, `inspection_completed` and `inspection_report_pdf_completed`. **DISPROVEN as the cause of P0-0 (2026-09-06).** It was the strongest candidate — a mechanism that produces exactly P0-0's symptom — but the Studio count returned zero rows in all four statuses, so no lead can be in one and this cannot be what renders Customer A's 9 sections. The trace that would have promoted it from suspected to confirmed was never possible. Verified 2026-09-06 by reading the code; no PROD query was run. **(1) No pipeline tab.** Absent from `statusOptions`, so a lead in one of these statuses is reachable only under the All tab — every other tab filters on `lead.status === statusFilter` (`:479`). **(2) `LeadDetail.tsx:621` throws.** `currentConfig.shortTitle` is unguarded and `currentConfig` is `STATUS_FLOW[lead.status]` from `:514`, so changing the status of such a lead is a TypeError. Note the asymmetry: every *render* site of the same lookup (`:1269`, `:1288–1293`, `:1458–1471`) IS optional-chained, so the status card degrades to grey with an empty title and empty next-action instead of crashing. One undefined lookup, two different failure modes — which is why this can look like "renders fewer sections" rather than like an error. **(3) Reversion field-clearing silently never runs.** `ALL_STATUSES.indexOf(lead.status)` returns -1, so `oldRank` is -1 and `isReversion` (`:517`) can never be true. **(4) `hipages_lead` is live, not dead.** Still read in four places — `AdminSidebar.tsx:51`, `useUnassignedLeads.ts:43`, `useAdminDashboardStats.ts:99`, and `deepLinkLeadReason.ts:79` treats it as a new-lead status for the schedule rail. Nothing writes it any more: `CreateNewLeadModal.tsx:309` and `receive-framer-lead/index.ts:825` both write `new_lead`. So any rows are legacy, from before the value stopped being produced. **DOWNGRADED TO P1 on 2026-09-06 — the full row now lives in P1 Workflow & data. Kept here so the ID keeps resolving.** The Studio count came back **zero**: no lead exists in any of the four orphaned statuses. | — | see P1 | | |
| P0-11 | **Lead status advances to "Report Sent" on admin approval, not on confirmed delivery.** Observed live on lead `MRC-2026-0174`: status reads **Report Sent** with **zero emails in Resend**. The hard-save failed, so no report was ever generated and nothing was ever delivered — the pipeline advanced anyway. An admin looking at the board sees that customer as served when they have not been, and there is no signal anywhere that contradicts it. This is the **same lead and the same failure** as the 2026-09-07 P0 (`/api/render-job-report-pdf` 504/500): the render endpoint failing is what produced the empty Resend result, so the status is not merely optimistic, it is contradicted by a known failure the system already logged. **Distinct from P0-B and neither closes the other** — P0-B is an *email row* logged as sent without checking Resend; this is *lead status* advancing on approval rather than on a delivery confirmation. Fixing one leaves the other. Acceptance should be that the status transition consumes a delivery outcome, and that an already-advanced lead with no delivery is detectable and repairable. Logged 2026-09-07, not fixed. **Mechanism traced 2026-09-08 by code read (no DB query) — the assumed mechanism is NOT the one in the code, so do not start from it.** Within `src/` there is exactly **one** automatic writer of this status: `src/pages/ViewReportPDF.tsx:1265-1267`, and it runs *after* `await sendEmail(...)`, not before. `sendEmail` (`src/lib/api/notifications.ts:324-349`) throws unless the Edge Function returns `success: true`, and `supabase/functions/send-email/index.ts:244` returns that **only** after Resend accepts and returns an id, having written `email_logs` with `sent`/`failed` at `:226`. No Edge Function and no migration writes this status anywhere. So the automatic path *is* delivery-gated, and most likely did not produce the observed state. Three candidates, in order of likelihood: **(a)** manual selection — `job_report_pdf_sent` is a freely user-pickable option in the status dropdown at `src/pages/LeadsManagement.tsx:82` and requires no delivery evidence at all, which alone reproduces the symptom; **(b)** a Resend account or sending domain other than the one checked; **(c)** `email_logs` disagreeing with Resend. **Decisive next step is one Studio query pack** (`db query` is blocked per standing order): `email_logs` and `audit_logs` filtered to this lead id — `audit_logs` names who set the status and when, which separates (a) from the rest in a single read. Do that before designing any fix; the acceptance criterion above is still right, but the defect it should attach to is not yet established. | [ ] | | | |
| P0-12 | **Completed job shows Total inc GST $0.00 — no pricing on a completed job.** Observed live on lead `MRC-2026-0174`, the same lead as P0-11. A completed job with a zero total means either nothing was priced or the total is not being read back, and in both cases the customer cannot be correctly invoiced. **Logged separately from P0-11 on purpose** — they surfaced on the same lead but have no established shared mechanism, and folding them together risks one being closed by a fix that only addressed the other. Unknown whether this is write-side (never priced), read-side (priced but rendering zero), or a consequence of the failed job-report pipeline. Not investigated. **Touches pricing, so any work here runs `gitnexus_impact` on `calculatePrice` first and respects the frozen surface rule on `src/lib/calculations/pricing.ts`.** Logged 2026-09-07, not fixed. **Render site located 2026-09-08 by code read (no DB query):** the `Total inc GST` row is `src/components/leads/InvoiceSummaryCard.tsx:289`, and its value comes from `d.totals.total_amount` — not from a column of that name. The column `inspections.total_inc_gst` does exist (`src/integrations/supabase/types.ts:659`), as does an RPC `calculate_total_inc_gst` (`:2371`), so there are at least two candidate sources and the UI reads neither directly. **The same Studio pack as P0-11 settles write-side vs read-side in one query:** if `inspections.total_inc_gst` holds a non-zero value for this lead the defect is in the read path and is a UI change; if it is `0` or `NULL` the job was never priced and the defect is upstream in the write path. Do not begin until that is known — the two have nothing in common but the symptom. | [ ] | | | |
| P0-13 | **Quote PDF layout overlap — customer-facing, on live quotes, reported by Vryan 2026-09-10 7:48pm and 7:50pm and unanswered for two days.** Two quotes were reported. **Lead numbers were not captured for both because the report named customers rather than lead numbers** — one is `MRC-2026-0168`; the other is identifiable only as the quote totalling **$17,174.90 + GST**, and its lead number should be got from Vryan and written in here. Three distinct defects on one page, with **three different owners** — the reason this row is one P0 and not three is that a customer sees them together, but do not treat them as one fix. **(a) The EQUIPMENT COSTS block renders on top of Option 2's numbered list — items 8 and 9 are illegible** — and the "does not provide replacement services" disclaimer overlaps the same region. **Mechanism identified by code read 2026-09-11:** the block is absolutely positioned at a hardcoded offset, `src/templates/inspection-report-template.html:570` — `position: absolute; top: 695px; left: 38px; width: 390px` — so any Option 2 list long enough to reach 695px runs underneath it and nothing reflows. **Same failure family as P2-10** (cover clipping past ~25 rooms): fixed-position layout against variable-length content, so expect it to recur wherever that pattern is used rather than treating this as one bad offset. **(b) "Capped at 5 days" prints directly below "HEPA Air Scrubber: $100/day × 1 (14 days)"** — self-contradictory on the same customer quote, and per Michael's 2026-09-09 ruling **no cap exists at all**. This is **P2-24**, now with visual proof that it is live and wrong rather than a code read. Written at `supabase/functions/generate-inspection-pdf/index.ts:1925` (`html.replace(/\{\{equipment_max_days\}\}/g, '5 days')`) into `{{equipment_max_days}}` at `inspection-report-template.html:570`. **(c) Dehumidifier, air mover and RCD show rate × quantity with NO day count. This is P2-23, which is MERGED AND NOT DEPLOYED** — `ff2d24a` added `hirePeriod()` to all four equipment lines and is on `origin/main`, so the repo is already right and the live quote is not. **(c) therefore needs no code whatsoever: it needs a deploy of `generate-inspection-pdf`. Do it first** — already-reviewed code, and it removes one of the three defects immediately. **MICHAEL TASK, NOT A CODEX LANE.** (a) and (b) live in the Edge Function and the Storage template, both SACRED and both human-applied; and per **P2-17** the live template is read from the `pdf-templates` bucket, not from git, so editing `src/templates/inspection-report-template.html` is **inert** until the object is swapped. Explicitly excluded from the 11–14 Sep Codex window (`docs/CODEX_WEEK_PLAN.md`). **Scope of verification, stated so it is not overread: the three mechanisms were confirmed by code read on 2026-09-11; the rendered PDFs themselves were NOT viewed.** Logged 2026-09-11. | [ ] | Michael (deploy, then EF + Storage template) | 2026-09-11, code read: `inspection-report-template.html:570` absolute positioning; `index.ts:1925` hardcoded `'5 days'`; `ff2d24a` merged and not deployed. PDFs not viewed. | |
| P0-14 | **LIVE: job booking confirmation silently blocked by the recipient cooldown.** `MRC-2026-0152`, 11 Sep, job on Monday: report-approved email sent **13:42:33**, `job_booked` activity **13:44:32**, confirmation attempt **13:44:33 → 429** from `send-email` itself. Resend has exactly **one** send to the recipient that day, none at 13:44; `email_logs` has **no row for the blocked attempt**. **Not self-healing; Michael is sending manually, defect remains open.** `supabase/functions/send-email/index.ts:176–186` checks any `sent` email to that recipient in five minutes, without distinguishing template/event. **`:188–192` returns 429 before `sendWithRetry` at `:212` and the sole `email_logs` insert at `:222–234`; the normal return bypasses both the insert and catch.** Provider retries therefore never run. `BookJobSheet.tsx:571–579` catches to console only. **Fix shape:** durably record the blocked attempt (at minimum failed status, 429/cooldown reason and lead/template attribution; check the log write), expose delivery failure to the operator, and distinguish legitimate transactional events with event idempotency plus durable deferred retry when throttling is required. Acceptance: report approval followed two minutes later by booking must not silently lose the confirmation; an actual cooldown rejection must leave an audit row. | [ ] |  | 2026-09-11, Michael: PROD activity/email_logs and Resend; repository trace |  |

## 🟠 P1 — Pricing engine rebuild

Rules and tables are in `docs/PRICING_CANON.md`. Nothing in P1 is built this round: Session I is plan only. P1-2 to P1-13 may be planned with the demolition rows marked INTERPOLATED; nothing ships before Clayton's demolition table (B1) is in.

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P1-1 | Load the new rate tables (Surface, Construction Site, Demolition, Subfloor; 2h–8h). | [ ] | | | |
| P1-2 | Commercial property type plus the 25% surcharge, folded into labour, never a visible line. | [ ] | | | |
| P1-3 | Industrial and Construction added to Premises Type. | [ ] | | | |
| P1-4 | Multi-day = day-rate multiples. Every day at the day-1 8h rate, no step-down. Retire the `dayRates` arrays. | [ ] | | | |
| P1-5 | Mixed jobs = total hours at the highest category present. Reworks Session C. | [ ] | | | |
| P1-6 | Session C rework and merge, on the existing branch `fix/option-stacking-equipment-days`. The equipment Days multiplier on it survives; only the labour half is rebuilt to the highest-category rule. Also carries the todo-log branch finding that `BookJobSheet` prints equipment days from the booking schedule, not the quote; read the quote's resolved days from the branch's `deriveEquipmentDays()`. **BookJobSheet half FIXED 2026-09-08** on `fix/booksheet-equipment-days`, raised as a defect by the terminal production gate (thread `01a08056-77a6-7980-be9f-d28b17c6f6e1`) and **verified by execution before the fix** — the pre-change DOM rendered `Equipment (× 1 day)` against a saved `equipment_days` of 4, so the claim is no longer UNVERIFIED. The sheet now selects `equipment_days`, and its heading reads `Math.max(equipment_days ?? 0, deriveEquipmentDays(saved labour hours))` — the same floor `reconcileLoadedEquipmentDays` applies, so legacy rows carrying the column default of 1 still fall back to labour-derived, and the booking form's own hours stepper (which moves the schedule, not the invoice) no longer moves the equipment heading. Regression test: `src/components/leads/__tests__/BookJobSheet.equipmentDays.test.tsx`, which projects the mocked row to the selected columns, so dropping `equipment_days` from the select fails the test rather than only the render. **The row stays open:** T16(b) — `mrc-cost-estimate` carrying the tracked guard hook — is still assigned here, and the Session C labour-half rework is tracked by its own merge. **WENT LIVE WHEN UNIT C MERGED:** before C nothing shipped could set an explicit day count, so every stored `equipment_days` reconciled back to the labour-derived value and the schedule-derived heading could never disagree with the quote. C's Section 7 Days stepper is the first writer, so the two figures can now differ on a real job and the sheet the technician works from can contradict its own quote — which is why this stopped being safely separable. Priority rises accordingly; do not read the original logging date as the measure of urgency. | [ ] | | | |
| P1-7 | Subfloor in BOTH options. Option 1 = surface + subfloor, Option 2 = demolition + subfloor. | [ ] | | | |
| P1-8 | Minimums and premiums (bathroom condensation 1h min, non-bathroom 2h min, demolition premium). | [ ] | | | |
| P1-9 | Fixed fees as separate line items (paid inspection, weekend callout, travel beyond 50 km). Never discounted. | [ ] | | | |
| P1-10 | Loyalty discount engine plus agency tagging. Per-agency property counter, labour only. Auto cap 20%, manual cap stays 13%. This session also rewords CLAUDE.md, `.claude/rules/australian-compliance.md` and `pricing.ts` so that "13%" reads as the manual cap only. | [ ] | | | |
| P1-11 | Technician cost-estimate view must match report pricing, including overrides. Acceptance: one pricing function, both surfaces call it. Same numbers by coincidence is what exists today, and it drifted. | [ ] | | | |
| P1-12 | Quote format: `$X,XXX.XX + GST` everywhere, keep the odd cents. | [ ] | | | |
| P1-13 | Print pricing chart for Glen and Clayton to confirm, generated from the live tables. | [ ] | | | |

## 🟠 P1 — Workflow & data

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P0-10 | **Four `lead_status` values exist in the database enum but in no TypeScript surface.** ID keeps its P0 prefix because IDs are stable and never reused; the priority is **P1**. Downgraded 2026-09-06 on a Studio count of **zero rows** — no lead is in `hipages_lead`, `contacted`, `inspection_completed` or `inspection_report_pdf_completed`. **Real defect, zero current impact. It is latent, not harmless:** the moment anything writes one of those values, that lead is unreachable from every pipeline tab (absent from `statusOptions`, `LeadsManagement.tsx:71`, and every tab filters `lead.status === statusFilter` at `:479`) and `LeadDetail.tsx:621` throws a TypeError on any status change, because `currentConfig.shortTitle` is unguarded while every render site of the same lookup is optional-chained. `ALL_STATUSES.indexOf()` also returns `-1`, so the reversion field-clearing at `:517` can never fire. The DB enum carries 20 values against 16 in `LeadStatus`, `STATUS_FLOW` and `ALL_STATUSES` (`src/lib/statusFlow.ts`). `hipages_lead` is still *read* in four places — `AdminSidebar.tsx:51`, `useUnassignedLeads.ts:43`, `useAdminDashboardStats.ts:99`, and `deepLinkLeadReason.ts:79` treats it as a new-lead status — but nothing writes it: `CreateNewLeadModal.tsx:309` and `receive-framer-lead/index.ts:825` both write `new_lead`. So the exposure is a future writer, not an existing row. Worth fixing; not urgent; causing no damage today. **This is NOT the cause of P0-0 — see that row.** | [ ] | | 2026-09-06, Michael, Studio: zero rows across all four statuses | |
| P1-14 | Internal notes disappear on some lead statuses. Status-dependent render bug, the same suspected mechanism as P0-0. Investigated inside Session G; splits back out only if G proves otherwise. | [ ] | G | | |
| P1-15 | Search leads by Lead ID. Same files as the lead list. | [ ] | H | | |
| P1-16 | Repeat-client quick create, prefilling address and contact. | [ ] | | | |
| P1-17 | Attachments on the job file, separate from lead notes. | [ ] | | | |
| P1-18 | **CLOSED 2026-09-11. Glen was told on 2026-09-09: pricing is visible to everyone, admin and technician.** The only outstanding action on this row was the telling — the decision itself was locked long before — so the row closes on the conversation, not on a code change. Original text, kept: All users get admin and technician access. Decision locked. Glen asked on 25 Aug for pricing to be visible only to him and Clayton; Appendix H of `docs/MRC_MASTER_BACKLOG.md` marks it "⚠️ superseded — everyone is admin now" and nothing was sent back to him. Verified in code 2026-09-08: no pricing field is role-gated anywhere — `hasRole('admin')` gates edit affordances on `src/pages/LeadDetail.tsx` only. Until the telling happened he was entitled to think the request was still queued; as of 2026-09-09 he is not. | [x] | | 2026-09-09, Michael told Glen directly — pricing visible to admin and technician alike | n/a |
| P1-19 | Glanz onboarding: admin and technician, app and Slack. Blocked on B2 (Glanz email and mobile). | [ ] | | | |
| P1-19b | Mobile nav bar detaches from the bottom and floats mid-screen. Folded into P1-20 (ruling 2026-09-05). | — | | | |
| P1-20 | Floating nav bar detaches from the bottom. Intermittent. Carries the ancestor-transform diagnosis (position:fixed silently becoming position:absolute; see that bug class in `docs/BUG_LEDGER.md`). P1-19b folded in. | [ ] | | | |
| P1-21 | "Recommend Dehumidifier Hire" toggle: investigate what it writes and whether it reaches the PDF or pricing, then rename or remove. | [ ] | | | |
| P1-22 | Offline does not work the way the team believes. The Dexie layer has zero production callers and the offline photo queue is wired to nothing (old TODO.md L1149–L1159, L1802–L1826), while technicians work in basements with no signal. Own session (O), investigation first: does any offline path work today, and what happens right now when a technician loses signal mid-form? Nothing is built until that is answered. Promoted from debt 2026-09-05. **ANSWERED 2026-09-05 — see `docs/OFFLINE_INVESTIGATION.md`. Any fix must address THREE independent defects: (1) the localStorage write never fires, (2) the restore path crashes when it renders, (3) the auth gate blocks a cold-cache offline mount. Fixing one or two does not give the team working offline. Item 1a — honest banner copy — shipped separately in PR #126.** | [ ] | O | | |
| P1-23 | **`this_week` and `overdue` overlap on the technician My Jobs list, so one stale job is counted twice.** `src/hooks/useTechnicianJobs.ts:424` filters `this_week` as Monday→Sunday with **no `>= today` clamp**, while `:416` applies exactly that clamp to `this_month` and the comment at `:405` claims the tabs are non-overlapping — the author knew the clamp was needed and it is missing from one of the two. A job scheduled six days ago and never completed therefore sits in **This Week and Overdue at the same time** and inflates both badge counts (`:440`, `:442`). The tab is labelled "This Week" with no date range. `/admin/schedule` is NOT affected: it prints real date ranges (`src/components/schedule/ScheduleHeader.tsx:163` via `formatWeekRange`). **Probably Glen's 6 Sep report, unconfirmed** — he said "schedule", and the Admin Dashboard "Completed This Week" tile is the other candidate; get the screenshot before building. Distinct from L832, which is that tile's `leads.updated_at` filter (`src/hooks/useAdminDashboardStats.ts:106`) — different screen, different mechanism. Logged 2026-09-08. | [ ] | | | |
| P1-24 | **Pending email confirmation can send completion A's report under completion B's identity.** `src/pages/ViewReportPDF.tsx:1333-1336`. Open email approval for completion A and let the mismatch check return A's saved version. While the dialog is still open, refetch the completion query to B, then choose `send_as_is`: `jobMismatchVersion` still holds **A's** version, but the newly rendered handler calls `performJobReportSend` against **B's** completion — so the send signs **A's** storage path and names the attachment with **B's** job number. A customer receives another customer's report under their own job heading. **UNVERIFIED** — Codex's own word. Static reproduction only, no execution; it did not run the path. Confirm the reproduction before building. **Acceptance criterion: a test that mocks the mismatch and send responses and asserts `sendEmail` is NOT called after the identity change.** The current path calls it. Suggested shape of the fix (Codex's recommendation, not a ruling): tag the pending send state with its originating completion id and reject confirmation when it differs from the current completion, including a mismatch result that arrives after a refetch. **Same defect class as branch `fix/viewreport-history-guards`, one path over** — state pinned to one completion outliving that completion. `fix/viewreport-history-guards` closed two instances on the *preview/export* path, both line refs against `origin/main` `dab41ba` as they read BEFORE that fix: (a) `ViewReportPDF.tsx:3034-3038`, the preview built from a truthy `jobHtmlUrl` while `jobCompletion` was null, dereferencing `jobCompletion!.id`; (b) `ViewReportPDF.tsx:308-311` interacting with `:570-575`, where a pinned history URL outlived its completion and the re-downloaded HTML was tagged with the NEW completion id, so both identity comparisons passed. This row is the *email send* path, which the preview guard does not cover and which `fix/viewreport-history-guards` did not touch — its hunks in that file are 235, 399, 579, 878, 2522, 2982, 2986, 3056, 3059, and 1333-1336 is in none of them. Found by the round-1 Codex review of `fix/viewreport-history-guards` at `5862bc6`, thread `01a07f94-1f81-77c2-b09b-a697228130ee`; accepted and deferred by Michael the same day, no round 2. Logged 2026-09-08. | [ ] | | | |
| P1-25 | **A real calendar with hours-in-day and per-technician availability, ServiceM8-style.** Recorded verbatim because it sets the priority: *"Very crucial on mobile"*. No current surface shows a day as hours, and there is no per-technician availability view, so a booking is made without seeing who is actually free and for how long. **Distinct from the two P2 calendar rows, and a prerequisite for both:** P2-1 is named free-text blocks on a calendar, P2-5 is auto-scheduler slot recommendations — each assumes the hours-and-availability view this row builds. Mobile-first at 375px is the primary requirement here, not a follow-up pass. Logged 2026-09-11. | [ ] | | | |

## 🟡 P2 — Equipment & scheduling (small version)

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P2-1 | Named calendar blocks: free-text name, block a time slot, not tied to a job. | [ ] | | | |
| P2-2 | Equipment pickup quick-fill: technician + job + equipment, saves a record, shows on the calendar. | [ ] | | | |
| P2-3 | Equipment unit and day selection on the job. Ties to the Session C Days multiplier. Absorbs the old TODO.md equipment-days work at L170 and its 11 sub-steps: extend the HEPA Days stepper pattern to Dehumidifier, Air Movers and RCD Box. | [ ] | | | |
| P2-4 | Two technicians on one job. 0a/0b/0c rehearsed on DEV. Blocked on B5. Separate session from P3-7; not run in parallel with it until someone has checked for file overlap. Also owns the `completed_by` decision and its two riders from the todo-log branch: photos on no-inspection jobs are visible only to the technician who wrote them (the RLS joins through `completed_by`), and the NULL-inspection photo visibility gap. Decide them together with who signs the completion report. | [ ] | | | |
| P2-5 | Auto-scheduler slot recommendations with travel-time estimates. | [ ] | | | |
| P2-33 | **Twelve stepper buttons render at `w-10` (40px) against the project's 48px minimum touch target.** The standard exists for gloved field use at a 375px primary viewport; these are 8px under it, and there are twelve of them on the one form. Lane A owns the fix (`src/pages/TechnicianInspectionForm.tsx`, `docs/CODEX_WEEK_PLAN.md`). **Verify at 375px in a browser, not by reading the Tailwind class** — the rendered box is what the standard is about, and padding or a parent can change it either way. Logged 2026-09-11. | [ ] | A / `fix/inspection-save-integrity` | | |
| P2-34 | **The technician job list does not show job TYPE, so a technician cannot tell what to load before leaving.** Demolition, surface treatment, subfloor and mixed jobs each need different equipment in the van, and the list distinguishes none of them — the type is only discoverable by opening the job. Straight operational cost: the wrong gear means a second trip. Logged 2026-09-11. | [ ] | | | |
| P2-45 | **The per-item blank-field conflict: "never prefill" and "blank means auto-days" cannot both hold on the same field.** A blank per-item days field has to mean exactly one of *the technician has not filled this in yet* or *derive the days automatically*, and the two rules currently claim it at once. **Settle this before the per-item form writer is built, not during** — the writer's zero-and-blank behaviour is precisely what P2-32 deferred its DB constraint on, and what P2-26 records as the same class (separate absent/zero *auto* mode from *invalid* explicit values, and surface the invalid rather than absorbing it). Deciding it after the writer exists means changing stored semantics on live rows. Logged 2026-09-11. | [ ] | | | |
| P2-46 | **An equipment line vanishes from the quote when a quantity is set but the days field is left blank.** The customer-facing effect is the damaging part: the item is silently absent from the quote rather than shown as incomplete, so equipment that will actually be deployed is never priced and never billed. It must still appear on the quote, priced off the auto-derived day count. **Depends on P2-45** — what a blank days field means is the question this row's fix has to answer, so do not fix it in isolation. Logged 2026-09-11. | [ ] | | | |

## 🟡 P2 — Report & PDF

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P2-6 | Infrared observations must print below the infrared section. | [ ] | | | |
| P2-7 | Infrared Observations section on the area page, under extra notes. | [ ] | | | |
| P2-8 | Moisture: plain number fields, no location on the PDF. Investigate first. Blocked on C3; reconcile it in the investigation. | [ ] | | | |
| P2-9 | Remove unused report pages (e.g. subfloor-only jobs printing empty sections). | [ ] | | | |
| P2-10 | PDF cover clips past ~25 rooms. Needs an EF change plus a Storage template upload. | [ ] | | | |
| P2-11 | Photo flow: button presses per area. Moved to Blocked as B6 (waiting on Clayton). | — | | | |
| P2-17 | The inspection PDF template has no sync path between the repo and Storage, so repo edits to it are inert: the Edge Function reads the template from the `pdf-templates` bucket, not from git. Documented as a trap in the backlog's Appendix I; this is the item that fixes it. One path that puts the repo template into Storage, verified by object metadata, not by re-fetch. Was PDF-CL18 on the `docs/todo-log-2026-08-31` branch. | [ ] | | | |
| P2-18 | **"PDF is out of date. Regenerate before sending to customer" banner persists after a successful regenerate.** Reproduced on INS-2026-0001 v10 (2026-09-05): the regenerate succeeded — 16 pages, "Report generated successfully", zero console errors — and the staleness banner stayed on screen. Two candidate causes, not yet distinguished: the freshness flag is not written on the success path, or it is written and the UI does not refetch after generating. Effect is the damaging part: admins see a stale-report warning on **every** freshly generated report, so the warning carries no information and will be ignored on the one occasion it is true. Diagnose before fixing — the two causes have different fixes and only one of them is a UI change. | [ ] | | | |
| P2-19 | **Property photo on the report cover renders as a broken image** — alt text "Property" with no image behind it. Observed on INS-2026-0001, same screen and same render as P2-18 (2026-09-05). Unknown whether the cover template references a path that no longer resolves, the photo is missing from Storage for this inspection, or the URL is signed and expired by render time. Note the related known trap in P2-17: the Edge Function reads the cover template from the `pdf-templates` bucket, not from git, so a repo-side "fix" to the template is inert until it is uploaded. | [ ] | | | |
| P2-20 | **375px verification outstanding on the job-report View button** (shipped `61c3940` + `cfb2fa0`, branch `chore/workflow-scaffolding`). The button opens the rendered report in a new tab via a Blob typed `text/html` so a report can be printed to PDF by hand while hard-save is down. It has **never been exercised in a browser** — logic is unit-tested (8 tests, proven to fail without the fix) and the deployed bundle was confirmed to contain it, but no one has clicked it. Session 1 could not run the check: both preview URLs sit behind Vercel SSO, and past that the preview origin has no app session, so an agent cannot authenticate. **Michael verifies manually after merge.** What to check at 375px: View renders beside Download in the mobile branch (`flex sm:hidden`); its box is >= 48x48 CSS px; `document.documentElement.scrollWidth <= 375`; clicking it opens a tab that RENDERS the report rather than showing its source (the BUG-23 / C11 failure); and switching version in history disables View until the new version loads (the `cfb2fa0` fix). Reset the service worker first or a green result is a false pass. | [ ] | `chore/workflow-scaffolding` / Session 1 | **Outstanding — bundle verified live, UI unverified** | BUG-23 (the defect the button routes around) |
| P2-21 | **Self-inline the Inter font into `job-report-template.html` and drop the `fonts.googleapis.com` dependency.** Tonight's P0 was `setContent` waiting on a lifecycle condition that never arrives; switching to `domcontentloaded` (`api/render-job-report-pdf.ts:202`, shipped 2026-09-08) fixes the symptom, but the render still depends on Google's CDN being reachable from the function region. **This is what makes that fix safe rather than merely effective — but what exactly goes wrong when the stylesheet stalls is UNRESOLVED, and both readings are recorded here because neither has been verified in a browser.** **Reading A (CC, 2026-09-08):** with `domcontentloaded` no `@font-face` is registered yet when the stylesheet is pending, so `document.fonts.ready` (`:204`) resolves *early*, and the PDF silently renders in the template's `sans-serif` fallback — a quiet typography regression nothing detects. **Reading B (Codex adversarial review, thread `01a07c3a-bf63-76c3-9ff0-9346ae8d4bde`, 2026-09-08):** Blink defers font readiness until after document load, so `:204` **hangs** rather than resolving early — and it is bounded by nothing this file sets (`protocolTimeout` defaults to 180 s against a 60 s ceiling), so the invocation is SIGKILLed before the render-error handler or the `finally` that closes Chromium. On that reading the 504 does not disappear, it moves off `setContent` and onto `:204`, and loses the JSON body and the browser cleanup on the way. Codex could not verify this — browser and Vitest execution were sandbox-blocked — and CC did not verify Reading A either. **Filed as BUG-25 (class C13), deferred with reason; T19 closes it from the timeout side.** **Self-inlining closes it from the other side, and is the only fix that makes the question moot** — with no external stylesheet there is no stall to resolve early or hang on. Settle A vs B with a browser test before relying on either. It also collapses **three** font waits to one local one: the `setContent` lifecycle wait, the explicit `document.fonts.ready` at `:204`, and `page.pdf()` itself — `PDFOptions.waitForFonts` **defaults `true`** (`node_modules/puppeteer-core/lib/types.d.ts:8210`) and is not overridden at `:206-211`. **Hard prerequisite: P2-17.** The live template is not in the repo — the Edge Function reads it from Storage at runtime (`supabase/functions/generate-job-report-pdf/index.ts:10`, `pdf-templates/job-report-template.html`), so editing `src/templates/job-report-template.html:7-9` is inert until the object is swapped. That is exactly the trap P2-17 exists to fix, and this rides with it rather than racing it. Shape when it happens: subset Inter to latin and only the weights the template actually uses, embed as base64 `woff2` in `@font-face`, delete both `preconnect` links and the stylesheet `<link>`. Swap is a Michael-run CLI upload verified by object metadata, never by re-fetch (Storage CDN serves stale reads). Check the View-button path too — it renders the same HTML from a Blob, so a `data:` font source may need `font-src data:` added to the CSP in `vercel.json`. | [ ] | | | |
| P2-22 | **Reconcile the two HTML copies written per job report — but the "it's redundant, delete it" premise does not hold as stated.** The upload in question is `api/render-job-report-pdf.ts:486` (line `387` in the deployed revision `70f4313`), writing the HTML into the `report-pdfs` bucket at `<jobCompletionId>/v-<ts>.html`. **The Edge Function does not already write that HTML on this path.** The endpoint calls the EF with `previewOnly: true` (`:237`), and the `previewOnly` branch at `supabase/functions/generate-job-report-pdf/index.ts:414-425` returns the HTML with explicitly zero persistence — its own comment reads "no `job_completions` UPDATE, no bucket upload, no `job_completion_pdf_versions` INSERT". The EF's upload at `:448-452` runs only on the **non**-preview path, into a **different bucket** (`inspection-reports`) under a different name (`job-report-<id>-v<n>-<ts>.html`). So on the hard-save path there is exactly one writer, not two. The endpoint's copy is also the one `job_completion_pdf_versions.html_storage_path` points at, which the `html_hash` mismatch guard and `ReportVersionHistory` both read — deleting it breaks the send-time guard. The real question is therefore whether the legacy `handleGenerate` copy and the hard-save copy should be reconciled to one bucket and one lifecycle, which is a design decision, not a deletion. Verified 2026-09-08 by code read. | [ ] | | | |
| P2-23 | **CODE MERGED 2026-09-11, NOT DEPLOYED — and that gap is the whole of why it is still visible to customers.** `ff2d24a` ("fix: print equipment hire period on inspection quote PDF (P2-23)") is on `origin/main` and gives all four equipment lines a `hirePeriod()`, so the repo is correct. The live quote is not, because `generate-inspection-pdf` has not been deployed. **Deploying it is the entire remaining action — no code, Michael only.** Re-reported with visual proof on 2026-09-10 by Vryan and now carried as part of **P0-13(c)**; that row says to do this deploy first, because it is already-reviewed code and it removes one of three customer-facing defects immediately. Do not close this row on the merge — close it on the deploy, verified against a rendered quote. Original text follows. **Equipment days never reach the inspection report.** `supabase/functions/generate-inspection-pdf/index.ts:1726-1728` renders dehumidifier, air mover and RCD as `$rate/day × qty` with **no day count**; only HEPA prints one, at `:1731` (`(N days)`). The customer therefore cannot read a total for three of the four equipment lines. **This is the half of Glen's request that no existing row covers:** P2-3 is the form UI (extend the HEPA Days stepper to the other three), P1-6 is `BookJobSheet` printing days from the booking schedule instead of the quote. Neither touches the PDF. Glen has chased it twice — 31 Aug, and again 7 Sep marked asap (see T23). The three missing day counts are in the Edge Function, not the template, so this does not need the Storage upload path — but anything that also changes the template does (P2-17 / BUG-3). Logged 2026-09-08. | [ ] | | | |
| P2-24 | **REOPENED 2026-09-11 — the 2026-09-08 close got the number right and the rule wrong.** Michael ruled on 2026-09-09 that there is **no equipment day cap at all**: the Days field is the cap, per job. `supabase/functions/generate-inspection-pdf/index.ts:1925` therefore writes `'5 days'` into `{{equipment_max_days}}`, which the template renders as "Capped at 5 days" (`src/templates/inspection-report-template.html:570`) on live customer quotes — stating a rule that no longer exists. **Escalated 2026-09-11: this is now part of P0-13, with visual proof from a real customer quote.** Vryan reported it on 2026-09-10 sitting directly below "HEPA Air Scrubber: $100/day × 1 (14 days)", so the same quote states a 14-day hire and a 5-day cap in adjacent lines. That moves this from a code read to an observed customer-facing defect. **Note the line number moved** — it was recorded as `:1917` on 2026-09-08 and is `:1925` as of `9f831b6`; anchor on the `equipment_max_days` replace, not the number. The close below was correct on its own terms — 5 was the right figure *for the cap as it was then understood*, and the 4 was the error — but the 9 Sep ruling removes the cap entirely, which the printed line does not know. **The fix is to stop printing a cap, not to change the number**, and it changes what customers are told, so it lands with the same discipline this row already used once and was vindicated by: get the replacement wording from Glen or Clayton before editing. This does **not** resurrect the 4-vs-5 error; both figures stay retired. It also closes the standing half of **L1226** ("shown on PDF but not enforced") by deletion rather than by enforcement — worth noting, because a session picking up L1226 to *build* enforcement would now be building a rule that was withdrawn. **Earlier history, kept verbatim below because the direction of the error is the useful part —** **INVERTED ON TRIAGE: the canon held the wrong number and the report was right all along. CLOSED 2026-09-08.** As logged earlier the same day this read *"the report prints a hardcoded 'Capped at 5 days'; the canon says the residential equipment cap is 4"*, and named `supabase/functions/generate-inspection-pdf/index.ts:1917` as the defect on the strength of `PRICING_CANON.md:5` making the canon win wherever documents disagree. **That inference was wrong, and it is wrong in a way worth keeping: the canon is the first document to check, not the one that is right by construction.** Glen confirmed **5 days** in Slack on 2026-09-08. The hardcoded `'5 days'` written into `{{equipment_max_days}}` is correct and has been correct on every customer-facing quote since it was written; `docs/PRICING_CANON.md` section 3 rule 8 and the `docs/MRC_MASTER_BACKLOG.md` glossary held the error. The 4 was never two independent sources agreeing — both files took it in the same commit, `b33e19e` on 2026-09-05, from the 28 Aug 2026 meeting notes. Both are corrected as of 2026-09-08, recorded and not overwritten, under **C4** in the canon. **What stopped this becoming a regression was the row's own refusal to edit code first:** it said *"this changes what customers are told, so get it from Glen or Clayton before editing"*. Actioned as written, it would have replaced a correct number with a wrong one on live customer quotes. Note also that **L1226 was never wrong** — it recorded "Capped at 5 days shown on PDF but not enforced", i.e. it quoted the printed figure and only complained about enforcement; it never claimed 5 was the wrong number. **NOT closed by this: the cap is still enforced nowhere.** Re-verified 2026-09-08 against `origin/main` at `db21282` — nothing in `src/` or `supabase/functions/` bounds the equipment day count at 4, at 5, or at any business figure. The engine's only ceiling is `MAX_QUOTABLE_EQUIPMENT_DAYS = 3650`, which is itself retracted and is not this cap (P2-26); the only database constraint is `CHECK (equipment_days >= 1)`, a floor. That gap stays with **L1226**. Two code comments now cite the retired 4 and will mislead the next reader — `src/lib/calculations/pricing.ts:242-243` and `:269-271`, the second of which is a `TODO(michael)` saying the meeting notes say 4 days residential. Both sit in a frozen surface and were deliberately left alone; they are worth a one-line fix whenever `pricing.ts` is next legitimately open. | [ ] | docs-only, 2026-09-08, `fix/booksheet-equipment-days` session | 2026-09-08. Corrected `PRICING_CANON.md` (section 3 rule 8, new C4 resolution, header date, retires list) and **three** places in `MRC_MASTER_BACKLOG.md`, not one — the glossary at :2425 plus two rate tables at :535 and :2057 that a sweep turned up carrying the same figure. `LAUNCH_TESTING_FINDINGS.md:274` also held it; that file is a dated record of the 2 Aug 2026 test run, so the finding was left intact and a dated correction appended beneath it instead — it ends in an actionable verification step, which is why leaving it silent was not an option. Enforcement re-checked by grep over `src/` and `supabase/functions/` at `db21282`: no 4-day or 5-day cap exists, confirming L1226 still holds. | n/a |
| P2-35 | **Remove "treated areas" from the job report unless it auto-populates from the inspection's areas.** As it stands it is a field someone must re-enter by hand, duplicating data the inspection already holds — so it is either blank, or stale, or a transcription of the area list one step removed from the source. Either auto-populate it from the inspection's areas or take it off the report; a hand-keyed duplicate of existing data is the one option that should not survive. Logged 2026-09-11. | [ ] | | | |
| P2-48 | **Report version identification is insufficient in practice — version history exists and still was not enough.** Clayton, 2026-09-10 4:10pm: he resubmitted a report several times and then had to tell Vryan in Slack which one was final, **by wall-clock timestamp** — *"the version that I've just sent one minute ago roughly so 4:09 pm is the finalised version."* That is the whole finding: the feature shipped, and a person still had to disambiguate out-of-band, in chat, using a clock. **Adjacent to P2-18** (the stale-PDF banner that persists after a successful regenerate) and a second real-world instance of the same gap — the report surfaces do not tell a human which artefact is the current one. Treat the two together when either is picked up; the shared question is what marks a version as final and where that mark is visible. Logged 2026-09-11. | [ ] | | | |
| P2-51 | **`JOB-2026-0013` still has no report artifact three days after submission.** Submitted **8 Sep 17:01**; on **11 Sep** there is **no `job_completion_pdf_versions` row and `pdf_url` is NULL**. Exact cause unresolved; do not assume PDF-CL17 or P1-S-5 explains this job. `src/hooks/useJobCompletionForm.ts:453,479–486` commits submission before optional generation, starts it only when review is not requested, and catches failures to console without durable retry in that path. Review-required jobs generate after approval (`src/pages/LeadDetail.tsx:631–647`). Next: correlate this job's review/approval state and generation logs, assign recovery, and verify a stored URL plus history row when generation is due. Intake records the persistent absence; whether generation failed or still awaits approval is not established. | [ ] |  | 2026-09-11, Michael: PROD artifact/history absence; repository trace |  |

## 🟡 P2 — CRM & comms

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P2-12 | Quote chasing. Audit the codebase first; a partial implementation may exist. | [ ] | | | |
| P2-13 | Job diary: ServiceM8-style comms log per job. | [ ] | | | |
| P2-14 | Google review request script, copy/paste SMS. | [ ] | | | |
| P2-15 | Lead view phone tap → message or call. | [ ] | | | |
| P2-16 | Notification bell live instead of 30 s poll. Prerequisite folded in: add the `notifications` table to the `supabase_realtime` publication (one-line migration; old TODO.md L2545). Distinct from P3-8, which is native push. | [ ] | | | |
| P2-25 | **No @mentions in the job-completion form's Job Notes.** `src/components/job-completion/Section9JobNotes.tsx` is toggles and plain textareas — no mention picker, no `lead_note_mentions` write. Lead notes do have them (`src/components/leads/LeadNotesSection.tsx`, shipped `a4c5436` 2026-08-26, PR #85, rendered at `LeadDetail.tsx:2010`), and Appendix H marks Clayton's "tag people in internal job notes" shipped on that basis. Whether that closes his request turns on which screen he meant: the lead's notes feed, or the notes a technician writes while completing a job. **Ask before building — B7.** Logged 2026-09-08. | [ ] | | | |
| P2-26 | **Equipment day-count hardening is half done, and one half of it is a regression I introduced.** Both raised by Codex round 2 on Unit A of the equipment-days stack (thread `01a08014-bae1-71c1-8888-4133965ab34b`), both marked **UNVERIFIED by execution** by Codex itself, both **confirmed by CC on inspection**, and both **filed rather than fixed** because round 2 is the two-round cap. **(a) HEPA's own hire period still accepts non-finite days.** `src/lib/calculations/pricing.ts:276-278` still uses the old truthiness form `equipment.hepaAirScrubberDays && equipment.hepaAirScrubberDays > 0`, so `hepaAirScrubberDays: Infinity` passes and `hepaQty * 100 * Infinity` is `Infinity`, or `NaN` when the quantity is 0 — poisoning `equipment.total`, the subtotal and GST. `isUsableEquipmentDays` guards only the *shared* days; the HEPA branch is a second, separate multiplication that was never routed through it. Pre-existing (it predates this stack) but squarely in the class the round-1 fix set out to close. Repro: `calculateCostEstimate({ nonDemoHours: 4, demolitionHours: 0, subfloorHours: 0, equipmentDays: 4, hepaAirScrubberQty: 1, hepaAirScrubberDays: Infinity })`, assert `Number.isFinite(totalIncGst)`. **(b) `MAX_QUOTABLE_EQUIPMENT_DAYS = 3650` is a duration policy wearing an overflow sentinel's label, and it silently reprices valid input.** `equipmentDays: 3651` with one dehumidifier returns **$119** (one auto-derived day) where the pre-fix engine returned **$434,469** — a number float64 represents exactly, so this is not an arithmetic boundary at all. Silently converting an out-of-range explicit period into a plausible one-day quote hides the error rather than surfacing it. **CC wrote the "overflow sentinel, NOT a business cap" claim into the code comment, into P2-24 and into the handback to Michael; Codex is right and all three are wrong as written.** Shape of the fix, per Codex: separate absent/zero auto mode from *invalid* explicit values, surface the invalid ones instead of absorbing them, and base any arithmetic protection on the computed monetary value rather than an unexplained day cutoff. **SHAPE OF THE FIX, recorded now so it is not relitigated (Michael, 2026-09-08):** separate absent/zero *auto* mode from *invalid* explicit values; **surface** invalid input rather than silently substituting a derived value; base any arithmetic protection on the **calculated monetary value**, never an unexplained duration cutoff; and fix HEPA's own guard **in the same unit** — it is the same defect at a second multiplication, and splitting them recreates the gap. The false wording in P2-24 and in the `pricing.ts` comment was **retracted separately on 2026-09-08** and does not wait on this row. **(c) A SECOND SURFACE — `BookJobSheet`'s equipment heading. Raised by Codex on the P1-6 booking-sheet fix (thread `01a080bf-5239-7123-b9da-b125a1a406a4`), filed here rather than fixed there.** The mechanism is real: `src/components/leads/BookJobSheet.tsx:285-288` resolves the displayed period with `Math.max(equipment_days ?? 0, deriveEquipmentDays(total))`, which skips `isUsableEquipmentDays`, so a saved `equipment_days` of 3651 prints "Equipment (× 3651 days)" while `calculateEquipmentCost` charges **one** day — technician instructions inconsistent with the quote. `NaN` and `Infinity` diverge the same way; zero and negative fall back correctly. **Why it was NOT fixed in the unit that found it:** Codex's recommendation was to align the heading *with the engine*, and the engine's behaviour at exactly that boundary is the thing this row retracts — (b) above already records that 3651 silently reprices to one day. Following the recommendation would replicate a retracted defect into a second surface. This row's own recorded shape — **surface** invalid input rather than absorb it — fixes both surfaces together, and that is one unit; bolting the retracted rule onto the heading is not (Michael, 2026-09-08). **Scoping consequence, and the reason this is on the row rather than in a review log: `BookJobSheet`'s heading is now a second surface P2-26 must fix, not just `pricing.ts`. Anyone scoping this row to the engine alone leaves the heading wrong.** Note also that `isUsableEquipmentDays` is **not exported**, so whatever replaces it must be exported or the heading will need its own copy — which is the duplication this row exists to prevent. **Reachability is UNKNOWN and neither side established it:** whether 3651 can actually be produced by the Section 7 Days stepper, or is barred by the column's constraints. **That is what separates a real defect from a theoretical one, and it is the FIRST thing to settle when this row is picked up** — it sets the priority of the whole row, not merely of this part. Codex ran no tests (`Tests were not run`), so **(c) is UNVERIFIED by execution**, source inspection only. Logged 2026-09-08. | [ ] | | | |
| P2-27 | **Withholding `costData` hides the cost editor's control but does not clear its draft, so a stale quote stays saveable — and B2's own test cannot see it.** Raised by Codex on Unit B2 (thread `01a08036-d67f-7461-a213-690747c46ea1`), **UNVERIFIED by execution** in Codex's own words, **confirmed by CC on inspection**. `src/components/pdf/ReportPreviewHTML.tsx` owns `editingCost` (`:294`) and `costForm` (`:295`); `startCostEdit` (`:788`) seeds the form once and the save handler (`:891`) submits `costForm`, **not** `costData`. So `ViewReportPDF.tsx:1711-1714` setting `costData` to null while pricing inputs are stale removes the UI but leaves the draft intact — when the refresh completes the old form returns unreseeded and Save submits the figures captured before the refresh. Repro (Codex): open the editor at 16h / 2 days / $238, move the fixtures to 4h / 1 day, trigger Add Area and let the refresh finish, dismiss the area dialog, save costs; assert `equipment_cost_ex_gst` is $119 — the retained draft submits $238. **The part that matters most: `src/pages/__tests__/ViewReportPDF.pricingInputs.test.tsx` MOCKS `ReportPreviewHTML` and captures the `costData` prop, so it structurally cannot detect this.** That makes this finding a live limit on the evidence Unit B2 shipped with, not merely a separate defect — B2's green tests do not mean the editor is safe, only that the prop is. **Acceptance: a test against the real editor asserting the PERSISTED save payload**, not the prop. Codex's shape: version cost drafts against the pricing inputs and reject stale saves until the automatic fields reconcile, preserving deliberate operator edits. **WENT LIVE WHEN UNIT C MERGED:** until C, nothing shipped ever set an explicit day count, so every stored `equipment_days` was labour-derived and reconciled back to auto — this row described a path no user could reach. C's Section 7 Days stepper is the first writer, so an explicit stored period is now ordinary and the window described above has customer quotes in it. Priority rises accordingly; do not read the original logging date as the measure of urgency. Logged 2026-09-08. | [ ] | | | |
| P2-28 | **`loadInspection()` republishes every pricing input outside the staleness gate B2 added, and its area-error path can push a zero-area estimate through it.** Raised by Codex on Unit B2 (thread `01a08036-d67f-7461-a213-690747c46ea1`), **UNVERIFIED by execution**, **confirmed by CC on inspection**. `loadInspection()` runs from three post-mount sites — `src/pages/ViewReportPDF.tsx:840` (after regeneration), `:943` and `:3598` — and sets `inspection`, `areasData` and `subfloorData` separately, never touching `pricingInputsStale`. Worse, its area query does `setAreasData((areas || []) as AreaRecord[])` at `:769`, so a failed read substitutes an **empty** area list, and the cost editor stays open over an estimate derived from no areas at all. Repro (Codex): load a 4h job, regenerate successfully, return the inspection successfully, then fail the following `inspection_areas` read; assert cost editing is unavailable and no estimate derived from empty areas can be saved — it currently remains available. **Pre-dates B2 but defeats B2's claimed coverage**, which is why it is filed rather than absorbed. Codex's shape: route every `loadInspection` refresh through the same guarded snapshot publication, preserve existing inputs on a failed read rather than substituting empties, and block cost saving when any required read fails. Distinct from P2-27: that one is the editor retaining a draft, this one is the page republishing inputs unguarded. **A second mechanism with the same remedy, folded in here rather than given its own row (T21):** overlapping refreshes can publish out of order. `refreshAreasAndPricingInputs` has no generation token, so if refresh A is still awaiting while refresh B completes with newer data, A then republishes its older snapshot and clears `pricingInputsStale` — restoring the obsolete quote the gate exists to prevent. Add Area stays available during a visibility-toggle refresh, so the overlap is reachable. Repro (Codex, round 3, thread `01a08041-5c9e-71b2-b2ca-71a7c2512a84`): with deferred responses, A reads `equipment_days=2` / 240 area minutes then waits on subfloor; the job moves to `equipment_days=1` / 0 subfloor minutes; B completes with those; then A's subfloor read resolves — assert `auto_equipment_ex_gst` stays $119, but A republishes 2 days and $238. **UNVERIFIED by execution.** **This race pre-dates the round-3 subfloor change** — CC verified that the round-2 helper at `6f0b90b` already did `await Promise.all(...)` then published with no generation guard, so the subfloor `await` widens the window rather than creating it. Codex's shape: give each refresh a generation token and let only the current generation publish inputs or clear `pricingInputsStale`; add deferred-response coverage for an older completion landing after a newer success and after a newer failure. **The existing tests resolve every query immediately and cannot exercise this ordering** — the same evidence limit as P2-27, one layer down. **A THIRD mechanism, and NOT the round-3 race — the generation token does not close it (terminal production gate, 2026-09-08, thread `01a08056-77a6-7980-be9f-d28b17c6f6e1`):** a **torn read inside a SINGLE refresh**. Round 3 above is refresh A overwriting refresh B — two refreshes racing *each other*. This is one refresh whose own two requests disagree: the pricing-inputs request and the areas request can return **different DB revisions**, both succeed, and `pricingInputsStale` clears on that inconsistent pair. **A generation token orders refreshes against each other and does nothing about this** — shipping it closes round 3 and leaves this open. The remedy recorded above is therefore only half the fix, and a session that ships the token alone will believe it complete. Acceptance needs a consistency check *across the pair* — a shared revision or `updated_at` assertion, or a single request returning both — not merely ordering. Anchored at `src/pages/ViewReportPDF.tsx:2267-2272` on pinned head `db21282`. Repro (Codex, verbatim): "Mount the report with 16 labour hours, one dehumidifier, `equipment_days=2`, no subfloor, and no overrides. Trigger an area refresh through Add Area. Use deferred query responses so the pricing request captures the old two-day row, another session finishes saving four labour hours and `equipment_days=1`, and the areas request returns the new four-hour state. Resolve both requests successfully. Assert that the reopened editor's `auto_equipment_ex_gst` equals **119**. The candidate instead computes **238**, interpreting the stale two days as an explicit hire period. Previously, stored equipment days were ignored, so this particular equipment overbilling did not occur." **UNVERIFIED by execution.** Concurrent-writer class — it needs a second session saving mid-refresh — so **not a production blocker** (Michael, 2026-09-08). **WENT LIVE WHEN UNIT C MERGED:** until C, no shipped UI set an explicit day count, so every stored `equipment_days` reconciled back to auto and an out-of-order republication could only restore a figure equal to the derived one. C's Days stepper is the first writer, so both the unguarded `loadInspection` publication and the overlapping-refresh race can now restore a genuinely different, customer-facing number. Priority rises accordingly; do not read the original logging date as the measure of urgency. Logged 2026-09-08. | [ ] | | | |
| P2-29 | **The form's own gating of when a day count counts as explicit is the one untested part of the equipment-days feature.** Unit C shipped the Section 7 Days stepper with **no test coverage of its three gating helpers**, and this row records exactly where the boundary of the tested work sits so it is not mistaken for complete. **Tested:** the pricing engine, including non-finite handling and parity against fixed pre-change literals captured at `63dca6b` (Units A and A′, `pricing.test.ts` + `pricing.equipmentDays.test.ts`); persistence and the read surfaces, including eight tests on `reconcileLoadedEquipmentDays` covering the explicit-vs-auto boundary in both directions (Unit B, `estimate-override.test.ts:103-127`); and the `ViewReportPDF` staleness gate, proven by execution at `expected 238 to be 119` (Units B2 and its round 3, `ViewReportPDF.pricingInputs.test.tsx`). **Untested:** `getLabourWorkDays` (`src/pages/TechnicianInspectionForm.tsx:1977`), `getExplicitEquipmentDays` (`:1989`) and `getSharedEquipmentDays` (`:1997`) — the decision C actually introduces, namely when a stepper value is an explicit hire period rather than the labour-derived default. They are module-scope consts, **not exported**, and there is no test file for that component at all, so covering them needed either a production-surface change or a full-page harness for a ~4,300-line form — either being its own unit, which is why C shipped without it rather than with a thin test that read as coverage. **Shape of the fix (Michael, 2026-09-08):** lift the three helpers out of `TechnicianInspectionForm.tsx` into `src/lib/calculations/` and unit-test them directly. They are pure functions of `formData`, so **no render harness is needed at all** — this is the cheap version and it is the right one. Logged 2026-09-08. | [ ] | | | |
| P2-30 | **`collapseMultiDayJobs` anchors a multi-day series to its earliest remaining day, not the next upcoming one, so a series with upcoming work this week can read as Overdue.** When today matches no row in the series, `src/hooks/useTechnicianJobs.ts:188-195` falls back to `activeIndex = 0` and anchors the card to `sorted[0]` — the earliest surviving booking — rather than the next future day. The gap is reachable in normal operation because the server query filters `status in ('scheduled','in_progress')`, so a completed middle day is dropped from the series and leaves a hole on today's date. **Reproduction, executed 2026-09-08** (throwaway vitest file, run then deleted): two `scheduled` rows in one series dated **2026-06-09** and **2026-06-11**, today **2026-06-10**, Melbourne week bounds **2026-06-08 - 2026-06-14**. Verified output: `collapsed len: 1`, `repr date: 2026-06-09`, `OLD unclamped inThisWeek: true`, `NEW clamped inThisWeek: false`, `in overdue (date < today): true`. So the series leaves This Week entirely while 11 June is still upcoming. **Surfaced by P1-23**, whose `>= today` clamp on `this_week` exposed this — it did not cause it: the `activeIndex = 0` fallback predates the clamp, and before the clamp the same series sat in This Week **and** Overdue simultaneously, which is the double count P1-23 exists to remove. **Accepted-deferred by Michael 2026-09-08, not fixed in the P1-23 unit:** nothing is hidden from the technician — the series still appears in Overdue — and fixing the collapse anchor changes both the card date and the `Day N of M` label at every call site, so it carries its own blast radius and is a separate unit of work. Raised as a [medium] finding by Codex on `f962a57` (thread `01a08136-0dec-77d0-8492-cd96eefacf58`) and verified by execution in-session rather than filed UNVERIFIED. **Adjacent, out of scope, unfiled:** the collapsed label read `Day 1 of 2`, with `totalDays` recomputed from surviving rows rather than the original title's total. Logged 2026-09-08. | [ ] | | | |
| P2-31 | **The equipment price lines are tested by scraping the Edge Function source as text, because nothing on the Deno side can be imported.** `supabase/functions/generate-inspection-pdf/index.ts:1734-1740` builds the four customer-facing equipment price strings inline, and both guards on them — `src/lib/calculations/quoteEquipmentDays.test.ts` (behaviour) and `src/lib/calculations/equipmentRateDrift.test.ts` (rate literals vs `EQUIPMENT_RATES`) — read that file as **text** and match on source anchors, because the EF imports from esm.sh and calls `Deno.serve` at module scope, so vitest cannot import it. The scraping tracks the shipped source today (proven 2026-09-08: mutating the day suffix to `(N d)` turned an unedited test file red), but it is anchored on a comment string and a `const` name, and it proves nothing about whole-function validity. **Fix:** extract the price-line builder into a sibling module, `supabase/functions/generate-inspection-pdf/equipmentPricing.ts`, containing no Deno APIs and no esm.sh imports — `index.ts` imports it relatively (Deno bundles the directory on deploy) and the vitest tests import it directly, so both guards become real imports and the source-scraping in both files is retired. **Covers both Edge Functions that hand-copy the rate literals**, not just this one: `generate-job-report-pdf/index.ts` carries the same hand-copied `EQUIPMENT_RATES` values and the same text-scraping guard (`equipmentRateDrift.test.ts` reads both), so do them in one unit rather than leaving one half scraped. Does NOT close the repo-to-deployed gap — that stays the download-and-compare Michael runs at deploy time, and no unit test can replace it. Raised by CC and independently stated as the same limit by Codex on the P2-23 review (threads `01a08139-bd09-7443-ab34-bd819662650b`, `01a0813a-c71a-71d3-8f79-30aa2181aa4c`). Logged 2026-09-08. | [ ] | | | |
| P2-32 | **Nothing at the database level stops a zero or negative quoted hire period on the three new per-item day columns.** `supabase/migrations/20260908233000_inspections_per_item_equipment_days.sql` adds `commercial_dehumidifier_days`, `air_movers_days` and `rcd_box_days` as bare nullable `INTEGER` with no `CHECK`, mirroring `hepa_air_scrubber_days` (`20260728120000_hepa_quote_columns.sql`), which also has none. The column they take over from **does** have one — `equipment_days INTEGER DEFAULT 1 CHECK (equipment_days >= 1)`, `20251122000001_add_pricing_columns_to_inspections.sql:88-89` — so the migration moves the hire period off a column the database defends onto three it does not, and a `0` or `-3` day count reaches `calculateEquipmentCost` unchallenged. **Deliberately not added in the migration, and the reasoning is the point:** the per-item writer does not exist yet, so its behaviour when a technician sets a quantity to zero is unknown — whether it writes `0`, writes `NULL`, or omits the column entirely. A `CHECK (>= 1)` that fires on an ordinary save is a worse failure than a permissive column: it surfaces in the field as a save that silently will not complete, and it would be guarding a row nobody can currently produce. **The guard belongs in the engine, not in the schema — same class as P2-26**, whose recorded shape is exactly this: separate absent/zero *auto* mode from *invalid* explicit values, **surface** invalid input rather than absorbing it, and base any arithmetic protection on the calculated monetary value rather than an unexplained cutoff. Pick this up **with** P2-26, not alone; splitting them recreates the gap the same way splitting HEPA from the shared guard did. If a DB constraint is still wanted once the writer's zero-quantity behaviour is known, the repo idiom is a `DO $$ … pg_constraint … $$` block adding `inspections_per_item_days_check CHECK ((commercial_dehumidifier_days IS NULL OR commercial_dehumidifier_days >= 1) AND (air_movers_days IS NULL OR air_movers_days >= 1) AND (rcd_box_days IS NULL OR rcd_box_days >= 1))`. Raised by the in-session adversarial review of the migration and **UNVERIFIED by execution** — the migration has not been applied anywhere, and no Postgres was reachable from the session that wrote it. Logged 2026-09-09. | [ ] | `feat/per-item-equipment-days` | | |
| P2-36 | **Six lead-source categories on the Framer contact form, replacing the current values.** Glen, 2026-09-09 9:10pm in #app-issues: **Private/Home Owner · Real Estate · Business · Government housing · Builder · Trades.** His reasoning is the part worth keeping, because it is an argument about information and not about taxonomy: *"I just dont see how valuable having website as a variable to the tracker be all that beneficial"* — every lead arrives via the website, so `lead_source='website'` is a constant, and the field currently spends its only slot recording it. `leads.lead_source` and `leads.lead_source_other` already exist, so the column needs no migration. **Scope is the chain, not the column:** Framer form field → `receive-framer-lead` Edge Function → `leads.lead_source`. That crosses `supabase/functions/`, a frozen human-applied surface, which is why this is **FILED, NOT SCHEDULED** for the 11–14 Sep Codex window (`docs/CODEX_WEEK_PLAN.md`). Decide the migration story for existing rows when it is picked up — six new values against a field whose historical content is almost entirely one constant. Logged 2026-09-11. | [ ] | | | |
| P2-37 | **`report_sent_date` is never written.** The column exists and nothing populates it, so any report-sent reporting, chasing or SLA measurement built on it reads empty and looks like "no reports sent" rather than like a missing writer. Pairs with P2-38 — same class, same likely cause, and worth one investigation across both rather than two. Note the related but distinct P0-11: status advances to "Report Sent" on admin approval rather than on confirmed delivery, so even once this column is written, *what* it should record — approval time or delivery time — is a decision, not an implementation detail. Logged 2026-09-11. | [ ] | | | |
| P2-38 | **`inspection_completed_date` is never written.** Same class as P2-37: the column exists, nothing populates it, and anything reading it sees an empty field rather than an error. This one also silently weakens any "stalled inspection" definition built on completion dates — see P2-43, which has to derive stalled-ness without it. Investigate with P2-37. Logged 2026-09-11. | [ ] | | | |
| P2-39 | **Six app-code writer paths write `activities.user_id` as NULL 100% of the time, and the timeline has no fallback for it.** Verified in PROD 2026-09-09, all NULL: `status_change` 8/8, `inspection_booked` 93/93, `booking_cancelled` 45/45, `job_completion_submitted` 5/5, `archived` 3/3, `lead_not_proceeding` 1/1. **This is app-code, NOT a migration** — no database function writes `activities`, checked in PROD the same day, so there is no trigger to fix and nothing to apply in Studio. Two halves, both in Lane C (`docs/CODEX_WEEK_PLAN.md`): **(a)** `ActivityTimeline` must render a row with no `user_id` as "System" / "Website" rather than a bare timestamp — **123 of 207 leads have `created_by` NULL, so this is a legitimate state and not an error state**, and the fallback should read as information, not as a failure; **(b)** the six writer paths pass `user_id`. Do (a) first: it fixes every historical row, which (b) never will. Logged 2026-09-11. | [ ] | C / `~/mrc-timeline-attr` | 2026-09-09, PROD counts per writer path | |
| P2-41 | **RESOLVED 2026-09-09 — 33 leads at post-inspection statuses with zero inspections is a legitimate path, not a defect firing.** 31 of the 33 carry a `job_booked` activity row, so booking a job without an inspection is something the business actually does. **The actionable part is the negative instruction: do NOT gate the status advance on an inspection id.** Doing so would break a real workflow to satisfy a rule nobody agreed to, and the shape of the data makes it look justified right up until it blocks a booking. Kept as a row so the count is not re-raised as a finding by the next person who queries it. **Honest residual:** the other 2 of 33 were not individually accounted for and were not pursued — not enough to change the conclusion, and recorded here rather than quietly rounded away. Logged 2026-09-11. | [x] | | 2026-09-09, PROD: 31/33 carry a `job_booked` activity row | n/a |
| P2-43 | **Stalled inspections are invisible on the admin dashboard — nothing surfaces a job that has quietly stopped moving.** Build is specced and partly written: `useStalledInspections` + test, then `StalledInspectionsCard` + tile + `AdminDashboard`, two commits and **one PR** — neither lands alone. Lane B, `~/mrc-stalled-sweep`, branch `feat/stalled-inspection-visibility` (`docs/CODEX_WEEK_PLAN.md`). **No longer urgent as of 2026-09-11, and must not be worked as live damage.** The three leads that made it urgent — `MRC-2026-0136`, `MRC-2026-0168` and `MRC-2026-0127` — are all finished, verified in PROD 2026-09-11: none appears in the stalled predicate any more, and Clayton completed them unprompted. The remaining stalled set is the two known triple-fire stubs on `MRC-2026-0124` (**P2-44**) plus two August jobs already scheduled. **The row stays worth building because it is prevention, not remediation** — the reason it looked urgent was that nobody could see the three leads, and that is exactly the gap it closes. Note it derives stalled-ness without `inspection_completed_date`, which is never written (**P2-38**). Logged 2026-09-11. | [ ] | B / `feat/stalled-inspection-visibility` | 2026-09-11, PROD: the three named leads no longer match the stalled predicate | |
| P2-44 | **Child rows sit on the wrong parent for `MRC-2026-0124`, and the obvious cleanup destroys the only copy.** `inspection_areas` and `subfloor_data` hang off stub inspection `b4aed2e0`, while the report row `2fd7b2a6` has **zero** areas — so the report renders empty while the data exists one parent over. **Both FKs are `ON DELETE CASCADE`: deleting the stub destroys the only area record there is.** The fix is a re-parent, not a delete, and it is live data surgery on PROD — **Michael only, explicitly not the 11–14 Sep Codex window** (`docs/CODEX_WEEK_PLAN.md`). Read-only preview and an approved row count before anything writes, per the P0-8 idiom. The stubs are the known triple-fire artefact, so settle whether the writer can still produce them before repairing the rows, or the repair is a one-off against a live source. Logged 2026-09-11. | [ ] | Michael (Studio) | | |
| P2-47 | **Invoicing is tracked outside the app by hand, and the convention is invisible to everyone who is not Vryan.** Vryan appends "M" to a lead number as a personal invoicing convention — `MRC-2026-0169` becomes "0169M". Nothing in the app knows this, and neither does search. **Observed consequence, 2026-09-10:** Glen could not find a customer who had paid — *"[customer] just paid bro but why cant i find her on reminders or on the app?"* Glen had also asked directly on 2026-09-09, *"are we tracking invoices on the app?"*, and that went unanswered. **The decision is the deliverable, not the feature:** either invoicing state belongs in the app, or the convention gets written down where the rest of the team can follow it. Both are acceptable; leaving it as private tacit knowledge is what produced the 10 Sep failure. Related to P3-1 (invoicing plus Xero linking), which is the full build — this row is the much smaller question of where invoicing state lives today. Logged 2026-09-11. **Database confirmation, 11 Sep:** PROD `invoices` holds **ZERO rows**, while **nine** leads are `invoicing_sent` and Slack records **INVOICED → PAID** transitions. `check-overdue-invoices` runs cleanly against nothing: `supabase/functions/check-overdue-invoices/index.ts:237–251` reads only overdue `invoices`, never lead status. A repo invoice writer exists (`src/lib/api/invoices.ts:517–527`); zero live rows confirms the operational disconnect, not that no code exists. Decide where invoicing lives and document ownership; the monitor's success is not evidence of billing coverage. Source: Michael's PROD/Slack checks supplied at intake. | [ ] | | | |
| P2-49 | **No field records whether a customer followed the remediation recommendations, so a pricing decision that has already been made cannot be applied.** Glen and Clayton agreed on 2026-09-10 to **charge for retreats where the recommendations were not followed**. That decision has nowhere to live today: it cannot be applied consistently across jobs, and — the part that will actually bite — it cannot be evidenced to a real estate agent who disputes the charge. What is needed is a recorded, timestamped answer at the point the retreat is quoted, not a note in someone's memory of the first visit. Settle who records it and when (inspection, completion, or retreat quote) before designing the field. Logged 2026-09-11. | [ ] | | | |
| P2-50 | **Property vocabulary mismatch plus a genuine missing premises answer.** PROD: `leads.property_type` is **NULL on 17 of 19** leads with a job completion, while completion `premises_type='residential'`; **five** leads compare dwelling type (`house/units`) with premises type (`residential`). These are different dimensions: `src/lib/validators/lead-creation.schemas.ts:138–148` uses dwelling-form values, while `Section2Summary.tsx:118–129` offers residential/commercial and `src/lib/api/jobCompletions.ts:15–17` saves that on the completion only. Do not copy residential into a dwelling-type field or classify every lead NULL as lost completion data. **`MRC-2026-0026` has completion `premises_type=NULL`, a genuine gap**; `generate-job-report-pdf/index.ts:309` defaults missing premises type to Residential, potentially concealing it. Define vocabulary/mapping and required-field rules, then reconcile verified gaps; acceptance must distinguish dwelling form from premises use and preserve a visible missing-answer state. | [ ] |  | 2026-09-11, Michael: PROD 17/19, five comparisons and named NULL; repository trace |  |
| P2-52 | **Duplicate status Slack post and four empty response bodies.** On **11 Sep 18:14:11**, one lead status change posted **twice identically**; four other posts returned empty bodies. Event cause/delivery outcome unresolved: empty body alone does not prove non-delivery, and this status-change duplicate is not yet tied to P0-C's scheduled dispatch duplication. `supabase/functions/send-slack-notification/index.ts:5–30,409–426` has no event/dispatch identity in its schema, posts on each valid invocation, and normally returns nonempty `{success:true}`. `src/lib/api/notifications.ts:575–587` accepts null response data without an error when invoke reports none. Correlate activity/event IDs, invocation IDs and webhook response status/body; propose event-specific idempotency and explicit missing-acknowledgement handling, with duplicate-event and empty-response acceptance cases. | [ ] |  | 2026-09-11, Michael: Slack/monitoring observations; repository trace |  |
| P2-53 | **Review and uncontacted-lead queues keep growing without trend ownership.** Across **five days**, completions awaiting review went **3 → 4 → 5 → 6 → 8 → 13**; uncontacted leads went **83 → 94**, oldest untouched since **5 Aug**. Keep this as its own operational trend row: agree an owner, review cadence, ageing/escalation threshold and a recorded next count/date for both queues. Related to P0-2 (list visibility can hide leads) and P2-43 (stalled inspections), but neither owns this combined trend. `src/hooks/useAdminDashboardStats.ts:94–100,118–123` counts unassigned new leads and leads at `pending_review`, not a history of completions awaiting review; do not compare those different predicates as the same series. | [ ] | Michael — assign queue owner/cadence | 2026-09-11 intake, Michael: monitoring trend across five days |  |

## 🟢 P3 — Backlog

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P3-1 | Invoicing plus Xero linking. | [ ] | | | |
| P3-2 | Business tracking suite. | [ ] | | | |
| P3-3 | Email domain switch to `mouldandrestoration.com.au`. Checklist folded in from the old TODO.md: seed-admin sender address (L49, L1329); DMARC enforcement record (L127); footer logo off the Supabase storage domain (L143); the Sent-folder visibility decision and its options, Option B preferred but deferred until the team's first clean week (L255–L332). | [ ] | | | |
| P3-4 | Framer → Supabase lead capture wiring. Live. | [x] | | 2026-09-05: leads with Source: Website arriving in PROD (Michael); the audit agent found the wiring in place and public leads arriving | n/a |
| P3-5 | Phase 2 job completion workflow. Spec at `docs/JOB_COMPLETION_PRD.md`. | [ ] | | | |
| P3-6 | SMS quick-action from lead numbers. | [ ] | | | |
| P3-7 | Equipment module: port `splitracker`, don't rebuild. Ask Michael for the link and Glen's screenshots first. Separate session from P2-4; not run in parallel with it until file overlap is checked. | [ ] | | | |
| P3-8 | In-app notifications (needs a native app). | [ ] | | | |
| P3-9 | Dedicated end-of-day report send window. | [ ] | | | |
| P3-10 | Photo-caption removal polish. | [ ] | | | |
| P3-11 | V2: AI-agent-first rebuild. Current app gets buttoned up only. | [ ] | | | |

## 🟢 P3 — Marketing & web (not app work)

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| M1 | Website redesign with a new contact form with photo upload. | [ ] | | | |
| M2 | Website SEO and Google optimisation (with Clayton, post-warehouse). | [ ] | | | |
| M3 | LinkedIn outreach strategy. Glanz drives. | [ ] | | | |
| M4 | Real estate agency canvassing, opportunistic. | [ ] | | | |
| M5 | Google Business Profile map pin and surfacing the 121 five-star reviews. | [ ] | | | |

## 🔧 Infrastructure / tech debt

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| T1 | `docs/todo-log-2026-08-31` branch unmerged. 11 findings logged; merge or fold into the backlog. | [ ] | | | |
| T2 | SF-2: `audit_log_trigger()` carries `anon EXECUTE`. | [ ] | | | |
| T3 | `pg_default_acl` hazard: every new `public` function silently regains `anon EXECUTE`. | [ ] | | | |
| T4 | API key rotation: Google Maps, OpenRouter, Supabase, Slack, Sentry. Resend is narrowed to E-3: verify the Edge Function secret, do not rotate the key again. | [ ] | | | |
| T5 | Dev Supabase environment separation. Plan at `docs/L4-environment-separation-plan.md`. P0-6 goes first on its own; T5 absorbs everything else. | [ ] | | | |
| T6 | PROD pre-flight for 0a/0b/0c. 15 read-only Studio blocks; PP6a needs human adjudication. | [ ] | | | |
| T7 | `npm run typecheck` is a no-op. Root tsconfig has `"files": []`; use `npx tsc -p tsconfig.app.json --noEmit`. The error count is not one number: 99, 122 and 135 were all measured on 31 Aug in different worktrees. Gate PRs on no NEW error lines against a baseline taken in the same tree the same day, never on the count. | [ ] | | | |
| T8 | Guard hook exists in only one place (`~/.claude/hooks/`). Track and register it in the repo. **Done for the tracking half; the home copy is deliberately NOT yet deleted — see T16.** | [x] | `chore/guard-hook-and-worktree-sync` / F | 2026-09-05, PR #129 merge `a6fc2b3`. Restored byte-identical to the live home copy (`b42aecf`), not from git history — `8eedd28^` holds a 78-line version with no rule 3.5. `.claude/settings.json` unchanged; the registration was already correct. Verified in **19 of 21 worktrees**: hook present, executable, hash `b42aecf`, and `passed 23, failed 0` run in the worktree itself. **The guard is distributed, not sound** — see P0-9. | n/a |
| T9 | Incident 1: `STATUS: NOT APPLIED` is unenforced prose. Needs a real gate. | [ ] | | | |
| T10 | Incident 5: write protection binds to tool names, not resources. | [ ] | | | |
| T11 | SessionStart warning for worktrees missing a named safety commit. Open, not started. | [ ] | | | |
| T12 | Worktree drift. **The old text — "17 worktrees, 14 current at `3191a39`, three blocked" — was wrong on every count.** There were 22 registered worktrees, not 17; ZERO were current, because `origin/main` had moved past `3191a39`; and the "three blocked" (`mrc-app-1`, `mrc-cost-estimate`, `mrc-reminder-ef`) were not the blocked set. `mrc-cost-estimate` was clean and `mrc-app-1` merged only after its superseded drafts were parked. The real blocker set was 10, dominated by one mechanism: the GitNexus hook rewriting its own generated block in `CLAUDE.md`. **Counting dirty files is not the right measure** — it says whether a merge can *start*, not whether it *auto-resolves*; a dry-run of all 21 found 5 content conflicts the dirty count could not see. | [x] | `chore/guard-hook-and-worktree-sync` / F | 2026-09-05. 18 swept + `mrc-lead-list` last with a fresh re-check (Session H had moved it mid-sweep); dead `wt-conflict-detail` registration pruned, 22 → 21. Conflicts auto-resolved only within an approved allowlist, abort otherwise — zero aborts. Verified per worktree: contains `a6fc2b3`, hook present + executable + hash `b42aecf`, `AGENTS.override.md` present, suite `23/0`. **19 of 21.** `mrc-offline` skipped (Session O owns it), `mrc-cost-estimate` excluded (real pricing divergence) — both tracked in T16. | n/a |
| T16 | **Step 4 of the guard-hook flip is deliberately held: `~/.claude/hooks/block-supabase-prod.sh` and its registration in `~/.claude/settings.json` are still in place.** Deleting them while any worktree lacks the tracked copy is incident 4 repeating. Unblocks only when BOTH conditions are met: **(a)** `mrc-offline` carries the tracked hook — its repo path does not exist today and its suite still defaults to `$HOME`, so it currently reports `passed 15, failed 0` against the home copy, which is exactly the false pass this work exists to kill. Assigned to **Session O's next merge of main**. **(b)** `mrc-cost-estimate` carries it — assigned to **P1-6**, which is the session that merges `fix/option-stacking-equipment-days`. Neither half is orphaned. Until then the home copy is harmless: its only cost was ambiguity about which file was authoritative, and the tracked copy has resolved that. | [ ] | O (a) / P1-6 (b) | | |
| T13 | **`api/*.ts` is typechecked by no tsconfig at all.** Three configs exist: root `tsconfig.json` has `"files": []` and no `include` (checks nothing, and references are not followed without `-b`); `tsconfig.app.json` has `"include": ["src"]`; `tsconfig.node.json` has `"include": ["vite.config.ts"]`. `api/` matches none of them, so `api/render-pdf.ts`, `api/render-job-report-pdf.ts` and `api/_shared/reportHash.ts` fall straight through the gap — no local command typechecks them. This is the *only* Node-executed code in the repo, it is the code that runs `puppeteer-core` + `@sparticuz/chromium`, and it is the code whose runtime Session N changed. **This is why N could not fully verify its own change**: a green `npm run build` proves the browser bundle compiles, not that the PDF functions survive a Node major bump — only a preview render does. Precedent that the gap bites: `e2a6b7a` shipped an `api/` import that passed local `tsc` and crashed at runtime in preview with `ERR_MODULE_NOT_FOUND`. Fix is a fourth tsconfig covering `api/`, wired into a real check. **N's runtime question is closed** — INS-2026-0001 regenerated cleanly on the preview under Node 24, so the functions do survive the bump. That was answered by a manual render, not by any automated check, which is exactly the gap: the same class of defect would still ship silently. The tsconfig hole itself stands. **AMENDED 2026-09-08 — the headline is now half wrong, and the half that changed is the interesting one.** Still true of `include`: all three configs still exclude `api/`, unchanged. **No longer true of reachability.** Two test files added since `2c8087e` — `src/lib/__tests__/renderJobReportPdf.instrumentation.test.ts:108` and `src/lib/__tests__/renderJobReportPdf.setContent.test.ts:113` — both `await import('../../../api/render-job-report-pdf')`. That import reaches out of `src` and pulls the file into the `tsconfig.app.json` program even though the glob never matched it, which surfaces `api/render-job-report-pdf.ts(196,24): error TS2339: Property 'headless' does not exist on type 'typeof Chromium'`. **The error is not new and is not a regression** — the same expression sat at `:123` at `2c8087e` and emitted nothing, because nothing type-checked it. It was latent. So `api/` is now **partially checked by accident rather than by configuration, which is worse than either extreme**: the coverage moves when a test's import graph moves. Rename or delete those two imports and the file silently drops out of the program again, taking the error with it and giving back a green 99 that means less than it looks. Scope of the accident, verified 2026-09-08: **only** `api/render-job-report-pdf.ts` is reached. `api/render-pdf.ts` and `api/_shared/reportHash.ts` are still unchecked — the five `api/render-pdf` hits in `src` are URL strings (`reportPipeline.ts:13`, `RenderPdfTest.tsx:55`), not module imports. The fix is unchanged and now more clearly worth doing: a fourth tsconfig covering `api/`, wired into a real check, so coverage is declared rather than inherited from a test's imports. **The TS2339 is deliberately NOT fixed** — see T14 for why the gate figure moved. | [ ] | | | |
| T14 | **`~/mrc-app-1/node_modules` is a Deno-managed tree that has drifted off the lockfile**, and this is very likely the cause of T7's unexplained 99/122/135 tsc spread. It is not an npm tree: it has a `.deno/` directory with 981 entries and every package is a symlink into `.deno/<pkg>@<ver>/node_modules/<pkg>`. Measured drift against `package-lock.json`: typescript **5.9.3 vs 5.8.3**, `@supabase/supabase-js` **2.112.3 vs 2.76.1**, jsdom **27.4.0 vs 27.0.1**, pdfjs-dist **5.7.284 vs 5.4.449**. A different TypeScript compiler emits a different error count from identical source, so the "baseline varies per worktree" note in `docs/GIT_HABITS.md` is not worktree content drift as assumed — it is *toolchain* drift. Corroboration: a clean lockfile-faithful `npm ci` in `~/mrc-node22` on 2026-09-05 gave TypeScript 5.8.3 and exactly **99** error lines, the lowest of the three. Consequence: never borrow or symlink that tree to seed another worktree, and take every baseline from a fresh `npm ci` in the tree being measured. **CONFIRMED AND CLOSED 2026-09-06, with the mechanism corrected.** The cause is drift between the **installed** `node_modules` and the **pinned** lockfile version. It is *not* the lockfile's dirty flag, which was the natural suspect and is wrong. **Evidence:** four worktrees created off `2c8087e` and seeded with a lockfile-faithful `npm ci` — `mrc-guard-fix`, `mrc-status-enum`, `mrc-pdf`, `mrc-send-email` — each resolved TypeScript **5.8.3** and each emitted **exactly 99** error lines from `npx tsc -p tsconfig.app.json --noEmit`. Four independent trees, one commit, one number. Against that, `~/mrc-travel-ef` had TypeScript **5.9.3 installed against a 5.8.3 lockfile** and is the shape that produces a different count from identical source. **A rewritten `package-lock.json` is a fingerprint, not the cause.** Four trees carried a modified lockfile on 2026-09-06 and the diff was byte-identical in all four (`@swc/core` `"dev": true` → `"devOptional": true`, plus that flag dropped from three platform entries; +3/−13). It changes **no package version** — all four still pinned typescript 5.8.3. It only tells you `npm install` was run there, which makes the tree suspect; it does not tell you the toolchain drifted, and a clean lockfile does not tell you it did not. **The check is one line, installed versus pinned:** `node -p "require('./node_modules/typescript/package.json').version"` against `node -p "require('./package-lock.json').packages['node_modules/typescript'].version"`. Equal → that tree's baseline is comparable. Different → discard it and re-seed with `npm ci`. **99 is now the gate**, and for the first time it is comparable across trees rather than per-tree. **Two surviving worktrees still carry the hazard and are called out in their own rows below: `~/mrc-travel-ef` and `~/mrc-reminder-ef`.** **AMENDED 2026-09-08 — the 99 is anchored at `2c8087e` and is stale from `dab41ba` onward. The honest figure on current `origin/main` is 100.** The delta is fully explained and is **not** toolchain drift: same 99 lines, exactly one added, nothing dropped. The added line is `api/render-job-report-pdf.ts` TS2339 `headless`, a **reachability** change described in T13 — a test's import dragged an always-broken file into the program. Drift was ruled out rather than assumed: installed TypeScript **5.8.3** == pinned **5.8.3** in the measuring tree with a clean lockfile, and both `package-lock.json` and `tsconfig.app.json` are **byte-identical** between `2c8087e` and `origin/main`. **Method, recorded so it can be repeated:** `git worktree add --detach <tmp> <anchor>`, symlink the measuring tree's `node_modules` into it — sound **only** because `package-lock.json` is byte-identical across the two commits, otherwise re-seed with `npm ci` — run `npx tsc -p tsconfig.app.json --noEmit` in both, then strip the `(line,col)` from every error line, `sort`, and `comm` the two sets. **Set-compare, never count.** Counting alone hides real movement in both directions: earlier the same day a 23-line shift made two pre-existing errors read as new while the total stayed at 100. **Test figure, same treatment.** Under identical env-less conditions: `2c8087e` = 71 files / **1147** passed, `origin/main` = 74 files / **1165** passed. The five env-blocked files hold **75** tests, measured by running exactly those five with stub `VITE_SUPABASE_*` values (a fake host, so nothing can reach a real project) — 5 files, 75 passed. That reconciles exactly: **1222 − 75 + 18 + 3 = 1168**, where 18 is what the repo gained since the anchor (three new test files) and 3 is the `fix/viewreport-history-guards` additions. **Name the trap: "5 files ≈ 54 tests" is wrong.** The naive gap from 1222 to 1168 is 54, but the blocked files hold 75; 21 tests added since the baseline mask the difference. Anyone reconciling a current count against 1222 must **add back what the repo gained before attributing the remainder to the env-blocked files**, or they will under-read the env gap by 21 and conclude the tree is healthier than it is. Gate going forward: still **no new error lines**, set-compared against a baseline taken in the same tree the same day — the absolute number is anchor-dependent and 99 is only correct at `2c8087e`. | [x] | — | 2026-09-06. Four `npm ci` trees at `2c8087e`: TypeScript 5.8.3 in all four, 99 tsc error lines in all four, 71 test files / 1222 tests passing in all four. `~/mrc-app-1` alongside them still runs 5.9.3 with a `.deno/` directory. | n/a |
| T17 | **`~/mrc-travel-ef` and `~/mrc-reminder-ef` are not safe to measure a tsc baseline in.** Both survived the 2026-09-06 worktree prune deliberately — travel-ef because `calculate-travel-time` is a frozen path pending the 30 Aug deploy, reminder-ef because it holds 4 unmerged commits — and **both still carry the rewritten `package-lock.json`**. `~/mrc-travel-ef` additionally has TypeScript **5.9.3 installed against a 5.8.3 lockfile**, which is exactly the T14 drift condition. Any tsc error count taken in either tree is untrustworthy and must not be compared against the 99 gate. Fix is one command per tree — `npm ci` — but it is deliberately not run yet: re-seeding `mrc-reminder-ef` would touch a tree holding unmerged work for a pending deploy, and that is the owner's call, not a cleanup task. Re-seed before either tree is used for measurement or review. | [ ] | | | |
| T15 | Codex 0.153.2 supports `PreToolUse` hooks (`codex features list`: `hooks stable true`; `PreToolUseDecisionWire` approve/block/allow/deny/ask). A Supabase guard is portable to Codex and can fail closed. Not doing it yet — Codex has no MCP and read-only reviews don't need it — but the earlier assumption that it couldn't be guarded was wrong. | [ ] | | | |
| T18 | **Pin the PDF functions to a region near Supabase.** `vercel.json:4-13` declares both `api/render-pdf.ts` and `api/render-job-report-pdf.ts` with `memory: 1024, maxDuration: 60` and **no `regions` key**, so they run in `iad1` (confirmed in the 2026-09-07 production logs); Supabase is Sydney. Every EF→Vercel and Vercel→Storage hop therefore crosses the Pacific, three times per render, carrying 7–11 MB of base64 JSON — the job-report EF embeds every photo as a data URI rather than a signed URL, which is why this payload is systematically the larger of the two (`api/render-job-report-pdf.ts:18-23`). Fix shape: add `"regions": ["syd1"]` to both function entries. **The before/after is already measurable from logs without opening a dashboard** — the phase timer logs `region` at `invocation_start` (`:371`) and brackets the transfer with `ef_headers_received` (`:243`) and `ef_body_read` (`:254`), the gap between which is exactly the payload crossing the wire. Take a baseline from those marks before changing anything. Logged 2026-09-08, not started. | [ ] | | | |
| T19 | **Replace the independent per-step timeouts with one deadline derived at handler entry.** `api/render-job-report-pdf.ts` currently carries three: `setContent` 45 s (`:202`), the EF-fetch `AbortController` 60 s (`:229`), and `page.pdf()`'s implicit `PDFOptions.timeout` default of 30 s (never passed at `:206-211`). **45 + 60 + 30 = 135 s against a `maxDuration: 60` ceiling** (`vercel.json:11`). Also entirely unguarded: `chromium.executablePath()` (`:186`, which decompresses the browser binary to `/tmp`), both Storage uploads (`:471`, `:486`), `auth.getUser` and the `has_role` RPC; puppeteer's own `protocolTimeout` (180 s default) is never set either. **The point is the sequencing, not the sum:** the EF fetch (≤60 s) runs *before* `setContent` (≤45 s), so those two alone reach 105 s, and Vercel SIGKILLs the invocation long before either internal guard can fire. Every one of these constants is therefore decorative on the path that matters — which is precisely why the phase timer writes each breadcrumb at the boundary instead of flushing an array at the end (`:54-60`). Fix shape: compute one deadline at handler entry from the platform ceiling minus a margin, and hand each step its remaining budget. Same treatment applies to the sibling `api/render-pdf.ts`. Logged 2026-09-08, not started. | [ ] | | | |
| T20 | **`main` is far enough ahead of `production` that the next merge is a release, not a fix.** Measured 2026-09-08 immediately after `git fetch origin`: `origin/production..origin/main` is **40 first-parent commits** (64 non-merge commits), spanning 2026-08-31 → 2026-09-07, with merge base `a5820dd` — the PR #113 merge. So merging tonight's P0 to `production` ships roughly 40 merged PRs at once under the banner of a one-line fix. Worth a deliberate release review rather than a routine promote: at minimum, walk the 40 with `git log --first-parent origin/production..origin/main`, and check the four `VITE_` Production env vars plus the bundle's Supabase ref before and after the deploy (the Supabase marketplace integration has clobbered them before — 2026-07-23 blank-page outage). Re-measure rather than trusting this number; it moves every merge, and the method is recorded here so it is reproducible. Logged 2026-09-08. **THE RELEASE HAPPENED — the 40-commit gap above is spent, 2026-09-08.** `origin/production` is now `5fc53c0` (PR #157, 2026-09-08) and `origin/production..origin/main` measures **0**: production contains every commit on `main`, verified individually for `2bb715f` (subfloor zero-out), `14b14f3` (PR #114), `c0350dc` (offline banner copy) and the 7 Sep job-report work (`61c3940`, `53e7bc6`). Any figure derived from the earlier reading — including a "115 commits behind" claim made in a 2026-09-08 audit that read a stale `origin/production` — is **void**; re-measure, never quote. What stayed open after the release was the habit, not the release — and it is now closed: **the post-promote env check was run and passed.** **11 Sep production-lag snapshot (separate from the completed 8 Sep release above): `origin/production=bd0e984`, unchanged since 8 Sep. The week's main-only Sentry ignoreErrors fix, equipment day counts and per-item equipment days are NOT in production.** Michael supplied the release-state fact; local refs corroborate `bd0e984` versus `origin/main=222386f`. The earlier zero-gap observation is historical. See `docs/CODEX_WEEK_PLAN.md` for how to read monitoring against each revision; this note does not reopen or undo the completed release/environment check. | [x] | | 2026-09-08 (Michael): live bundle `assets/index-Byp-gSMj.js` returns **4 matches for the PROD project ref**, so the deployed JS is pointed at PROD and the 2026-07-23 marketplace-clobber class did not fire on this promote. Bundle read, not a dashboard claim. | n/a |
| T21 | **The backlog triage has still not started.** It was the plan for the 2026-09-07 session before the `/api/render-job-report-pdf` P0 jumped the queue, and it has been deferred at least once before that. Re-measured 2026-09-08 after the reconciliation intake: this file holds **119 open `[ ]` rows** of 165 table rows (120 before T20 was ticked the same day), counted with `grep -c '\| \[ \] \|'` and `grep -c '^\| '` (the backslashes are the markdown table escape — the patterns themselves carry a bare pipe). The earlier figures on this row — 107 of 152, and the "98" before that — are superseded; the count moves with every intake, so **re-count rather than quoting any of them**. The 2026-09-08 reconciliation of eighteen team requests against the code added seven of the current rows (P1-23, P2-23, P2-24, P2-25, T22, T23, B7) and is itself an argument for the triage: six of the eighteen requests turned out to be untracked. Detail for every item lives in `docs/MRC_MASTER_BACKLOG.md` (2,567 lines); the previous triage pass, its method and its rulings are the "Engineering debt carried over from the old TODO.md" note at the bottom of this file, which judged 291 old items down to 99 live-and-unhomed and folded 18 of those. Triage is a session in its own right and should be scheduled as one, not attempted at the end of a build session. Logged 2026-09-08. | [ ] | | | |
| T22 | **`manual_total_inc_gst` is a complete final-price override with no UI, and it reads as a working feature.** The column is saved (`src/pages/TechnicianInspectionForm.tsx:4121`) and loaded (`:3384`), the type carries `manualTotal` (`src/types/inspection.ts:146`), and `src/lib/calculations/pricing.ts:382` holds a full override branch that back-derives ex-GST and GST from a manual total — but **nothing sets `manualTotal` above its initial `0`** (`:3005`). The override that actually works is the per-field labour/equipment one (`dacd7cf`, PR #78). This is the C10 shape in `docs/BUG_LEDGER.md` — a subsystem with no caller — and the next reader will conclude the final price can be typed. Decide delete-or-wire. The P1 rebuild rewrites this file, so settle it before P1-11. Logged 2026-09-08. | [ ] | | | |
| T23 | **A request chased twice can leave no trace in the tracker: Glen's 7 Sep "asap" escalation on equipment days is recorded nowhere.** He raised drying-equipment days on 31 Aug (Appendix H carries the 31 Aug line) and escalated on 7 Sep marked asap. The escalation appears in no row of this file, no line of `docs/MRC_MASTER_BACKLOG.md` and no session log — found on 2026-09-08 while reconciling eighteen team requests against the code. Consequence: session planning cannot see that an owner has asked twice, so P2-3 and P2-23 read as ordinary backlog. Needs a capture step between Slack and this file, and a way to mark a row as chased and by whom. Not a code change. Logged 2026-09-08. | [ ] | | | |
| T24 | **One vitest test fails on the Node version `package.json` pins, and the runtime we were using was hiding it.** `src/lib/api/reportPipeline.test.ts` → `fetchVersionPdfBlob` → "returns the file from the signed URL when it resolves" fails with `TypeError: blob.text is not a function`. **Isolated by A/B on that single file, 2026-09-08 in `~/mrc-equip-days` at `dab41ba`: Node 24.20.0 → 18/19; Node 23.7.0 → 19/19.** `package.json` `engines.node` is `24.x`, so the *correct* runtime is the failing one — this is not a reason to pin back. Mechanism is jsdom / `Response.blob()` interop inside the test environment: the test sets `global.fetch` to return `new Response(PDF_BYTES)`, `fetchVersionPdfBlob` calls `response.blob()`, and on Node 24 that yields an object with no `.text()`. **Not caused by any diff** — it reproduces on a clean tree at `origin/main` — and **not caused by `.env.test.local`**, since the test mocks `global.fetch` outright and never reaches Supabase. It was previously invisible for two independent reasons: trees without an env file cannot load this file at all (see the `AGENTS.md` note added with this row), and trees on Node ≤ 23 pass it. **Production impact is UNVERIFIED and reasoned only** — same discipline as P1-24: `fetchVersionPdfBlob` runs in a browser, where `response.blob()` returns a real Blob with `.text()`, so the expectation is that this is a test-environment artefact with no customer-facing consequence. That expectation has not been executed against a browser and must not be recorded as though it had. Acceptance criterion: run the download path in a real browser (or a Playwright context) and confirm the blob has `.text()`; if it does, fix the test's environment rather than the source. **Named and excluded from this session's gate by ruling (Michael, 2026-09-08): the equipment-days baseline is 74 files / 1240 tests, 1239 pass, this one failure known — "no NEW failures".** Logged 2026-09-08. | [ ] | | | |
| T25 | **Sentry connector unavailable for all seven monitoring runs, 8–11 Sep.** Connection stayed **`needs_reconnect`**: four days with no browser-error visibility through monitoring. **Michael must reconnect OAuth; this is an account action, not a code item.** Verify the next run can read browser errors before treating silence as health. Separate from P1-S-1's production suppression rules and P2-40's build-environment tagging; reconnecting fixes neither, and this observation does not prove the SDK stopped ingesting events. | [ ] | Michael — OAuth reconnect | 2026-09-11 intake: all seven connector checks, 8–11 Sep | n/a (account action) |
| S0b | **The production gate validates which refs a review used, not which commits — a stale checkout passes it.** `docs/DEPLOYMENT.md` (shipped `cdcadeb`, 2026-09-08) fixed the base: the production review compares `origin/production...origin/main`, not `origin/main`, because by then the work is already on `main` and an `origin/main` base reads an empty diff. That half is correct. What it does **not** do is bind the review to the commits actually being released. It checks the symbolic range and a nonzero line count, both of which a week-old checkout satisfies. Reproduced 2026-09-08: `origin/production...2f351f9` is 118 insertions / 3 deletions and `origin/production...origin/main` is 123 / 3 — both print a `Target:` line the gate accepts and a count well above zero, but the first omits a commit, and `2f351f9` is a genuine ancestor of `origin/main`. `main` advancing *after* a review opens the same hole from the other end. **Same class as the near-miss already on the record** (`docs/codex-review-log.md`, `fb013db` row): a stale `origin/main` made the branch measure 663 reviewable lines of already-merged work, and only a manual `git fetch origin` caught it — the gate as written would not have. **Shape of the fix:** refresh both refs before the review; record the reviewed base and head SHAs; verify those SHAs against the PR before opening it and again before merging; invalidate the gate if either moves. **Must land before the end-of-sprint terminal CLI pass** — that pass *is* the production gate, so a stale ref there under-reviews the entire sprint in a single run. Raised by Codex adversarial review thread `01a07ea7-482d-7081-bd15-9d8175d66825` (round 2 of 2 on the S0 docs unit), ACCEPTED-DEFERRED by Michael because fixing it needed a round 3 the two-round cap forbids. Do not fold into another unit — it gates the sprint's own closing review. | [ ] | | | |
| P2-40 | **Sentry tags every build as `production`, so preview and production issues commingle in one environment.** `src/lib/sentry.ts:28` sets `environment: import.meta.env.MODE`, and Vite's `MODE` is `"production"` for any `vite build` — which is what Vercel runs for **preview** deployments too. So a preview deploy reports as production, and there is no way to filter an issue by where it actually came from. **The damaging direction is the one that looks fine:** noise from previews inflates production issue counts and alerting, and a real production regression is harder to see inside it. Fix is to derive the environment from a Vercel-provided variable (`VERCEL_ENV` exposed as a `VITE_` var at build time), not from `MODE`. **Not closed by PR #167** (`fix/sentry-prod-visibility`, merged 2026-09-09) — verified 2026-09-11, `:28` still reads `import.meta.env.MODE`; that PR addressed a different axis. Distinct also from the standing gap where `!response.ok` paths throw without `captureBusinessError`, so HTTP-error branches exist only in the Sentry *logs* dataset and never become issues. Logged 2026-09-11. | [ ] | | 2026-09-11, code read at `src/lib/sentry.ts:28` | |
| P2-42 | **Migration drift: `20251112000020_add_lead_activity_triggers.sql` is in the repo and its function is absent from PROD.** So a file's presence in `supabase/migrations/` is not evidence that its objects exist, and this one is the concrete instance. **The general shape is worse than the single case: 111 of 126 repo migrations are absent from the ledger**, which makes `schema_migrations` useless as an oracle for what is actually applied — a lookup there returns "not applied" for the overwhelming majority of migrations that plainly *are*. **Use `src/integrations/supabase/types.ts` as the oracle instead** (it is generated from PROD) and diff per object; then write a ledger row after every hand-apply, or the gap keeps widening. **Needs PROD queries, so it is explicitly out of the 11–14 Sep Codex window** — every `supabase` command is banned there (`docs/CODEX_WEEK_PLAN.md`). Michael runs the reconciliation in Studio. Relevant to P2-39, which concluded "no DB function writes `activities`" — that conclusion was reached against PROD directly, not against the ledger, which is the method this row is arguing for. Logged 2026-09-11. | [ ] | Michael (Studio) | | |

## ⏸️ Still blocked — waiting on someone

| ID | Item | Waiting on | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|---|
| B1 | Full residential demolition hourly table. Interpolated rows in use meantime. | Clayton | [ ] | | | |
| B2 | Glanz email and mobile. | Glen | [ ] | | | |
| B3 | 0b PROD verification. | Michael | [ ] | | | |
| B4 | What is the "Split" tab in splitracker? Ask before speccing P3-7. | Glen | [ ] | | | |
| B5 | Glanz Sunday availability (currently Wed/Fri/Sat). | Glen | [ ] | | | |
| B6 (was P2-11) | Photo flow: button presses per area. Bulk capture shipped; does it cover his complaint? | Clayton | [ ] | | | |
| B7 | @mentions in job notes (P2-25). Lead notes have them; the job-completion Job Notes screen does not. Which did he mean? | Clayton | [ ] | | | |

## 🔥 Health-check findings — 26 Aug to 11 Sep

The systemic pattern: functions return 200 on failure. Session J audits every Edge Function for it before fixing A to E one at a time. Constraint on J: do not touch `calculate-travel-time` or `send-inspection-reminder` until Michael confirms the pending deploys of 30 Aug landed.

**Session J is not worth running yet (measured 2026-09-06), and the reason is the constraint above.** Counting `status: 200` returns per Edge Function: `calculate-travel-time` **6**, `send-inspection-reminder` **2**, then `send-slack-notification`, `send-email`, `receive-framer-lead`, `fetch-resend-email` and `check-photo-moisture-orphans` at **1** each, and `seed-admin`, `manage-users`, `generate-job-report-pdf`, `generate-inspection-summary`, `generate-inspection-pdf`, `export-inspection-context` and `check-overdue-invoices` at **0**. The two worst offenders are, by a clear distance, exactly the two files J is forbidden to touch — they hold 8 of the 13 occurrences between them. What is left inside J's permitted scope is one occurrence each across five functions, one of which (`send-email`) is carved out to L anyway. J becomes worth a session when the 30 Aug deploys are confirmed landed and the freeze lifts; until then it is four single occurrences, and running it early spends a session to reach the least of the problem. **This is a scheduling finding, not a person-blocker** — nobody is being waited on except for confirmation that a deploy landed.

**P0-A diagnosis corrected 11 Sep:** the earlier suggestion that this was probably only the Vercel `hard_save` writer is superseded by a confirmed Edge Function constraint mismatch. `generate-inspection-pdf` writes an invalid generation type and swallows 23514; see P0-A. `api/render-pdf.ts`, MRC-APP-1A and T13 remain separate paths/issues. Historical Session J scheduling notes above are not the current diagnosis or scope.

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| P0-A | **LIVE: every inspection EF history insert fails the generation-type constraint; response still 200.** Since at least **7 Sep**; by 11 Sep **all 48 PROD inspections have fewer `pdf_versions` rows than their `pdf_version` counter**. Michael read the PROD constraint directly: `CHECK (generation_type = ANY (ARRAY['legacy_ef_render','hard_save','manual_upload_fallback']) OR generation_type IS NULL)`. The function instead writes **`generation_type: 'legacy_ef'`**, `supabase/functions/generate-inspection-pdf/index.ts:2391` (`bd0e984` production-revision source: `:2383`), so each such insert raises **PG 23514**. Parent URL/counter already updated at `:2354–2361`; `:2397–2399` only logs the insert error, then `:2403–2411` returns `success: true` with default **HTTP 200**. **Supersedes the earlier Vercel-only hypothesis above.** Fix shape: use the permitted writer value, make history persistence failure visible instead of reporting complete success, keep counter/history writes coherent, and reconcile historical gaps after diagnosis. Counter deficits alone do not prove every missing row has this one cause: the separate `returnHtml` path also advances counters without history. Acceptance: persisted generation creates a permitted history row; injected 23514 cannot return complete success. | [ ] |  | 2026-09-11, Michael: PROD constraint, 23514 failures and 48/48 counts; repository trace |  |
| P0-B | **LIVE: bounced booking confirmation remains logged as sent, with no resend.** **"Inspection Booking Confirmed — 21 Sept 2026"**, recipient domain `cplusm.com.au` (address omitted), bounced **10 Sep 10:10**; no resend through the 11 Sep intake and `email_logs.status='sent'`. The customer probably never received the confirmation. Repo mechanism: `send-email/index.ts:90–91,222–228` records Resend API acceptance as `sent`, not later delivery; `fetch-resend-email/index.ts:122–133` returns provider `last_event` on demand without updating the stored row. No bounce reconciliation writer was found in `supabase/functions/`; this is a repo observation, not proof about external webhook configuration. Fix shape: reconcile provider events by message ID, surface bounced/failed outcomes, and assign recovery after checking the bounce reason/contact route. Acceptance: a provider bounce after acceptance updates the app record and becomes actionable. Distinct from P0-14, which never reaches Resend or inserts an email row. | [ ] |  | 2026-09-11 intake, Michael: Resend bounce/no resend and PROD sent row; repository trace |  |
| P0-C | **Scheduled functions still double-fire; new evidence locates duplication downstream of pg_cron.** Seven-run intake: `send-inspection-reminder` twice every hour for **13 straight hours**, `check-overdue-invoices` twice at **09:00**, and `generate-job-report-pdf` twice on **10 Sep**, producing **23505 plus a duplicate Slack post**. **PROD `cron.job` has exactly ONE entry per job and every `cron.job_run_details` row is one `succeeded` run: this is pg_net / dispatch-layer duplication, not duplicate cron registrations.** Exact request-redelivery mechanism remains unresolved. `check-overdue-invoices/index.ts:13–24` already describes duplicate delivery between pg_net and the gateway; reminder has an atomic claim at `send-inspection-reminder/index.ts:328–351`, so double invocations alone do not prove double customer emails. Correlate cron run → pg_net request → EF invocation IDs and verify each side effect's idempotency. Diagnose as ordinary follow-up per 11 Sep intake; P0 prefix retained as a stable ID. The shipped duplicate-scheduling fix does not close this recurring downstream failure. | [ ] |  | 2026-09-11, Michael: PROD cron tables/run details and monitoring logs; repository trace |  |
| P0-D | `login_activity` has written nothing since February. | [ ] | | | |
| P0-E | Google Maps API key. Verify first, may already be fixed. | [ ] | | | |
| P1-S-1 | **Production still suppresses real failures; fix is merged to main only.** PR #167 removed **Failed to fetch, NetworkError, Load failed, AuthRetryableFetchError** from `src/lib/sentry.ts` (`:15–18,63` at `222386f`). Production remains **`bd0e984`**, whose `ignoreErrors` still contains all four. Thus an 8–11 Sep monitoring note that those strings are suppressed is **CORRECT about production and stale about main**. Do not reimplement the merged fix or mark production visibility restored from a main merge. Separate OAuth monitoring gap: T25. | [ ] |  | 2026-09-11: local git comparison bd0e984 vs 222386f; Michael supplied production state |  |
| P1-S-2 | Sentry dev project has had zero events since 26 Aug. Check DSN and env wiring. | [ ] | | | |
| P1-S-3 | Sentry replays at 80% of the free quota; period ends 17 Sep. Lower the sample rate. | [ ] | | | |
| P1-S-4 | **Render-endpoint failures never reach Sentry.** The `!response.ok` branch in `src/lib/api/jobReportPipeline.ts:96-109` throws without calling `captureBusinessError`; only the transport `catch` (`:56-63`) reports. `src/lib/api/reportPipeline.ts:125-135` has the same shape. Result: **8 production failures over 30 days (4x 504, 4x 500) were invisible as Sentry issues** — they existed only as console logs scraped by `consoleLoggingIntegration`. Found while diagnosing the 2026-09-07 P0. Its own unit; do not fold into the render fix. Note `api/` and `supabase/functions/` have NO Sentry SDK at all, so server-side errors live only in Vercel/Supabase platform logs. | [ ] | | | |
| P1-S-5 | **Version reuse/race on `job_completion_pdf_versions` is still occurring 11 Sep.** `generate-job-report-pdf` regenerations reuse a version instead of advancing the persisted history, raising **23505** on **`(job_completion_id, version_number)`** and dropping the new row; concurrent invocations chose version 1 on 7 Sep, with another duplicate invocation on 10 Sep (P0-C). Repo nuance: `supabase/functions/generate-job-report-pdf/index.ts:408` already computes **`(jc.pdf_version \|\| 0) + 1`** on both regeneration branches. It uses the snapshot fetched at `:178–182`; concurrent readers can choose the same version, and parent-update failure at `:479–480` is only logged. History rejection at `:500–501` is also only logged before success/HTTP 200 at `:506–514`, leaving Storage HTML without a history row. The observed persisted counter non-advance is factual; which read/update/dispatch caused each occurrence needs correlated logs. `api/render-job-report-pdf.ts:284–322` already reads latest history and retries 23505 up to three times; EF does not. Acceptance: overlapping generations from one initial counter commit distinct versions or return an explicit recoverable failure. Separate EF unit, Michael-applied deploy. | [ ] |  | 2026-09-11 intake, Michael: recurring PROD 23505; repository trace |  |
| MRC-APP-1D | "Photo upload failed" on `/technician/inspection`, iPhone Safari, first 2 Sep. Adjacent to the #111/#112 photo work. | [ ] | | | |
| MRC-APP-1B | "Failed to upload lead note attachment" on `/leads`, first 27 Aug. Suspect commit `20eac74`. | [ ] | | | |
| MRC-APP-1A | "Hard-save endpoint unreachable: POST /api/render-pdf" on `/admin/leads`. | [ ] | | | |
| MRC-APP-14 / -17 | MIME-type errors on `/admin/schedule` and a technician job page. | [ ] | | | |
| MRC-APP-18 | "Inspection form save failed", 25 Aug. | [ ] | | | |
| MRC-APP-19 | ServiceWorker registration AbortError. | [ ] | | | |
| MRC-APP-1C | Validation block on an incomplete inspection. Not a fault. Ignore. | — | | | |
| P2-X-1 | Duplicate lead submission (27 Aug, two confirmations sent). Add idempotency to the lead form. | [ ] | | | |
| P2-X-2 | `fan_out_notification` RPC returning PGRST202; one in-app notification row lost 31 Aug. | [ ] | | | |
| P2-X-3 | `manage-users` 503s, twice on 1 Sep. | [ ] | | | |
| P2-X-4 | Two job completions stuck in draft since 24 Aug. | [ ] | | | |

About 80 leads sit uncontacted in `new_lead`. Check P0-2 before treating that as an ops failure: if the list hides leads 51 onwards, the team cannot see them.

## ✅ Shipped — do not re-log

**Leads:** repeat clients, unit numbers kept, lead notes with @mentions + attachments, full-database search, optional preferred date/time, "Start Job" opens job form, @mentions in job notes.

**Schedule:** To Schedule search rail, Reschedule deep-links, hour/minute/AM-PM picker, conflict banner detail, booking cancel resets lead correctly, 7 stranded leads restored + 3 duplicates archived, colour-coded cards.

**Photos:** auto captions, before-photos on the day, before + after photos on jobs with no inspection, bigger delete target, bulk upload.

**Reports/PDF:** false "NOT saved" error fixed, email size fix, hide-area-from-report, ex-GST display, two layout overlaps fixed, room names legible on cover, real save error messages.

**Pricing:** 2h rates corrected, saved estimates recompute at current rates, manual override works.

**Notifications:** in-app bell, Framer confirmations + reminders logged, duplicate scheduled-job firing fixed historically; **downstream duplication recurred in 8–11 Sep monitoring — see P0-C**.

**Sessions C & D:** Equipment Days multiplier (branch, keep). Option 2 stacking fix (branch — labour half needs rework per §Mixed jobs). Subfloor toggle zero-out (merged `14b14f3`).

**Official fix list §6 items 1–3 complete:** report emailing bug, unit numbers, repeat clients.

## Engineering debt carried over from the old TODO.md

Audited 2026-09-05 against the backlog. The old file is `docs/TODO.md` at commit `3191a39`; `L` numbers below point into it. Its first 1,448 lines (291 open items) were judged item by item: 60 already have a backlog ID, 68 shipped, 54 were launch-era checks that are dead, 10 were superseded by the pricing canon or the rebuild, and 99 were live and unhomed. None of the 99 blocks handover: nothing here stops Glen, Clayton or Vryan getting through normal work. Eighteen of their lines were folded into P2-3, P3-3 and P1-22 on 2026-09-05 (rulings in hand); the remaining 65 clusters are kept as one line each so they are not lost, and are not scheduled. Lines 1,449 to 2,624 of the old file were checked only for handover blockers (none found) and are not itemised here; read them from the commit above if needed.

Three findings from the audit are tracked as items:

| ID | Item | Done | Branch / Session | Verified | Ledger entry |
|---|---|---|---|---|---|
| E-1 | NEEDS INVESTIGATION. `types.ts` declares `PostgrestVersion` 14.17, which matches neither PROD (13.0.5) nor DEV (14.5) as recorded on 17 Aug. Commit `54a60b9` on 27 Aug set it; the source project is unknown. Nothing should be trusted from `types.ts` until this is answered. Do not guess. | [ ] | | | |
| E-2 | P3-4 Framer wiring confirmed live; P3-4 closed. | [x] | | 2026-09-05: Source: Website leads in PROD (Michael) | n/a |
| E-3 | Verify the Resend Edge Function secret. The key was rotated (the old TODO.md records it); whether the EF secret was updated is unknown. Check the secret and a live send. | [ ] | | | |

Live, unhomed, not blocking (one line per cluster; sub-steps folded into their parent):

- L36 — Fix Supabase Site URL from /admin subpath to root https://mrcsystem.com
- L138 — Evaluate Apple Branded Mail via Apple Business Connect before BIMI
- L155 — Delete SUPABASE_SERVICE_ROLE_KEY from Vercel
- L860 — Drop unused formatPercent import in TechnicianInspectionForm.tsx
- L901 — Reports page fails whole-page on revenue query error; technician surfaces degrade instead
- L912 — Decision: defer unifying Reports error degradation to its own scoped change
- L934 — Section 7 both-options save guard over-fires on auto-save and navigation
- L935 — Section 7 both-options save guard over-fires; move check to submit/PDF time
- L958 — Extract jobCompletionSchema.ts or document keeping validation inline
- L1011 — PDF-CL1: rename misleading pdf_versions.pdf_url to html_public_url
- L1014 — PDF-CL4: drop inspections.pdf_blob_url column once unread
- L1015 — PDF-CL5: decide whether to add audit trigger on pdf_versions
- L1017 — PDF-CL7: previewOnly EF branches should write an audit row
- L1018 — PDF-CL8: build unified job-report version-history UI
- L1019 — PDF-CL9: remove legacy write path in generate-job-report-pdf EF
- L1020 — PDF-CL10: drop job_completions.pdf_blob_url column
- L1022 — PDF-CL11: job_completion_pdf_versions RLS policies missing from repo
- L1023 — PDF-CL12: **WITHDRAWN 2026-09-07 — premise disproven, no work to do.** The row claimed the 23505 retry in `api/render-job-report-pdf.ts:198-236` was dead code *because no UNIQUE constraint exists*. It does exist: Supabase EF logs on 2026-09-07 show a live `23505 duplicate key ... (job_completion_id, version_number)`, and Postgres raises 23505 **only** on a unique violation, naming the columns of the index it violated. So the retry is live, working code — keep it. **Why the row was wrong, which is the part worth keeping:** the table's `CREATE TABLE` was made in Studio and is not in the migrations folder at all — stated outright in `supabase/migrations/20260531150202_job_completion_pdf_versions_pipeline_columns.sql:18-19`. Grepping the repo for a unique index therefore returns nothing whether or not one exists, and absence was read as proof of absence. That is the exact method the pre-flight rule in `CLAUDE.md` forbids: schema state is verified via `information_schema`, never via repo file presence. See P1-S-5, which is the same constraint seen from the other side and is correct.
- L1024 — PDF-CL13: render-job-report-pdf reports wrong validation error for bad mode
- L1025 — PDF-CL14: stale fire-and-forget JSDoc on sendEmail in notifications.ts
- L1026 — PDF-CL15: /admin/render-test PDFs accumulate unpruned in report-pdfs Storage
- L1027 — PDF-CL16: report-pdfs bucket has no DELETE policy, cleanup remove() calls are silent no-ops
- L1028 — PDF-CL17: switch generate-job-report-pdf to signed photo URLs instead of base64 data URIs. **Production failure attached, 11 Sep intake: 10 Sep 19:58, a 20-photo job returned HTTP 546, "Memory limit exceeded".** `supabase/functions/generate-job-report-pdf/index.ts:235–269` downloads six photos concurrently, builds buffer/binary/base64 copies and retains every data URI. This supports the memory mechanism; no memory profile was taken in this intake. Evaluate signed photo URLs with sufficient lifetime/access for rendering instead of retaining photo bytes in EF memory; verify with representative photo sizes under the EF limit. Michael's monitoring observation is a production fact; no code changed.
- L1050 — AH2 follow-up: consider warning at hide time about narrative references
- L1051 — AH3 FIX: Demolition page survives hide when demolition_content is set
- L1057 — AH3 product call: drop Demolition page or keep and prompt re-edit
- L1074 — AH5 sweep: check photos, moisture_readings, job_completions for select drift
- L1088 — AC1: on-site before-photo provenance is a heuristic, not a stored fact
- L1101 — AC1 residual false positive: general inspection photo picked as before photo gets Delete
- L1116 — AC1 proper fix: add photos provenance column via migration, replace heuristic
- L1119 — AC1: delete the session-id union when provenance column lands
- L1121 — AC3: stale-PDF banner does not clear after successful regeneration (+1 sub-steps folded)
- L1134 — AC4: sentinel captions shown raw to technicians in before-photo picker
- L1159 — AC2: OutdoorPhotoSlotGrid breaches outdoor sentinel caption invariant
- L1163 — AC2: ImageUploadModal.tsx:136 breaches outdoor sentinel invariant
- L1164 — AC2: ImageUploadModal.tsx:168 maps direction_photo to illegal photo_type 'direction'
- L1168 — AC2: ViewReportPDF.tsx:2307 writes caption null, violating Stage 4.1 invariant
- L1170 — AC2: loadUnplacedPhotos has no job_completion_id filter; admin pool shows job photos
- L1172 — AC2 question: does any admin workflow legitimately re-place a job photo into an inspection slot?
- L1174 — AC2: isNetworkLevelError duplicated four ways; consolidate into shared module
- L1183 — W6.1-A: unify property_occupation enum label rendering across surfaces
- L1185 — W6.1-B: read old status from lead.status in FinishLeadSection logFieldEdits
- L1187 — W6.1-C: parallelise sequential snapshot fetches with Promise.all
- L1188 — W6.1-C: Promise.all the three snapshot awaits in TechnicianInspectionForm section save
- L1189 — W6.1-C: Promise.all subfloor_data and inspection_areas fetches in TechnicianJobDetail
- L1195 — W6.1-F: anchor percent group in check-photo-moisture-orphans caption regex
- L1197 — W6.1-G: migration 20260513_phase5_dead_column_drop.sql lacks HHMMSS suffix
- L1199 — W6.1-H: scrub raw DB error details from check-photo-moisture-orphans response
- L1226 — L1 finding 2: 'Capped at 5 days' shown on PDF but not enforced
- L1298 — Verify RLS policy parity between DEV and PROD
- L670 — Upload job-report-template.html to bucket under same name
- L789 — Keep pre-deploy `vercel env ls production` check as habit
- L803 — Triage the 6 old git stashes; recover wanted, drop rest
- L820 — Rename Leads-to-Assign card subtitle to avoid 'Needs attention' collision
- L827 — Decide whether to reschedule overdue cron for AEDT drift in October
- L829 — Rotate the DEV admin password
- L831 — Google Fonts Inter woff2 fails to load on preview (CSP font-src)
- L832 — Decide 'Completed This Week' semantics (updated_at filter vs completion events)
- L386 — Remove SUPABASE_SERVICE_ROLE_KEY from Vercel Preview and Production scopes
- L407 — Step 4: runbook block B applies 20260729153000 constraint migration after A
- L446 — Run INVOICE_INTEGRITY_RUNBOOK on DEV first, confirm clean, then PROD
- L448 — Runbook block B: apply invoice_totals_integrity_checks migration (two VALID CHECKs)
- L470 — Decision: both CHECK constraints required, neither alone catches both defects
- L1380 — Verify SYSTEM_USER_UUID secret set in production Supabase (+2 sub-steps folded)
- L1398 — S5 Refresh PHASE_2_EXECUTION.md '16 active tables' count

