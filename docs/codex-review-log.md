# Codex review log

One row per review. This log is the only input for any knowledge-base entry on this workflow.
Procedure, rulings and the exact invocation: `docs/CODEX_WORKFLOW.md`. Rows here follow its §5; the ~150 limit is the mechanical count in its §4.

| date | repo / worktree | diff lines | review type | model / effort | findings | acted | rejected | unclear | missed later | wall time | usage after `/status` | notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-09-05 | mrc-merge / docs/backlog-pricing-canon-and-ledger, ddfa6ca vs main 3191a39 | 93 added, 3 files | adversarial, foreground, `--wait --base main`, MRC default focus, round 1 of 2 | gpt-6-astra / high, config intent only (#705) | 0 | 0 | 0 | 0 | — | 26s | not captured, no non-interactive path found | Target: `branch diff against main`. Verdict: approve. Codex thread `01a0704d-d03b-7422-b6a7-734dadbb0329`. Smoke test on the Part A setup diff, docs only. During the review Codex ran `cat AGENTS.md; cat AGENTS.override.md` as a disk read of files adjacent to the change; `codex debug prompt-input` confirms the instruction channel carried only the override. No git hooks in this repo, so #697 does not apply. Review gate off in both state roots (see Setup findings 1). |
| 2026-09-05 | mrc-node22 / chore/node-24, dc52bdc vs main 4d7c15b | 13 added, 6 removed, 5 files | adversarial, foreground, `--wait --base main`, MRC default focus, round 2 of 2 | gpt-6-astra / high, config intent only (#705) | 0 | 0 | 0 | 0 | — | 52s | not captured, no non-interactive path found | Target: `branch diff against main` — checked before reading the body, as briefed. Verdict: approve, no material findings. Codex thread `01a07185-4c26-7b41-9a90-6279e251bc2e`. Config + docs diff (the Node 24 pin). Codex independently re-derived this session's dependency result rather than taking it on trust: it evaluated all 1147 lockfile entries against Node 24 and ran a live import of the PDF dependencies under v24.20.0, both clean. It again ran `cat AGENTS.md ...` as a disk read of files adjacent to the change — same behaviour as round 1, so that is now a pattern rather than a one-off. Invoked by running `scripts/codex-companion.mjs` directly, because `/codex:adversarial-review` carries `disable-model-invocation: true` and cannot be called by the model; flags were identical to what the slash command would have passed. Codex's only caveat matches the session's own stated limit: full build and deployed PDF render not independently verified by it. |
| 2026-09-05 | mrc-offline / fix/offline-banner-copy, c0350dc vs main 61f77e9 | 27 added, 16 removed, 2 files | adversarial, foreground, `--wait --base main`, MRC default focus, round 3 of 3 | gpt-6-astra / high, config intent only (#705) | 0 | 0 | 0 | 0 | — | 65s | not captured, no non-interactive path found | Target: `branch diff against main` — checked before reading the body, as briefed. Verdict: approve, no material findings. Codex thread `01a071c5-e728-7783-8b9b-8b09fb15ded7`. First **code** diff of the three runs (rounds 1 and 2 were docs and config): the offline banner copy, P1-22 item 1a. Codex did not take the claim on trust — it read `OfflineBanner.tsx`, `useOfflineSync.ts` and grepped `saveDraft(|queuePhotoOffline(` to confirm for itself that nothing writes the offline queue before agreeing the old copy was false. It ran `cat AGENTS.md` alongside the changed files for the **third** consecutive run, so the adjacent-file disk read is confirmed as standard behaviour, not a pattern of two. Its one next step — run the OfflineBanner suite — could not be actioned by Codex because `mrc-offline` has no `node_modules`; I had symlinked `~/mrc-app-1/node_modules` (lockfiles verified identical), run the suite 14/14 green and a tsc baseline check (122 before and after, no new error lines), then removed the symlink before invoking the review. Sequencing artefact, not a gap: the verification exists, Codex just could not repeat it. Next run should leave the symlink in place so Codex can execute tests itself. |
| 2026-09-05 | mrc-guard-hook / chore/guard-hook-and-worktree-sync, `8984fed` vs main `c9af490` | 161 added, 5 removed, 2 files — 166 raw, **~42 reviewable** (see precedent below) | adversarial, foreground, `--base main`, focus "can any Supabase CLI invocation reach PROD through a path the allowlist doesn't cover" + MRC default focus, round 1 of 1 | gpt-6-astra / high, config intent only (#705) | 3 | 3 | 0 | 0 | — | not captured (single foreground call) | not captured, no non-interactive path found | Target: `branch diff against main` — checked before reading the body, as briefed. Verdict: **needs-attention**. Codex thread `01a071d5-3765-7542-8e89-9c7673b3a34f`. **Base verified before the run, not assumed**: local `main` and `origin/main` both at `c9af490`, and three independent measurements (`git diff main`, `git diff origin/main`, `git show HEAD`) agreed at 161/5 — so the issue #653 base-widening trap was ruled out rather than hoped past. Three HIGH findings, every one **independently reproduced** by feeding synthetic PreToolUse payloads to the hook: 7 of 7 probes bypassed. No Supabase CLI was invoked and no database was touched by the reproduction. Findings: (1) the CLI regex misses absolute paths, `env` wrappers and newline-separated commands, and because rule 1 runs *after* detection, `/opt/homebrew/bin/supabase functions deploy foo --project-ref <PROD>` returns ALLOW; (2) rules 3 and 3.5 scan the whole collapsed string, so `supabase --version; supabase db query --linked -f x.sql` returns ALLOW; (3) `CLAUDE_PROJECT_DIR` overrides the payload cwd, so the hook can verify a DEV link while the CLI resolves PROD. Triage: 3 acted, 0 rejected, 0 unclear — all three carry file:line and a stated failure mode and all reproduce, so none were rejectable on form. **EXIT by decision, not by convergence**: three findings survived triage, and round 2 on the same diff would re-litigate rather than add, so the cycle was stopped and reported (Michael, ruling). Fixes deferred to **P0-9 / Session R**; the restored hook is byte-identical to the copy already gating this machine, so distributing it is strictly an improvement on no guard, and the sweep proceeded on that basis. Incident 3 in `docs/POST_INCIDENT_FRAMEWORK.md` reopened on this evidence. **This is the first review of the night whose findings changed the plan** — rounds 1 to 3 were all clean approvals. And finding 2 is the same class as this session's own theme: line 87 of `scripts/test-supabase-guard.sh` asserts `"supabase --version; echo x"` → ALLOW, so the suite does not merely miss the bypass, it pins it as correct behaviour — a test that keeps passing because it asserts the bypass is correct. |
| 2026-09-06 | mrc-app-1 / feat/schedule-rail-search-deeplink at c2803de, dirty tree (43 files) | working tree, not a branch diff: GitNexus skill regen churn + untracked docs; not measured | adversarial, foreground, NO --base — **UNINTENDED**: a plan-mode probe passed `--help` to the subcommand, which has no help flag; the token became focus text and started a live review | gpt-6-astra / high, config intent only (#705) | 1 | 0 | 0 | 1 | — | not captured (over 120 s; the Bash tool backgrounded it) | not captured | Target: `working tree diff` — wrong target under ruling 3, so this is logged as an incident, not a review. Verdict: needs-attention. Codex thread `01a074c4-4dcb-7522-b9a3-bffba4f02b5f`. Finding (medium): `docs/TEST_LEAD_PURGE_ZZ_TEST_MICHAEL.md:161-165` — BEGIN in one SQL Editor request and COMMIT in another cannot hold the transaction, so the Storage deletes in block B1 can outlive a failed row delete. Untracked doc outside this session's scope; carried to Michael untriaged. Self-collect mode read every untracked file in the checkout: two personal root notes (since moved to `~/dev/policies`), the purge runbooks, `docs/multi-tech/deploy/*`. PII scan of those: business addresses and test-lead gmail addresses only; the four phone-pattern hits are migration ids and clock times. Michael to confirm nothing else. The `--help` trap is now §3 of `docs/CODEX_WORKFLOW.md`. |
| 2026-09-07 | mrc-app-1 / chore/workflow-scaffolding, `61c3940` vs `origin/main` `68c8f73` | 81 added, 3 removed, 2 files — **87 reviewable** excl. `docs/sessions/` | adversarial, background with `--wait`, **`--base origin/main`** (see note), focus: lifted-callback races, object-URL lifetime, `window.open` semantics, blob-tab CSP, disabled-gate stranding | gpt-6-astra / high, config intent only (#705) | 1 | 0 | 0 | 0 | — | ~4 min | not captured, no non-interactive path found | Target: `branch diff against origin/main` — printed and checked before the body was read. Verdict: **needs-attention**. Codex thread `01a07bc8-2e40-7cc2-9536-78fbae6b37aa`. **Base deviation declared:** Michael's brief said `--base main`; `CLAUDE.md` forbids it verbatim (stale local `main`, the #653 class, and a bad base exits 0), so `--base origin/main` was used and the deviation reported rather than silently taken. Unit A only — an escape-hatch View button opening the already-rendered job report in a new tab via a Blob typed `text/html`, deliberately not `window.open(pdf_url)` because Storage serves public-bucket HTML as `text/plain` + `nosniff`. Unit B (the 504 root cause) was NOT started: Michael is pulling Vercel runtime logs first. One medium finding, **not applied** (review-then-stop): `jobReportHtml` is not invalidated when `htmlUrl` changes, so during a version switch or a regenerate the View button stays enabled and exports the PREVIOUS version. Codex reproduced it with a deferred second fetch. CC independently confirmed the mechanism in source — the effect keys on `[htmlUrl]` and the parent's copy is only reassigned when the new fetch resolves, while `disabled={!jobReportHtml}` stays false throughout; `jobPdfUrlOverride` is exactly how `ReportVersionHistory` previews an old version, so the trigger is real. Cannot corrupt data (read-and-print path only), but wrong-version export is precisely the blast radius an escape hatch for emailing customers sits in. Carried to Michael untriaged. Gates before the review: tsc 122 vs a 122 baseline measured in this tree the same day, **0 new error lines after normalising a +12/+50 line-number shift** — the raw sorted diff falsely showed 7 'new' lines and the count alone would have hidden it; vitest 71 files / 1222 tests pass. One self-caught bug fixed pre-commit and recorded so it is not re-derived: `window.open(url, '_blank', 'noopener')` returns `null` by spec, so the blocked-popup branch would have fired on every success and revoked the object URL out from under the new tab. **375px browser verification NOT done** — Bash permission is denied on `.env*`, so which Supabase project `npm run dev` targets could not be established, and the standing order says local may be PROD; not run rather than run against an unknown backend. Needs a Vercel preview or Michael confirming the local target. |
| 2026-09-07 | mrc-app-1 / chore/workflow-scaffolding, `cfb2fa0` vs **`0e23baa`** | 218 added, 14 removed, 3 files — **232 reviewable** excl. `docs/sessions/` | adversarial, background with `--wait`, **`--base 0e23baa` (pre-declared stacked parent)**, focus: can any interleaving still export HTML that is not what the iframe shows — same-URL refetch, rapid switching, StrictMode double-invoke, cleanup-vs-start ordering, resolve-after-unmount, handler-vs-disabled disagreement; plus, is the regression test capable of failing | gpt-6-astra / high, config intent only (#705) | 0 | 0 | 0 | 0 | — | ~5 min | not captured, no non-interactive path found | Target: `branch diff against 0e23baa` — printed and checked before the body was read. Matches the pre-declared stacked parent per `docs/CODEX_WORKFLOW.md` §4.3. Verdict: **approve**, no material findings. Codex thread `01a07bd5-9eed-7f93-ab88-26433a9a4a73`. **OVER THE CAP WITH A WAIVER, granted by Michael BEFORE the run** — 232 vs 150. CC measured, refused to subtract the 130-line new test file or the 15 lines of ledger prose to get under (the rule forbids it and the waiver is Michael's), presented the split options, and Michael waived. Production-code delta alone was 87. Recorded so the breach is visible rather than absorbed. Unit A2 fixes the single medium finding from the `61c3940` review: lifted report HTML was untagged and survived a version switch, so View stayed enabled over the PREVIOUS version and could have emailed a customer the wrong report. Now tagged with job id + source URL, readiness is an identity match, revoked on load-start and unmount, re-checked in the handler. Codex verified in-memory against StrictMode double-invocation, rapid version switching, same-URL reloads and resolve-after-unmount — all clean. **It could NOT run vitest (sandbox write restrictions), so this is an approval without test execution**; CC ran the suite separately: 72 files / 1230 tests, and proved the new test fails against the defect by reverting only the two behavioural clears (2 failed / 6 passed), then restoring to a `cmp`-identical file. One honest coverage boundary Codex raised and CC is carrying forward rather than letting the approve verdict bury: the tests assert the **callback contract**, not that the exported Blob equals the iframe's `srcDoc`. They cannot diverge in current code — both read `loadedJobReport.html` — but nothing would catch a future divergence. Closing it needs a component test over `ViewReportPDF` itself, a larger unit than A2. Carried to Michael, not acted on. Also in this unit: `docs/BUG_LEDGER.md` gains class **C11** (an attribute set at write time overridden at read time) and **BUG-23** (Storage serves public-bucket HTML as `text/plain` + `nosniff` despite `contentType: 'text/html'`), which is why Unit A opens a self-typed Blob rather than the Storage URL. Still open on the inspection side. |
| 2026-09-06 | mrc-app-1 / chore/session-logs, `e7b35ed` vs origin/main `dc55c15` (PR #139) | 110 added, 0 removed, 3 files (mechanical count excl. docs/sessions/; the 70-line template excluded) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: session-log hook attack surface, round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 2 | — | 114s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a075d2-ccbe-74d3-a0c8-d8d0ea20a21e`. Findings: (high) two sessions starting on the same branch and day can select the same filename and the later `>` truncates the first log (`session-start.sh:135-136`); (medium) the prior-log glob `*-<slug>.md` also matches a branch whose slug is a suffix of another's, e.g. slug `logs` matches `chore-session-logs` (`session-start.sh:82-84`). Self-collect mode (3 files). Both carry file:line and a reproduction; **untriaged, to Michael** under ruling 1. Hook proven by seven synthetic SessionStart payloads before the review. |
| 2026-09-06 | mrc-app-1 / docs/codex-workflow, `c8a4a69` vs origin/main `dc55c15` (PR #140) | 101 added, 1 removed, 2 files | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: contradictions with the guards, stale-main base, skipped review, unsupported plugin claims, round 1 of 1 | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 2 | — | 119s | not captured | Target: `branch diff against origin/main` — matched. Verdict: needs-attention. Codex thread `01a075d6-eb7e-77b2-ad11-c4adc98701ed`. Findings: (high) §6 describes a Codex guard and AGENTS.md sections that are not on HEAD (`CODEX_WORKFLOW.md:75-79`) — true by series order, they are #143 and #141; (medium) the count command's `-- .` pathspec is cwd-relative and undercounts from a subdirectory (`CODEX_WORKFLOW.md:55-58`). Inline-diff mode (2 files). Untriaged, to Michael. |
| 2026-09-06 | mrc-app-1 / docs/agents-md, `4444a0d` vs origin/main `dc55c15` (PR #141) | 96 added, 13 removed, 2 files | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: weakened rules, non-verbatim quotes, edit/push permissions, import side effects, override precedence, round 1 of 1 | gpt-6-astra / high, config intent only (#705) | 3 | 0 | 0 | 3 | — | 122s | not captured | Target: `branch diff against origin/main` — matched. Verdict: needs-attention. Codex thread `01a075db-dfdd-73e3-801b-a24581122bfd`. All three findings are series order: (high) `AGENTS.override.md` still tracked, so Codex loads it instead of `AGENTS.md` (`AGENTS.md:3`; deleted in #142); (high) `.codex/hooks/codex-guard.sh` referenced but absent (`AGENTS.md:25`; #143); (medium) Lane C scope points at `docs/CODEX_WORKFLOW.md` §6, absent (`AGENTS.md:63-65`; #140). Inline-diff mode. Untriaged, to Michael. Live evidence the same day: with the override present `codex debug prompt-input` carries only the override; without it, AGENTS.md. |
| 2026-09-06 | mrc-app-1 / docs/claude-md-cc-only, `c4e8155` vs **docs/agents-md** `4444a0d` (PR #142, stacked) | 11 added, 84 removed, 2 files | adversarial, foreground, companion script direct, `--wait --cwd <root> --base docs/agents-md` (parent pre-declared), focus: rules lost with the removed text, brief carried over, specifics claims, whether AGENTS.md loads, round 1 of 1 | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 2 | — | 98s | not captured | Target: `branch diff against docs/agents-md` — matched the pre-declared parent. Verdict: needs-attention. Codex thread `01a075e1-0c3e-7363-b744-de1f54917633`. Codex stated it received `AGENTS.md` for this review (override gone). Findings, both series order: (high) the permission-layer bullet describes deny rules that were only in the uncommitted `.claude/settings.json` (`CLAUDE.md:145`; #144); (medium) the session-log bullet describes hook behaviour that is #139 (`CLAUDE.md:147`). Untriaged, to Michael. |
| 2026-09-06 | mrc-app-1 / chore/codex-guard-hook, `e27bad7` vs origin/main `dc55c15` (PR #143) | 67 added, 0 removed, 2 files | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: bypass by path/wrapper/quoting/subshell/newline, fail-open, launcher form, regex false positives and misses, trust inertness, round 1 of 1 | gpt-6-astra / high, config intent only (#705) | 3 | 0 | 0 | 3 | — | 163s | not captured | Target: `branch diff against origin/main` — matched. Verdict: needs-attention. Codex thread `01a075e5-3fea-75d2-bd46-1821ee16f8cf`. **First substantive findings of the series**, all reproduced by Codex with inert payloads: (high) `/usr/bin/git`, `env git`, quoted `'git'`, `-C/tmp` without a space, a subshell, `;` chaining and a newline before the command all pass the two git regexes (`codex-guard.sh:12-13`; line 39 collapses newlines — the P0-9 class, which the replay inherits from the CC guard); (medium) false denies on a commit message containing ` -a ` and on a later command's `-u` (`codex-guard.sh:12-13`); (medium) when `git rev-parse --show-toplevel` fails the `sh -c` launcher expands to `/.codex/hooks/codex-guard.sh` and exits 126 with no deny JSON — fail-open on launcher failure (`hooks.json:9`). Before the review: 26 synthetic payloads 26/26 as expected and one live `codex exec` deny — neither covered the reproduced bypass forms. Untriaged, to Michael. |
| 2026-09-06 | mrc-app-1 / chore/repo-deny-list, `0f49099` vs origin/main `dc55c15` (PR #144) | 26 added, 12 removed, 1 file | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: glob evasion, over-blocking, allow-list widening, effect on hooks and sub-agents, round 1 of 1 | gpt-6-astra / high, config intent only (#705) | 4 | 0 | 0 | 4 | — | 112s | not captured | Target: `branch diff against origin/main` — matched. Verdict: needs-attention. Codex thread `01a075e9-4075-7600-9e86-77269cd436e6`. Findings, inert payloads only: (high) `env supabase --workdir . db reset --db-url …` matches no glob and both CC hooks allow it (`settings.json:10-12`); (high) `git -C . push origin HEAD:refs/heads/production --force` matches no deny and both hooks allow it (`settings.json:22-25`); (high) `gh --repo owner/repo pr merge …` skips the ask entry (`settings.json:44`); (medium) the unconditional linked-target deny blocks `migration list --linked`, which the Supabase guard's rule 3.5 deliberately allows against a verified DEV link (`settings.json:14`). Untriaged, to Michael. |
| 2026-09-06 | mrc-app-1 / chore/session-resume, `ac22a32` vs origin/main `e076aa6` (PR #149) | 108 added, 0 removed, 2 files (mechanical count excl. docs/sessions/) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: the Stop hook's write/delete/race/loop/leak surface and its settings.json registration, round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 4 | 0 | 0 | 4 | — | 164s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a07644-3943-7212-8883-b7653edba1a7`. Findings: (high) whole-file replacement can discard an edit another writer makes between the read and the `mv` — atomic rename prevents partial reads, not lost updates (`session-resume.sh:70-75`); (high) both awk passes treat a fenced `## Resume from here` inside the step log as the section boundary, so quoted log content can redirect the replacement and delete what follows it (`:72-74`); (high) session matching is a substring grep over the whole file, so a step line quoting another session's `- Session id:` can select the wrong log and overwrite its Resume section (`:27-32`); (medium) the missing-log path prints the session id in the systemMessage and in a marker filename under `$TMPDIR` (`:79-82`). Self-collect mode (2 files). All four carry file:line and a reproduction; **untriaged, to Michael** under ruling 1. Before the review: a 17-check smoke test, three attacker agents on scratch copies (8 findings, all fixed before install), and C1 (three nested turns in a scratch clone, section rewritten after turn 1 and again by turn 3, one heading throughout). |
| 2026-09-06 | mrc-app-1 (scratch worktree) / chore/session-window-docs, `b21b7d7` vs origin/main `e076aa6` (PR #150) | 85 added, 1 removed, 5 files (mechanical count excl. docs/sessions/) | adversarial, foreground, companion script direct, `--wait --cwd <worktree> --base origin/main`, focus: token handling in window-remaining.sh, correctness of the printed window line, docs contradicting hooks or rulings, the handover line's scope, round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 4 | 0 | 0 | 4 | — | 216s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a07711-3ea0-7102-b031-96e058b81874`. Findings: (high) curl loads `.curlrc` even with `-K -`, so a configured trace or libcurl output file could persist the Authorization header — `-q` as the first argument (`window-remaining.sh:26-27`); (high) with `TMPDIR` unset the cache is a predictable path in shared `/tmp`, `cat` follows a planted symlink and SessionStart would print its target into context (`:39-40`); (medium) `docs/CODEX_WORKFLOW.md:110` forbids hand-editing "Resume from here" while this checkout has no `session-resume.sh` and no Stop registration — that hook is PR #149, the other half of the split; (medium) no session log in the reviewed checkout names `window-remaining.sh` — the log is committed after the review by design. Self-collect mode (5 files). All four carry file:line and a reproduction; Codex stated it accessed no live credential. **Untriaged, to Michael** under ruling 1. |
| 2026-09-07 | mrc-app-1 / chore/workflow-scaffolding, `53e7bc6` vs **`84015f3`** | 40 added, 15 removed in the endpoint + a 203-line new test + 4 doc lines — **262 reviewable** excl. `docs/sessions/` | adversarial, background with `--wait`, **`--base 84015f3` (the reviewed Unit B commit)**, focus: can anything still escape a mark including via call-site ARGUMENTS, did the guard change any path, is the regression test capable of failing or does it pass vacuously through over-mocking, is the corrected evaluation-order comment accurate, is the 122→124 tsc claim right | gpt-6-astra / high, config intent only (#705) | 1 | 0 | 0 | 0 | — | ~4 min | not captured | Target: `branch diff against 84015f3` — printed and checked before the body was read. Verdict: **needs-attention**. Codex thread `01a07bfb-88a6-7e93-b676-e6ba3addd8e7`. **OVER THE CAP WITH A WAIVER, granted by Michael BEFORE the run** — 262 vs 150, same call as the `cfb2fa0` row today. Production-code delta alone was **55**; the overage is entirely the mock harness the regression test needs. CC measured mechanically and **refused to subtract the test file** to get under. Unit B2 applies the medium finding from the `84015f3` row: the phase timer's `console.log` is now guarded, the construction-time clock read is guarded separately, and a `new URL(env.url)` was removed from a mark argument because it was evaluated at the call site outside the guard — the same defect class one level up. **One medium finding remains, NOT applied** (review-then-stop): `describeError(err)` is *also* evaluated at the call site, outside the guard, at `:262` and `:446`. An error object with a throwing `name` getter or throwing `constructor` access replaces the intended JSON 500/502 with a rejected handler promise. Codex reproduced it with the existing fixtures and states plainly that it assumes a dependency rejects with a throwing accessor, which the seven regression tests do not exercise. Same class as the two CC closed — CC fixed the `new URL` instance and missed these two. Carried untriaged. **Codex confirmed, by independent in-memory fault injection:** both original log-sink failures fixed; clock and serialisation failures contained; the tests exercise the real handler and removing the guard restores both defects; the corrected evaluation-order comment is accurate; the 122→124 tsconfig-graph explanation checks out. **Codex could NOT run vitest (sandbox-blocked), so this carries no test execution.** CC ran it separately: **73 files / 1237 tests** pass, and proved the new test fails against the defect by stripping only the `try/catch` while leaving every breadcrumb in place (6 of 7 failed), then restoring to a `cmp`-identical file. **tsc is 124 against a 122 baseline and this is not a regression:** both delta lines are the pre-existing `chromium.headless` (TS2339) and `waitUntil: 'networkidle0'` (TS2322) errors *in the endpoint itself*, made visible for the first time because the new test imports that file into the app tsconfig graph — `api/**` is in no tsconfig (T13). Neither line is touched by any diff on this branch and both appear verbatim in the untouched sibling `api/render-pdf.ts`. Isolated by running the gate with and without the test file (124 vs 122) and diffing the normalised error sets. **The baseline for this tree is now 124.** The test lives under `src/lib/__tests__/` rather than `api/__tests__/` because Vercel turns files under `api/` into deployed functions; the underscore exclusion `api/_shared/` relies on is not verifiable from a local build and this landed immediately before a production deploy. Recorded as a deliberate trade-off, not the end state. |
| 2026-09-07 | mrc-app-1 / chore/workflow-scaffolding, `84015f3` vs **`fb6107f`** | 81 added, 4 removed, 1 production file + TODO row — **86 reviewable** excl. `docs/sessions/` | adversarial, background with `--wait`, **`--base fb6107f` (pre-declared stacked parent)**, focus: no behaviour change on any path, evaluation-order equivalence of the `executablePath()` hoist, no PII or secrets in the new logs, timer cannot alter control flow | gpt-6-astra / high, config intent only (#705) | 1 | 0 | 0 | 0 | — | ~3 min | not captured | Target: `branch diff against fb6107f` — printed and checked before the body was read. Matches the pre-declared stacked parent, same provision as the `0e23baa` row above; `origin/main...HEAD` would have been **383** because the branch already carries Unit A's 297 already-reviewed lines, so reviewing against origin/main would have re-reviewed reviewed work AND breached the cap. Unit B alone is 86. Verdict: **needs-attention**. Codex thread `01a07be7-c367-7a42-a3d3-2ea2507845f9`. Unit B is observability only — a phase timer emitting one line per boundary at the boundary (not buffered to the end, because Vercel SIGKILLs at maxDuration and the running phase is the one needed), `VERCEL_REGION` once per invocation, and error-kind capture on the caught paths. **One medium finding, NOT applied** (review-then-stop): the timer calls `console.log` unguarded, so a throwing logging sink at `browser_launched` exits before the browser cleanup `try/finally` and leaks the browser; the same after `version_inserted` turns a committed save into a failed request. Codex reproduced it by fault injection against both revisions. Codex itself states the finding assumes a throwing sink and does not establish that the deployed logger throws. **Codex also corrected a CC claim**, and it is right: the `executablePath()` hoist is NOT evaluation-order-identical — `chromium.args` was read *before* `executablePath()` in the original object literal and is read *after* it now. The code comment and the commit message on `84015f3` both assert equivalence and are therefore wrong as written. Codex found no runtime regression from the reorder (it checked `@sparticuz/chromium` for `graphicsMode`/`setGraphicsMode` coupling and found none used in this repo), but the claim itself is inaccurate and is carried to Michael rather than quietly corrected. No new customer-content or secret logging identified — the diff logs byte counts and error kinds only. Gates before the review: app tsc **122 error lines vs the 122 baseline** measured in this tree the same day; `api/` is in **no tsconfig** (T13) so that gate is blind to the edited file, and a standalone check on it reports the same two pre-existing errors (`chromium.headless`, `waitUntil: 'networkidle0'`) as the **untouched** sibling `api/render-pdf.ts` and nothing new; vitest **72 files / 1230 tests** pass. Carried to Michael untriaged under ruling 1. |
| 2026-09-07 | mrc-app-1 / chore/session-resume, `3cd9981` vs origin/main `e076aa6` (PR #149, re-review after triage fixes 1–3) | 117 added, 0 removed, 2 files (mechanical count excl. docs/sessions/) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: are the three fixes (fence-aware awk passes, exact Header-only session-id match with id validation, hashed marker and 8-character id in the notice) correct and complete, anything new; concurrent-edit finding declared out of scope; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 2 | — | 107s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a07939-e1ac-7ab3-b51e-081f8d2da465`. Findings: (high) every parser toggles fence state on `^```` only, ignoring fence length, tilde fences and indentation — a four-backtick block containing triple-backtick lines around a quoted `## Resume from here` passes the balance guard and the replacement still deletes a following bullet and closing fence; the same construction around `## Header` plus another session's exact id makes `header_field` select the wrong log (`session-resume.sh:81-83`); (medium) the missing-log notice interpolates the first eight characters of the session id, and an accepted id of eight characters or fewer is disclosed in full (`:91`). No substantive finding in the Stop registration. Self-collect mode (2 files). Both carry file:line and a reproduction; **untriaged, to Michael** under ruling 1. Original findings 2, 3 and 4 of the 2026-09-06 row closed by the fixes as triaged; finding 1 (concurrent edits) carried, design pending. |
| 2026-09-07 | mrc-app-1 (scratch worktree) / chore/session-window-docs, `99036bb` vs origin/main `e076aa6` (PR #150, re-review after triage fixes 5–6) | 97 added, 1 removed, 7 files (mechanical count excl. docs/sessions/; includes the two 2026-09-06 review rows) | adversarial, foreground, companion script direct, `--wait --cwd <worktree> --base origin/main`, focus: are the two fixes (`curl -q` first; cache in a private 0700 directory, symlinks rejected, mktemp + rename) correct and complete, anything new; the two split-artefact findings declared out of scope; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 1 | 0 | 0 | 1 | — | 88s (second attempt) | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a0793e-297d-7622-b71d-4f2fc02b1646`. Finding: (medium) `write_cache` runs `mkdir -p` and `chmod 700` before the symlink check, so if `claude-hooks` is a symlink to an existing user-owned directory the chmod follows it and sets that unrelated directory to 0700 before the write is rejected (`window-remaining.sh:27-28`). Codex states the curl fix is correct. Self-collect mode (7 files). Carries file:line and a reproduction; **untriaged, to Michael** under ruling 1. Original findings 1 and 2 of the 2026-09-06 row closed by the fixes; findings 3 and 4 (no Stop hook on this branch; session log absent from the reviewed checkout) recorded as **artefacts of the two-PR split**, not fixed. **First attempt failed locally, not at Codex**: job `review-mtqhv82a-shqp5z` from the recreated worktree path `wt2` exited in 1 s with `failed to load configuration: No such file or directory` and no thread; `codex debug prompt-input` ran clean from the same directory and `~/.codex/config.toml` was intact, so the cause was the companion's shared session runtime holding a cwd that had been deleted and recreated. Re-run once from the worktree moved to `wt3`. |
| 2026-09-07 | mrc-app-1 / chore/session-resume, `d1b601d` vs origin/main `e076aa6` (PR #149, re-review after round-2 fixes: CommonMark fences, optimistic concurrency) | 144 added, 0 removed, 2 files (mechanical count excl. docs/sessions/) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: are the two changes correct and complete, anything new; the 8-character id prefix declared kept by ruling; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 2 | — | 145s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a0794f-1d37-72c0-a750-158aa8165d5f`. Findings: (high) the closing-fence test accepts only spaces after the fence while CommonMark permits tabs, so a closer followed by a literal tab is not recognised; `fences_balanced` still passes because a later closer masks it, and the suffix pass then drops the section after the Resume section (`session-resume.sh:30`); (medium) the digest is taken after `fences_balanced`, so a writer inserting an unclosed fence between the validation and the snapshot becomes the accepted baseline — separate from the documented re-read-to-rename window (`:120-121`). No substantive issue in the Stop registration. Self-collect mode (2 files). Both carry file:line and a reproduction; **untriaged, to Michael** under ruling 1. Closed by this round: the 2026-09-06 concurrent-edit finding (approach A, shim-mktemp fixture catches an edit between snapshot and re-read, shim-mv fixture shows the documented residual) and the fence-variants finding of the first re-review as triaged. **The 8-character id prefix finding is closed by ruling, not fixed** (Michael, 2026-09-07): session ids are UUIDs; an id of eight characters or fewer does not occur. |
| 2026-09-07 | mrc-app-1 (scratch worktree) / chore/session-window-docs, `dc8cfae` vs origin/main `e076aa6` (PR #150, re-review after round-2 fix: symlink check before mkdir/chmod; §8 worktree trap) | 101 added, 1 removed, 7 files (mechanical count excl. docs/sessions/; includes the review rows) | adversarial, foreground, companion script direct, `--wait --cwd <worktree> --base origin/main`, focus: is the chmod-ordering fix correct and complete, anything new, the §8 bullet; the two split-artefact findings declared out of scope; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 0 | 0 | 0 | 0 | — | 65s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: **approve**, no material findings. Codex thread `01a07951-cb8c-7d00-835b-73ea4f35cf99`. Codex: the symlink guard and ownership check fix the reported chmod failure; inert guard tests and shell syntax checks passed; no material regression in the workflow bullet; live credential and network behaviour not exercised. Self-collect mode (7 files). Closes the 2026-09-07 chmod-ordering finding. The two 2026-09-06 medium findings on this branch remain recorded as **artefacts of the two-PR split**. Run from a fresh worktree path (`wt4`), per the new §8 trap. |
| 2026-09-07 | mrc-app-1 / chore/session-resume, `bce5593` vs origin/main `e076aa6` (PR #149, re-review after round-3 fixes: tabs after a closing fence, digest snapshot before fence validation) | 149 added, 0 removed, 2 files (mechanical count excl. docs/sessions/) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: are the two changes correct and complete, anything new; the 8-character id prefix declared kept by ruling; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 1 | 0 | 0 | 1 | — | 156s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a079bd-cbe7-7ea2-a791-49e3c6abde31`. Codex: both requested fixes are correct and the committed Stop registration has no substantive defect. Finding: (high) the backtick opener accepts any line beginning with three or more backticks, including an inline span such as three backticks + `inline` + three backticks, which CommonMark does not treat as a fence opener because its info string contains a backtick; with the review's fixture (`## Resume from here`, that inline line, `## Open`, `- KEEP`, then a four-backtick block containing a triple-backtick line) `fences_balanced` passes and the replacement deletes the whole Open section with no concurrent writer involved (`session-resume.sh:26-29`). Recommendation: reject a backtick opener whose remainder contains a backtick. Self-collect mode (2 files). Carries file:line and a reproduction; **untriaged, to Michael** under ruling 1. Closes the two findings of the previous row (tabs after a closing fence; digest ordering). Branch now at 149 reviewable lines. |
| 2026-09-07 | mrc-app-1 / chore/session-resume, `230fad7` vs origin/main `e076aa6` (PR #149, final review after the marker redesign; last round by decision) | 146 added, 0 removed, 2 files (mechanical count excl. docs/sessions/) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: can the marker path write outside the markers, can the one-time migration lose data, does the concurrency snapshot still hold, anything new; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 2 | — | 146s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a079cb-db9d-7c20-a444-97d2b673631f`. Codex: marker-count checks and the Stop registration passed; both findings are on the migration path. Findings: (high) the inline-span parser gap is destructive on the migration path — with an inline backtick span and a bare triple-backtick line before `## Resume from here`, then `KEEP`, another span, a closer and `## Step log`, the heading is actually inside a code block, both awk checks pass, the markers enclose `KEEP` and the closing fence, and the next replacement deletes them (`session-resume.sh:84-88`); (high) the migration boundary and the inside-bounds refusal recognise only column-zero `## ` lines, so a one-space-indented ` ## Open` heading is not seen, the end marker lands at EOF, and the replacement deletes the Open section (`:86-88`). Recommendations: reject backtick openers with a backtick in the info string or refuse migration on such lines; recognise level-two headings with up to three leading spaces and refuse migration when one sits inside a fence. Self-collect mode (2 files). Both carry file:line and a reproduction; **untriaged, to Michael** under ruling 1; **no further rounds on #149** (the two-round cap was exceeded and the branch is closed after this review). Closed by this round: the round-3 inline-span finding on the destructive path (markers replace parsing there). Both open findings are confined to logs that still carry the heading without markers; logs created from the template on #150 never enter migration. |
| 2026-09-07 | mrc-app-1 / chore/session-resume, `28b7a3a` vs origin/main `e076aa6` (PR #149, review after dropping automatic migration and the fence parser) | 113 added, 0 removed, 2 files (mechanical count excl. docs/sessions/; the legacy log's two marker lines are excluded) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: can the marker path write outside the markers or to the wrong log, does the concurrency snapshot still hold, anything new; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 1 | 0 | 0 | 1 | — | 164s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a079d6-c9de-74a0-ad9e-88d14ece6357`. Codex: marker validation passed eight checks; snapshot ordering and the Stop registration show no new blocker. Finding: (medium) with the parser gone, `header_field` accepts any exact `- Session id:` line inside the first Header section, including one quoted in a fenced example placed before the next `## `; two same-branch logs, A alphabetically first with B's id quoted that way and both carrying valid markers: a Stop for B selects A and rewrites A's resume content while B stays stale, and the digest cannot catch it because A did not change concurrently; 230fad7's fence-aware selection rejected this fixture (`session-resume.sh:34-41`). Recommendation: keep parsing plain but require exactly one Session id field in the first Header, compare after scanning the section, refuse ambiguous headers; add the two-log fixture. Self-collect mode (2 files). Carries file:line and a reproduction; **untriaged, to Michael** under ruling 1. Closed by this round: both round-4 migration-path findings (no migration path exists any more). Hook 135 → 102 lines. |
| 2026-09-07 | mrc-app-1 / chore/session-resume, `3060718` vs origin/main `e076aa6` (PR #149, closing review after the ambiguous-Header refusal; #149 closed to further rounds by decision) | 121 added, 0 removed, 2 files (mechanical count excl. docs/sessions/) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: can the marker path write outside the markers or to the wrong log, does the concurrency snapshot still hold, anything new; round 1 of 1 (review-then-stop) | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 2 | — | 160s | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a079fd-7f4a-7291-94e3-70ec60c2d7d4`. Codex: marker validation and the committed Stop registration passed the checks performed. Findings: (medium) ownership is validated before the digest is captured, so a writer that changes the selected log's Session id or adds a duplicate between selection and the snapshot becomes the accepted baseline and the log can still be rewritten — separate from the documented digest-to-rename race; recommendation: recheck the first Header (exactly one matching Session id) immediately after taking the digest and keep the final comparison (`session-resume.sh:89-95`); (medium) `header_field` answers "ambiguous" for any Header with more than one Session id line regardless of the requested id, so with two ambiguous logs A/A and B/B a Stop for B names A's log in the notice and the one-time marker then hides B's own unmaintained log; recommendation: notice only a log that both has multiple fields and matches this session's id, keep every ambiguous log ineligible, add the A/A versus B/B fixture (`:48-49`). Self-collect mode (2 files). Both carry file:line and a reproduction; **untriaged, to Michael** under ruling 1; **no further rounds on #149**. Closed by this round: the round-5 wrong-log selection finding (the reviewer's two-log fixture is now in the suite and passes). Hook 110 lines. |
| 2026-09-07 | mrc-app-1 / chore/session-resume, `3060718` vs origin/main `e076aa6` (PR #149, round 6, closing review) | 121 added, 0 removed, 2 files (settings.json unchanged since 5ccf2ee) | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: the ambiguity fix (`header_field` requires exactly one `- Session id:` line in the first Header and compares after scanning the section) plus the reviewer's two-log regression, round 1 of 1 — #149 closed by ruling after this run | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 0 | — | 03:50–03:53 UTC | not captured | Target: `branch diff against origin/main` — printed and matched before reading. Verdict: needs-attention. Codex thread `01a079fd-7f4a-7291-94e3-70ec60c2d7d4`. Codex: "Marker validation and the committed Stop registration passed the checks performed." The round-5 finding (wrong-log selection through a fenced example in the Header) is closed: the two-log fixture is S24/S25 in the suite and passes (B written, A byte-identical, a Stop for A refused with the notice). Findings, verbatim: (1) [medium] Revalidate ownership after taking the digest snapshot (.claude/hooks/session-resume.sh:89-95) — Selection validates the Header before the digest is captured. A concurrent writer can change the Session id or add a duplicate in that interval; the changed log becomes the accepted baseline and can still be rewritten. Reproducing test: select a valid A-owned log, change its Header to B before line 95, then continue. The marker and final digest checks pass despite ownership now failing. This is separate from the documented digest-to-rename race. Recommendation: Immediately after capturing the digest, recheck that the selected log’s first Header contains exactly one Session id matching this session; retain the final digest comparison. (2) [medium] Restrict ambiguity notices to logs containing this session id (.claude/hooks/session-resume.sh:48-49) — header_field returns 2 for every duplicate-field Header regardless of the requested id, so ambiguous_log returns the first ambiguous log belonging to anyone. Reproduced with alphabetically ordered logs containing A/A and B/B: Stop for B selects neither and names A’s log in its notice. The one-time marker then suppresses further guidance, leaving B’s actual log unmaintained without identifying it. Recommendation: For notice selection, require both multiple Session id fields and at least one exact match for the current session. Keep all ambiguous logs ineligible for writing and add the A/A versus B/B regression test. **Both carried, not fixed** (Michael, ruling 2026-09-07): each needs a concurrent writer racing the selection-to-digest window, and the worst case is a stale Resume section, not lost work. **#149 is closed by ruling; no further rounds.** Review run by session 57592e44 (its output captured in that session's scratchpad); row written by session e83b760f from the captured output. |
| 2026-09-07 | mrc-app-1 / chore/workflow-scaffolding, `e495abd` vs **`1046c60`** | 287 reviewable excl. `docs/sessions/`, 4 files | adversarial, background `--wait`, **`--base 1046c60` (pre-declared stacked parent)**, focus: is any instrumentation call still reachable outside a guard incl. timer construction, final emit, catch/finally; can the guard itself throw; does swallowing hide a real failure or change the client's status; does the timing work eat the 60s budget; does the new test exercise the throwing sink or assert the implementation back to itself | gpt-6-astra / high, config intent only (#705) | 2 | 0 | 0 | 0 | — | ~6 min | not captured, no non-interactive path found | Target: `branch diff against 1046c60` — printed and checked before the body. Verdict: **needs-attention**, 2 medium. Codex thread `01a07c02-ec90-7aa1-a13d-9b9d448621de`. **Nothing applied (review-then-stop).** **OVER THE CAP AND NOT WAIVED** — 287 vs 150, flagged in the announcement before the run rather than discovered after. Unlike the A2 run, no waiver was requested or granted for this one; it is on the record as a breach awaiting Michael's disposition. Scope covers `53e7bc6` (exception isolation, Session 2) and `e495abd` (C4 read-side ledger entry, Session 1). F1 (medium): `describeError(err)` is evaluated at the call site **before** control enters `mark`'s try, so the very class of bug `53e7bc6` set out to kill still has live instances at `api/render-job-report-pdf.ts:262-263` (502 path), `:446` (render 500) and `:337` (signing). The `:337` case rejects **after the version row commits** — the same already-committed-save-reported-as-failure shape the P0 client cannot recover from (`jobReportPipeline.ts:96-109`). Codex reproduced both by fault injection with an error whose `name` getter throws. Residual, not newly introduced. F2 (medium): the test's `await import('../../../api/render-job-report-pdf')` at `:107-108` pulls the endpoint into `tsconfig.app.json`'s program, surfacing TS2339 `chromium.headless` (`:196`) and TS2322 `networkidle0` (`:202`) — **tsc 124 vs the 122 baseline, so the branch fails its own gate.** Both pre-existing; identical constructs at `api/render-pdf.ts:128`/`:133` stay invisible because nothing imports that file. **Corroboration worth recording:** CC measured F2 independently BEFORE the review ran (normalised line-number diff against a same-tree baseline) and had already written it into `.ai/FINAL_REVIEW.md`; Codex reached it by compiling with and without the test. Two methods, same two errors. **Disagreement surfaced rather than resolved:** Codex treats F2 purely as a violation to remove and suggests moving server tests to a separate TS project — which would restore the T13 blind spot to protect the number. CC's stated view is that this test accidentally bought `api/**` its first-ever type coverage, and that `waitUntil: 'networkidle0'` is one of the two live rival causes (R1) for the P0, so editing it mid-diagnosis would corrupt Michael's evidence. Michael decides. Codex again could not run vitest (sandbox); CC ran it: 73 files / 1237 pass. |

`model / effort` is what `~/.codex/config.toml` was set to, not what ran — the plugin gives no way to verify that from the thread (issue #705).
| 2026-09-08 | mrc-app-1 / chore/workflow-scaffolding, `fb013db` + `86dbcbb` vs `origin/main` `37171b7` | 175 reviewable (excl. `docs/sessions/`): api 2, TODO 10, review-log 1, new test 162 — **over the 150 cap, waived by Michael before the run** on the grounds that the production change is 2 lines and ~110 of the test file is scaffolding transcribed from the already-reviewed `renderJobReportPdf.instrumentation.test.ts`; splitting by commit cannot help since the review reads the whole branch diff, and extracting a shared harness would raise the count because numstat counts deletions too | adversarial, foreground, `--wait --cwd /Users/michaelyoussef/mrc-app-1 --base origin/main`, focus on the `waitUntil` change and whether it can render before fonts or images are ready | gpt-6-astra / high, config intent only (#705) | 1 (medium) | 0 | 0 | 0 | — | ~3m | not captured | Target: `branch diff against origin/main`. Verdict: needs-attention. Codex thread `01a07c3a-bf63-76c3-9ff0-9346ae8d4bde`. **`git fetch origin` was run immediately before the review and mattered: it moved `origin/main` `68c8f73..37171b7`, and against the stale ref the branch measured 663 reviewable lines of already-merged work (PR #155). Reviewing without the fetch would have been the #653 class.** The single medium — `document.fonts.ready` at `:204` left unbounded once `setContent`'s 45 s bound is narrowed — is **deferred with reason** by Michael: `networkidle0` is deterministically broken and confirmed by production logs, Codex's scenario is a source-based prediction it could not verify (browser and Vitest execution sandbox-blocked) and needs the stylesheet to actually stall, so always-broken → conditionally-broken is a win. Recorded as **BUG-25** under new class **C13**, closed properly by **T19**; disposition in `.ai/REVIEW_RESOLUTIONS.md` round 5. The finding also **corrected CC's own stated risk** — CC predicted an early resolve and a silent `sans-serif` fallback, Codex predicts a hang; neither verified, both recorded in **P2-21** as unresolved. Codex's other two next steps were not new work: the 150-line split was the pre-granted waiver, and the P2-21 correction was applied. |
| 2026-09-08 | mrc-app-1 / chore/workflow-scaffolding, `c57507f` vs `origin/main` `4088c7f` | 90 reviewable (excl. `docs/sessions/`): AGENTS.md 46, docs/DEPLOYMENT.md 41+3 — under the 150 cap | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: does any new sentence contradict an existing AGENTS.md section (Reviewer-Codex under Roles, the no-second-copy discipline under Workflow); is the renumbered Deploy Flow list internally consistent; do the anchors resolve; is anything asserted about hooks or branch topology factually wrong | gpt-6-astra / high, config intent only (#705) | 1 | 1 | 0 | 0 | — | ~2m | not captured | Target: `branch diff against origin/main`. Verdict: **needs-attention**. Codex thread `01a07e93-4083-7a42-870f-519f2e0abfb1`. S0 docs-only unit: the "For the reviewer" section in `AGENTS.md` and the new "Production gate" section in `docs/DEPLOYMENT.md`. **One finding, [high], ACCEPTED by Michael and fixed in this commit:** the gate named `origin/main` as the base for the production review, carried over from the per-unit review where it is correct. At the production gate the work is already on `main`, so that range is empty — the gate would have passed on nothing, and its own `Target:` check would have told a reader to discard the only correct review. Reproduced before accepting: `git diff --shortstat origin/main...origin/main` empty, `git diff --shortstat origin/production...origin/main` 2 files / 123 insertions / 3 deletions. Fix states both bases with their conditions (`origin/main` for per-unit, `origin/production` for the gate) and makes an empty range or zero line count an explicit fail. Third real fix against the reviewer's standing record. |
| 2026-09-08 | mrc-app-1 / chore/workflow-scaffolding, `cdcadeb` vs `origin/main` `4088c7f` (round 2 of 2 on the S0 docs unit; `git fetch origin` run immediately before, ref unchanged) | 117 reviewable (excl. `docs/sessions/`): AGENTS.md 48, docs/DEPLOYMENT.md 65+3, codex-review-log 1 — under the 150 cap | adversarial, foreground, companion script direct, `--wait --cwd <root> --base origin/main`, focus: is `origin/production...origin/main` actually right for this topology; does the new Target validation leave any path where an empty or wrong range reads as a pass; is the base table consistent with Deploy Flow and with AGENTS.md; does AGENTS.md now duplicate the base in a way that can drift | gpt-6-astra / high, config intent only (#705) | 1 (medium) | 0 | 0 | 0 | — | ~2m | not captured | Target: `branch diff against origin/main`. Verdict: **needs-attention**. Codex thread `01a07ea7-482d-7081-bd15-9d8175d66825`. Confirmed the round-1 fix: the corrected range matches the topology, the round-1 log row is internally consistent, and AGENTS.md restates the base but does not contradict `docs/DEPLOYMENT.md`. **One new finding, [medium], ACCEPTED-DEFERRED by Michael:** the gate validates symbolic ref names and a nonzero line count but never that the reviewed commits are the ones being released, so a stale checkout passes. Reproduced before accepting: `origin/production...2f351f9` is 118/3 and `origin/production...origin/main` is 123/3 — both print an accepted `Target:` and a nonzero count, but the former omits a commit; `2f351f9` is a real ancestor of `origin/main`. Same class as the near-miss recorded on the `fb013db` row above, which was caught by hand rather than by the gate. **Deferred, reason recorded:** the fix needs a round 3 on this unit and the two-round cap forbids one. What shipped in `cdcadeb` is correct but incomplete — it fixes *which refs*, not *which commits*. Filing beats stacking. Carried as **S0b** in `docs/TODO.md`, which must land before the end-of-sprint terminal CLI pass, since that pass **is** the production gate. |
| 2026-09-08 | mrc-app-1 / chore/workflow-scaffolding, merge commit `a715771` (parents `7bdc1cf` branch, `4088c7f` origin/main); shipped in PR #158, merged as `dab41ba` | not measured — **no review was run on this commit** | **MISS — merged without review.** Round 2 of 2 on this unit covered `cdcadeb`. The `origin/main` merge and the `docs/TODO.md` conflict resolution came after it and were never reviewed | — | — | — | — | — | — | — | — | **Process miss, recorded after the fact. Not unwound.** The last review of this unit (thread `01a07ea7-482d-7081-bd15-9d8175d66825`) read `cdcadeb`. `a715771` then merged `origin/main` and resolved a conflict in `docs/TODO.md` — five rows in the Infrastructure table, resolved to T20(main), T21(main), T22, T23, S0b(branch). **Precise scope, measured 2026-09-08:** `git show --cc a715771` reports the resolution as content differing from both parents, but at line granularity **zero lines are novel** — every line is verbatim from one parent (S0b from `7bdc1cf`, T20 and T21 from `4088c7f`). What existed on neither parent is the resulting **file state**, and that state shipped unreviewed. **Accepted after the fact:** docs-only, no `src/`, `api/`, `supabase/` or config in the merge, and each row was verified byte-identical to its winning side before the resolution was written. **Same failure class as 7 Sep** — a review whose verdict was taken to cover commits it never read. CC flagged the exposure before the merge ("whatever the eventual PR contains is what needs reviewing, not just my 117 lines"); Michael overruled and has recorded that CC was right. Rule written into `docs/DEPLOYMENT.md` → "Production gate" so it is enforced rather than remembered: a conflict resolution is reviewable content in its own right. Related standing debt: **S0b** (`docs/TODO.md`), which binds a review to the commits actually being released. |
| 2026-09-08 | mrc-viewreport / fix/viewreport-history-guards, `5862bc6` vs origin/main `dab41ba` | 239 added, 14 removed, 2 files — **253 reviewable** excl. `docs/sessions/`; split **40 fix / 213 tests** | adversarial, foreground, companion script direct, `--wait --cwd <worktree> --base origin/main`, focus: any remaining path where one completion's report HTML can be exported/printed/emailed under a different completion (refetch, regenerate, pin-then-edit, unmount/remount); any surviving null-`jobCompletion` dereference; and whether each new test would still pass with the source fix reverted, round 1 of 2 | gpt-6-astra / high, config intent only (#705) | 1 | 1 | 0 | 0 | — | not captured | not captured, no non-interactive path found | Target: `branch diff against origin/main` — printed and checked before the body was read. Verdict: **needs-attention**. Codex thread `01a07f94-1f81-77c2-b09b-a697228130ee`. **OVER THE CAP WITH A WAIVER, granted by Michael BEFORE the run** — 253 vs 150. CC measured mechanically, subtracted nothing, and presented waiver/split/drop-the-tests options rather than deciding. Michael's stated reason for the waiver, recorded verbatim in intent: **the tests ARE the evidence for the fixes, and separating them makes both rounds meaningless.** The split is 40 lines of fix against 213 of test; the overage is entirely the harness, which is indivisible because both defects are parent-state interactions that need `ViewReportPDF` mounted, not `JobReportPreview` in isolation. Unit closes the two 2026-09-08 terminal-Codex defects (thread `01a07e93-4083-7a42-870f-519f2e0abfb1`): (1) `jobHtmlUrl` stayed truthy off a pinned history version, so a job query resolving to null still rendered the preview and dereferenced `jobCompletion!.id`, unmounting the report screen; (2) the pin outlived the completion it was taken from, so the preview re-downloaded the old URL and tagged that HTML with the NEW completion id — both identity comparisons then passed and one job's report could be exported under another job's heading. Fixes: the preview is not constructed without a loaded completion, and the pin now carries its owning `jobCompletionId` and is discarded on a genuine mismatch. A *missing* completion is deliberately not treated as a mismatch, so a transient refetch failure no longer discards the admin's selection. **Tests were written and demonstrated failing before either fix**, then re-verified against the identical test file by restoring `HEAD`'s source: 3 failed / 8 passed before, 11 passed after. Codex agrees by static analysis that all three would fail on a revert, and states plainly it **did not run them** — so this is again an assessment without test execution on Codex's side; CC ran them. One HIGH finding, **not applied** (review-then-stop), and Codex marked it **UNVERIFIED by execution** itself: `ViewReportPDF.tsx:1333-1336` — the pending-email mismatch dialog keeps `jobMismatchVersion` from completion A while the completion query moves to B, so choosing `send_as_is` signs A's storage path and names the attachment with B's job number. **Factually outside this diff**: the touched hunks are 235, 399, 579, 878, 2522, 2982, 2986, 3056, 3059 — 1333-1336 is in none of them, and Codex's own text says "This pre-existing email path remains outside the new preview guard." Same defect class as the one just fixed (state pinned to one completion outliving it), one path over. Carried to Michael untriaged. Codex's second next-step is a fair critique CC is NOT acting on under ruling 1 and is recording so the approve-adjacent verdict does not bury it: the export test asserts A's HTML is never exported, which the brief explicitly allowed to be satisfied either by exporting B or by View staying disabled — so it would also pass if no blob were created at all. Under the fix exactly one blob is created and it is B's; the assertion is simply weaker than that fact. **DISPOSITION (Michael, 2026-09-08): ACCEPTED-DEFERRED.** Filed as its own row **`docs/TODO.md` P1-24**, carrying the reproduction, the UNVERIFIED marker and the acceptance criterion (a test mocking the mismatch and send responses that asserts `sendEmail` is NOT called after the identity change). Reason for deferring rather than fixing in this unit, recorded: the path is **outside this diff**, **pre-existing**, and **UNVERIFIED** — Codex's own word, static reproduction only, no execution — so it needs a failing test before anyone touches it. **No round 2**: a second pass over the same code would re-litigate rather than add, since the finding is not in the code under review. 1 acted (accepted + filed), 0 rejected, 0 unclear — counted the way the `mrc-guard-hook` row above counts a finding deferred to a filed item. **Post-review change, declared:** on Michael's instruction the export test was tightened from "A's HTML is never exported" to `expect(exported).toEqual([JOB_B_HTML])` — exactly one blob, containing B's HTML — because the weaker form also passes when no blob is created at all. Re-verified against the unfixed source: still 3 failed / 8 passed, so the stronger assertion still reproduces. Reviewable count moved **253 → 256** (+3); judged by Michael too small to re-review, and recorded here rather than absorbed silently. Self-collect mode (3 files in range with the session log). **Codex did NOT read `AGENTS.md` on this run** — the previous three logged code reviews all `cat`-ed it alongside the diff, and the 2026-09-05 rows call that "confirmed as standard behaviour, not a pattern of two". This run breaks it. **Recorded as a fact, not a finding** — no conclusion drawn about this review's quality; it is here so the trend is visible if reviewer quality drifts. Gates before the review: `npm ci` re-seed (the worktree had no `node_modules` at all), installed TypeScript 5.8.3 == lockfile pin 5.8.3 (T14 pass); tsc 100 vs a 100 baseline measured in this tree the same day, **0 new error lines** after normalising a 23-line shift that made 2 pre-existing errors look new; full suite 1165 → 1168 passed (+3 = the new tests) with the same 5 files unloadable before and after — those 5 fail at import on missing `VITE_SUPABASE_*` because this worktree has no `.env*` and the deny list blocks an agent from copying them, verified pre-existing rather than assumed. |
| 2026-09-08 | mrc-equip-days / fix/equipment-days-form, `d474e67` vs `origin/main` `89af01f` (Unit A of the A/A′/B/C equipment-days stack) | 146 reviewable (excl. `docs/sessions/`), 4 files: pricing.test.ts 116, pricing.ts 24+3, AGENTS.md 2, docs/TODO.md 1 — under the 150 cap | adversarial, foreground, companion script direct, `--wait --cwd <worktree> --base origin/main`, focus: can an explicit `equipmentDays` reach labour / the 0.87 manual cap / GST / `EQUIPMENT_RATES`; is absent-or-0 byte-identical on every path incl. manual override and the option-1/option-2 dual write; behaviour on negative, fractional, NaN or unbounded day counts; are the new tests capable of failing, round 1 of 2 | gpt-6-astra / high, config intent only (#705) | 1 (medium) | 0 | 0 | 0 | — | ~2m | not captured | Target: `branch diff against origin/main` — printed and checked before the body was read. Verdict: **needs-attention**. Codex thread `01a07fb8-f6a9-7731-adcf-22e5dedc2fb4`. **BASE CORRECTED BEFORE THE RUN — S0b class, third sighting today.** Michael's brief named `--base 63dca6b`, taken verbatim from CC's own earlier wording "the previous commit". `63dca6b` **predates the merge commit `7da0dd4`**, so `63dca6b...HEAD` spans the merge and pulls in S1's `ViewReportPDF.tsx` and its 211-line test — **424 lines**, over the cap, re-reviewing already-merged work (the row above). All three candidates measured before choosing: `63dca6b` 424, `7da0dd4` 143, `origin/main` 146. `origin/main` used per the `61c3940` precedent — correct base, deviation declared, never silently taken — because it is canonical, under the cap, and includes the 3-line docs commit `63dca6b` that no review had yet covered. **The fault was CC's instruction, not Michael's execution of it.** Stacked parents are correct from A′ onward since those commits descend from the merge. **One medium finding, NOT applied (review-then-stop):** `Infinity` passes the new day guard at `pricing.ts:247-250` — `equipment.equipmentDays && equipment.equipmentDays > 0` accepts it; a nonzero quantity then yields `Infinity` and an omitted quantity yields `0 × Infinity = NaN`, poisoning `equipment.total`, `subtotalExGst` and GST. `Number.MAX_VALUE` overflows similarly. **CC confirmed the arithmetic by inspection** and adds as context, not rebuttal: no committed caller supplies the field yet, and Unit C will feed it only from an integer stepper and an integer DB column, so this is engine hardening rather than a reachable corruption path today. Codex says the same in its own words — "demonstrates an engine defect, not observed persisted corruption" — and states plainly it **did not run the tests**; CC ran them separately (75 files / 1281 tests, 1280 pass, the only failure the known T24 `reportPipeline` one). Its second next step is the sharper one and is carried too: **absent/0 comparisons against the same modified engine cannot independently prove historical parity** — only fixed pre-change output literals can. That is a real limit of both `pricing.test.ts`'s two parity tests and Unit A′'s seven-shape sweep, and it was not visible to CC when A′ was designed. Untriaged, to Michael under ruling 1. |
| 2026-09-08 | mrc-equip-days / fix/equipment-days-form, `071fdf3` vs **`d474e67`** (round 2 of 2 on Unit A; pre-declared stacked parent) | **187 reviewable** (excl. `docs/sessions/`), 4 files: pricing.equipmentDays.test.ts 156, pricing.ts 23+5, docs/TODO.md 1+1, codex-review-log 1 — **over the 150 cap, WAIVED BY MICHAEL BEFORE THE RUN, granted at 187** | adversarial, foreground, companion script direct, `--wait --cwd <worktree> --base d474e67`, focus: is the non-finite guard complete incl. `-0`, denormals, near-sentinel values and the separate HEPA multiplication; is 3650 an overflow sentinel or a business cap in disguise; is silent fallback the right failure mode or does it hide corrupt persisted data; do the PRE_CHANGE literals constitute independent evidence of historical parity; anything new | gpt-6-astra / high, config intent only (#705) | 2 (both medium) | 0 | 0 | 0 | — | ~3m | not captured | Target: `branch diff against d474e67` — printed and checked before the body was read. Verdict: **needs-attention**. Codex thread `01a08014-bae1-71c1-8888-4133965ab34b`. **Waiver reason, Michael's words:** "the overage is entirely content I asked for in triage, and both fixes answer a single finding set, so reviewing them apart is worse than reviewing them over the cap — the guard read without its own regression cases is the false-green shape." Granted at **187** rather than the 184 first named; the +3 are the P2-24 filing and the round-1 log row, both requested after the number was set — **Michael's sequencing, recorded as such, not scope creep**. `d474e67` is also the correct base independent of the waiver: `origin/main` would re-read round 1's already-reviewed 116 test lines (`84015f3` precedent). **Round 1's two findings were both fixed in this commit and both are confirmed closed by this round.** **TWO NEW MEDIUM FINDINGS, FILED NOT FIXED (two-round cap), both UNVERIFIED by execution in Codex's own words, both confirmed by CC on inspection — carried as `docs/TODO.md` P2-26:** (a) HEPA's own hire period at `pricing.ts:276-278` still uses the old truthiness form, so `hepaAirScrubberDays: Infinity` yields `Infinity`, or `NaN` at zero quantity — `isUsableEquipmentDays` guards only the *shared* days and the HEPA branch is a second multiplication that was never routed through it; (b) **`MAX_QUOTABLE_EQUIPMENT_DAYS = 3650` is a duration policy, not an arithmetic boundary, and it silently reprices valid input** — `equipmentDays: 3651` with one dehumidifier returns $119 where the pre-fix engine returned $434,469, a figure float64 represents exactly. **CC wrote the "overflow sentinel, NOT a business cap" claim into the code comment, into P2-24 and into the handback to Michael; Codex is right and all three are wrong as written.** That is the second time in this session Codex corrected CC's own reasoning rather than the code (the first was the parity self-comparison in round 1). Codex again **did not run the tests**; CC ran them separately — 75 files / 1290 tests, 1289 pass, the only failure the known T24 `reportPipeline` one — and **proved the round-1 guard tests capable of failing** by reverting only the guard: 4 of 19 failed, and the right 4 (Infinity, finite-total, `MAX_VALUE`, sentinel), with NaN/negative/fraction/parity still passing because the fix does not change those; file restored `cmp`-identical. Codex's third next step is carried too and is fair: the PRE_CHANGE literals are **selected-field** parity (6 fields), not complete output parity, and the file should say so. Untriaged beyond the pre-authorised "file it" ruling, to Michael. |
| 2026-09-08 | mrc-equip-days / fix/equipment-days-form, `342f4b5` vs **`beef5e9`** (Unit B; pre-declared stacked parent — the retraction commit, NOT `071fdf3`) | **146 reviewable** (excl. `docs/sessions/`), 7 files — under the 150 cap | adversarial, foreground, companion script direct, `--wait --cwd <worktree> --base beef5e9`, focus: can the reconcile rule mis-classify an explicit period that equals the derived days; save→load→save round-trip fidelity; do the three read surfaces agree with each other and the engine; is anything ViewReportPDF reads now stale; can any path write a wrong figure to a saved quote, round 1 of 1 (report-and-stop) | gpt-6-astra / high, config intent only (#705) | 1 (medium) | 0 | 0 | 0 | — | ~3m | not captured | Target: `branch diff against beef5e9` — printed and checked before the body was read. Verdict: **needs-attention**. Codex thread `01a08022-5fbd-7893-b31d-1ecdff02b67e`. **Base correction, second in this stack:** Michael named `--base 071fdf3`; the retraction commit `beef5e9` sits between it and B, so `071fdf3` would have re-read those 36 lines and reported **182**. Measured both before sending; `beef5e9` gives exactly 146. **One medium finding, NOT applied (report-and-stop), and unlike rounds 1 and 2 on Unit A this one is squarely IN-DIFF:** `ViewReportPDF.tsx:1683-1690` — Unit B's third added line feeds the mount-time `inspection.equipment_days` into `autoEstimate` alongside `areasData`, which several handlers refresh independently. When the refreshed hours derive fewer days than the retained stored value, the reconcile rule reads that stale figure as an **explicit** hire period and the cost editor saves the inflated quote. Codex's repro: 16h inspection, auto `equipment_days=2`, one dehumidifier; another session rewrites it to 4h / `equipment_days=1`; add an area, open and save the cost editor — `equipment_cost_ex_gst` saves **$238** where $119 is correct. **CC confirmed the mechanism in source AND found it more reachable than Codex framed it: no second session is required.** `setInspection` is never a full refetch after mount (`:1551`, `:1799`, `:1992` are partial merges), while `setAreasData` is called from four sites including the area-update handler at `:2119`, which writes `inspection_areas` and refetches. So a single user editing an area's treatment time downward inside ViewReportPDF, then saving the cost editor, reaches the same reclassification alone. That moves it from a concurrent-writer race — the class Michael has previously ruled carry-not-fix — to an ordinary single-session edit sequence. **Before Unit B the path did not exist**: `equipmentDays` was never passed here, so the engine always derived from hours and no reclassification was possible. Codex states it traced the code without running tests, and asks for the two-session regression test; the single-session case is CC's addition and is also untested. Carried to Michael untriaged. |

## Rulings — precedent

Standing rules established by a decision on a specific run, recorded here so the next
run that hits the same wall does not have to re-litigate it.

**Reasoning-level catches are a separate category from the code-level record (Michael, 2026-09-08).**
The reviewer's standing record — 13 false flags to a handful of real fixes — was built on findings
about *code*. Two catches on the equipment-days stack were not that: in round 1 Codex showed that
absent/0 parity tests compared the modified engine against itself and therefore proved nothing about
historical parity; in round 2 it showed that a constant CC had introduced, commented, filed in
`docs/TODO.md` and described to Michael as an "arithmetic-overflow sentinel" was nothing of the kind.
Neither was a defect a test would have caught — both were CC's *reasoning* about its own work, stated
confidently and wrong, and in both cases CC had already written the claim into tracked files. Count
these separately when judging whether a review round earns its cost: a reviewer that only ever
re-checks code would have passed both.

**Reviewable surface excludes byte-identical restores** (Michael, 2026-09-05, on
`8984fed`). A byte-identical restore of an unchanged tracked file is excluded from the
~150-line reviewable-surface count in the entry gate. The reviewer still receives the
file as context — it is not withheld — but only new or modified logic counts toward
the limit. Applied on `8984fed`: 166 raw lines, ~42 reviewable, because 124 of them
were a restore of a file that had been running unchanged for days. Without this rule
the commit would have failed entry condition 4 at 166 > ~150, and the review that
found three HIGH bypasses would never have run.

Both rounds so far ran in worktrees that carry `AGENTS.override.md` (`mrc-merge`, `mrc-node22`), so both had the reviewer brief and the log stands — but only 4 of 21 worktrees carry it; see the Session F scope note in `docs/TODO.md` (Session P, 2026-09-05).

## Do not review

Any branch whose diff contains customer PII is reviewed locally or not at all. Codex review sends the reviewed content to OpenAI, and the ruling is that no customer PII goes to OpenAI (Michael, 2026-09-05). Check the diff for names, emails, phone numbers and addresses before running any `/codex:*` command on it.

- Branch `docs/backlog-pricing-canon-and-ledger`, 2998 insertions, documentation only, no logic. Exceeds the ~150 line review limit by 20x and carries redacted customer data. Reviewed by Michael, not Codex. Round 1 smoke test on ddfa6ca stands as the loop's proof. (Michael, 2026-09-05)
- Commit `3af5241` (branch `docs/todo-node24-verified`), 7 insertions / 3 deletions, tracker text only — row N wording, the T13 note, and new rows P2-18 and P2-19 — no code and no config. Deliberately not reviewed: the round 2 review ran on the pin branch before this follow-up existed, and re-running `--base main` after the merge would have reviewed an empty diff and exited 0. Recorded so the gap is on the record rather than looking like an oversight. (Michael, 2026-09-05)
- Branch `docs/session-2026-09-06-close` (stacked on `docs/codex-workflow`): the six rows above and the session log `docs/sessions/2026-09-06-chore-session-logs.md`, nothing else. Not reviewed, by the `3af5241` precedent: the rows post-date the reviews they describe. (Michael's plan, 2026-09-06)

## Setup findings, 2026-09-05

1. **#684 split-root observed live.** The companion writes `stopReviewGate` to `os.tmpdir()/codex-companion/<workspace>/state.json` unless `CLAUDE_PLUGIN_DATA` is set, and it is not set in Claude Code's Bash tool. The Stop hook runs with it set and reads `~/.claude/plugins/data/codex-openai-codex/`, which is empty, so it falls back to the default. The gate is off in both roots, but only because the default is off, not because we set it there. If the default ever flips, this bites.
2. **`review_model` recognition unverified on codex-cli 0.153.2.** All three keys (`model`, `model_reasoning_effort`, `review_model`) are set to `gpt-6-astra` / `high` as the #705 mitigation. Whether the plugin honours `review_model` specifically could not be confirmed without spending quota. An unknown, not a pass.
3. **Pinned at codex-cli 0.153.2.** `codex doctor` reports 0.153.4 available. Staying pinned until three logged runs are done. **Condition met 2026-09-05** — rounds 1, 2 and 3 are logged, all clean. The unpin is Michael's call, not an automatic consequence; nothing here upgrades on its own.
