<!-- {{TOKEN}} fields are filled by .claude/hooks/session-start.sh on startup/clear.
     <fill: ...> fields are filled by hand as the first step of the session.
     Builder-Codex: never create a second log for a branch. Append to the newest
     docs/sessions/*-<branch-slug>.md; only if none exists copy this file by hand,
     replace every {{TOKEN}}, and set Tool to Codex. -->
# Session log — 2026-09-15 — work-main

## Header

- Date: 2026-09-15
- Lane: **Session 2** — verify `candidate/2026-09-15-587415e` on DEV; prepare every DEV command for Michael. No application code. Session 1 (integration) is closed; Session 3 (lanes) runs in parallel in its own worktrees.
- Branch: work in `~/mrc-app-1` (work-main, where the hook created this log) and `~/mrc-verify`. The log is committed on `integration/2026-09-15` as `docs/sessions/2026-09-15-integration-2026-09-15-2.md`, following Session 1's pattern. This `work-main-3.md` stays an untracked working copy; the committed file is canonical.
- Worktree: /Users/michaelyoussef/mrc-app-1 (log) · `~/mrc-verify` (detached at the tag, scratch tests only) · `~/mrc-integration` (EF deploy CWD for Michael; one docs commit by CC)
- Tool: CC
- Model: claude-opus-5[1m] xhigh (manager). Routing (Michael): DEV query pack opus 5 xhigh; save-race analysis + decisions 1-2 fable max; 375px browser agent sonnet 5 high NOT launched until Michael says Session 3's four lanes are finished. At most three sub-agents until then.
- Baseline commit: tag `candidate/2026-09-15-587415e` (tag object `16ebc8b`) → `587415e`. `origin/integration/2026-09-15` = `7e7cb07` = tag + `062ee44` + `7e7cb07`, and `git diff --stat 587415e 7e7cb07` touches only `AGENTS.md`, `CLAUDE.md`, `docs/**`, so the pinned Vercel preview build at `7e7cb07` carries the tag's application code. Blobs identical at tag and tip: `generate-job-report-pdf/index.ts` `dd918b5`, `send-email/index.ts` `b84f795`, `generate-inspection-pdf/index.ts` `57804c5`, `config.toml` `2c3fa8a`. `package-lock.json` `929f0df` identical at `dedd0a3`, tag and tip.
- Starting tsc error lines: inherited from Session 1 Step 2 at `587415e` — 100 lines / 41 distinct (Node v24.20.0, TS 5.8.3). Not re-measured by this session.
- Starting test count: inherited from Session 1 Step 2 at `587415e` — 84 files / 1348 tests, 1 fail (T24); `node --test` generate-job-report-pdf 5/5. Not re-measured by this session.
- Session id: 245717ba-7a04-4b71-9e17-4a1344740165

## Intent

Step 0 (where DEV's `email_logs` Slack trigger posts) → E (E-Q4 read, migration, `send-email` deploy, suppressed-row check only if Step 0 clears) → J+K `generate-job-report-pdf` deploy verified by download → P0-A proof and a deliberate I-F1 23505 on DEV; S never-settles race test. CC applies, deploys and pushes nothing; the PROD ref is never used.

## Touching

- this log (working copy in `~/mrc-app-1`; committed copy `docs/sessions/2026-09-15-integration-2026-09-15-2.md` on `integration/2026-09-15`)
- scratchpad files only for the DEV/PROD sheets (query pack, procedures, command sheet, step 0 sheet, isolation sheet, decision docs, PROD reads)
- `~/mrc-verify` — new detached worktree at the tag; the race agent's two scratch test files (untracked, never committed there); removal is Michael's call
- `~/mrc-integration` (one docs commit, explicit paths):
  - `docs/TODO.md` — P2-55, P2-56, P2-57 added; P0-6, L-K2 and L-S2 rows amended
  - `docs/repro/REPRO-save-queue-hang-587415e.test.tsx.txt`, `docs/repro/REPRO-save-queue-deadline-contract-587415e.test.tsx.txt`, `docs/repro/REPRO-save-queue-runs-587415e.txt` — new
  - `docs/decisions/2026-09-15-send-email-idempotency.md` — new
  - `docs/sessions/2026-09-15-integration-2026-09-15-2.md` — new (this log)

## Step log

Append a line BEFORE each unit of work. Sub-agents prefix every line with `[<agent-name>]`.
Format: `- HH:MM · tool · agent · what · files · outcome` — tool is `CC` or `codex`, so both agents' work reads in one log.

- 18:1x · CC · CC · orientation: Session 1 log (596 lines), TODO follow-up units + deploy notes, memories SF-5 / DB access / EF deploy / guard hook · (none) · done
- 18:2x · CC · CC · free space 17 GiB on /System/Volumes/Data; `ps`: three `claude` CLI processes (this one + two others) and four codex app-servers · (none) · noted
- 18:2x · CC · CC · tag vs tip vs preview: diff stat, EF + config + lockfile blobs · (none) · preview build = tag's application code (header)
- 18:2x · CC · CC · Step 0 static read: all three tracked bodies of `email_logs_notify_slack()` hard-code PROD's `send-slack-notification` URL with Vault `internal_webhook_secret`; `docs/multi-tech/deploy/DEPLOY-LOG.md:171` records live DEV Studio result 2026-08-31 "posts_to_prod = true" and DEV Vault `internal_webhook_secret` created 2026-05-27 · (none) · finding drafted; whether PROD accepts DEV's secret is unproven
- 18:2x · CC · CC · a "TRIAGE — session 3" message from Michael arrived in this window; not acted on (Session 3 owns lanes 1/2/4 and their worktrees) · (none) · flagged to Michael
- 18:28 · CC · CC · create detached worktree `~/mrc-verify` at the tag; `node_modules` symlinked to `~/mrc-integration`'s clean `npm ci` (same lockfile `929f0df`, TS 5.8.3) · `~/mrc-verify` · see next line
- 18:28 · CC · [dev-query-pack] · launched, opus, read-only, writes scratchpad only · scratchpad · running
- 18:30 · CC · CC · `~/mrc-verify` created: detached `587415e`, `git status` clean, node_modules symlink in place; disk 17 GiB free · `~/mrc-verify` · done
- 18:31 · CC · [save-race] · launched as a one-agent workflow (fable, effort max — the Agent tool cannot set effort): S-Q1 never-settles characterization + L-S2 contract test at the tag, scratch test files in `~/mrc-verify/src` only · `~/mrc-verify`, scratchpad · running. "Decisions 1-2" not found in any file — asked Michael, not guessed
- 18:31 · CC · CC · `node --test supabase/functions/generate-job-report-pdf/index.test.mjs` at the tag in `~/mrc-verify`, Node v24.20.0, pre-deploy check · (none) · tests 5, pass 5, fail 0
- 18:31 · CC · CC · DEV command sheet (Step 0 CLI digests, EF capture → gate → deploy → download verify for send-email, generate-job-report-pdf, generate-inspection-pdf; E order) · scratchpad `session2-dev-commands.md` · written
- 18:33 · CC · CC · Step 0 finding reported to Michael; suppressed-row check held; asked where "decisions 1-2" are defined · (none) · STOPPED for Michael on Step 0
- 18:4x · Michael · — · rulings: Step 0 isolation = option (b). Order: (1) A4 sha256 of DEV `internal_webhook_secret` vs PROD digest, Michael compares, match/no-match recorded here and on the SF-5 row; (2) rotate DEV Vault `internal_webhook_secret` to a random DEV-only value, only after CC confirms from the trigger source that a non-2xx from send-slack-notification cannot abort the `email_logs` insert (if it can: option (a) instead); (3) rotate DEV `SLACK_WEBHOOK_URL` to a DEV-only channel webhook, or a sink value recorded as "DEV Slack dark"; (4) PREPARE ONLY the full DEV-isolation sheet (every PROD-valid DEV Vault secret, every cron target reaching PROD) = SF-5 remediation list. Suppressed-row check and priming email only after 1–3 are done and logged. Ratified: deploy generate-inspection-pdf if DEV is not on `57804c5` (sheet step 1 decides). Michael runs nothing until the query pack lands. Decisions 1-2 defined (send-email idempotency key; PDF-CL17 signed URLs vs base64 with PROD 546 log read) — analysis only, one-agent workflow fable/max. Session 3's lanes finished → browser agent released · (none) · recorded
- 18:4x · CC · [decisions-1-2] · the line logged here as "launched" was premature: that turn wrote only this log line, no workflow went out · (none) · corrected
- 18:5x · CC · [dev-query-pack] + [save-race] · both died on `API Error: Can't reach the API server (ENOTFOUND)`: pack agent before returning, save-race after 35 tool uses / 819 s · (none) · Michael: "try again"
- 18:5x · CC · CC · retry: pack agent resumed with its context; save-race re-run told to check for partial scratch files first; [decisions-1-2] launched (fable, max, analysis only, PROD reads as files) · scratchpad, `~/mrc-verify` · running
- 18:5x · CC · CC · state after the failures: pack agent wrote no file; save-race left only `session2-race/01-sanity-concurrentSave.txt`, no scratch test in `~/mrc-verify` (status clean incl. untracked); disk 17 GiB free · (none) · noted
- 18:5x · CC · CC · preview URL for `7e7cb07` from the GitHub deployment status: `https://mrc-system-k5bxomy03-michaelyoussef396s-projects.vercel.app` (Vercel status success). SF-5 tracker row = `docs/TODO.md` P0-6 (integration tree line 111). Playwright MCP = plain `npx @playwright/mcp@latest`, no `--isolated`, so a persistent profile is possible and the SW/cache reset is mandatory · (none) · noted
- 18:5x · CC · [dev-query-pack] · resumed with additions: A1 body-shape checks (pg_net async call, EXCEPTION handler, pgsql-http absent) for item 2; section G = DEV-isolation sheet (item 4, prepare only) as a third file · scratchpad · running
- 18:5x · CC · [browser-375] · launched as a one-agent workflow (sonnet, effort high), Playwright MCP, 375px, phase 1 = S + H+C only; I, J+K, E wait for DEV deploys and Slack isolation; hard stop on any send-slack-notification / send-email / resend call · scratchpad `session2-browser/` · running
- 18:5x · CC · CC · item 2 safety check (trigger source + pg_net semantics) and item 3 sink safety (every DEV `SLACK_WEBHOOK_URL` consumer's failure path) · (none) · done
  - Item 2, static: AFTER INSERT binding; `net.http_post` is pg_net; the whole body sits in `EXCEPTION WHEN OTHERS … RETURN NEW` (`20260823090000:263`, `:295-297`). Supabase pg_net docs verbatim: "HTTP requests are not started until the transaction is committed." So a non-2xx from send-slack-notification cannot abort the `email_logs` insert. The only reader of Vault `internal_webhook_secret` is this function. Conditional on pack A1/A2 showing DEV's live body has the same shape; otherwise option (a).
  - Item 3, static: every `SLACK_WEBHOOK_URL` consumer catches failure — send-slack-notification (`:427`, 500 + `error_logs`), client helper (`notifications.ts:568-595`, never throws), receive-framer-lead (`:442-460`, `:877-887`), check-overdue-invoices (`:142-158`). A sink value breaks no DEV flow.
- 18:5x · CC · CC · preview protection check: `curl` → HTTP 302 to `vercel.com/sso-api` (Vercel deployment protection) · (none) · Michael must log in to Vercel and to the app inside the Playwright window; browser agent polls 15 min
- 18:5x · CC · CC · Step 0 items 1–3 sheet · scratchpad `session2-step0-isolation.md` · written; not runnable until the pack's A1–A3 land
- 19:0x · Michael · — · logged in in the Playwright browser window (Vercel SSO + app) · (none) · browser agent's 30 s login poll should now proceed
- 19:00 · CC · [save-race] · sanity run of the unit's own test on Node v24.20.0 (absolute path, dummy env inline) · src/pages/__tests__/TechnicianInspectionForm.concurrentSave.test.tsx · 4/4 green, 1.60 s; worktree clean at 587415e
- 19:02 · CC · [save-race] · read the S queue at the tag and the client stack · TechnicianInspectionForm.tsx :4002-4011 :4576-4597 :4735 :717-765 :324-347 :4971-4983, client.ts:100-129, vite.config.ts:86-95, supabase-js/postgrest-js fetch wiring · no client-side bound on a write; SW NetworkFirst 10 s is GET-only
- 19:13 · CC · [save-race] · wrote two scratch files (harness copied from concurrentSave.test.tsx, original untouched) and ran the characterization file · `TechnicianInspectionForm.saveQueueHang.race.scratch.test.tsx`, `TechnicianInspectionForm.saveQueueDeadline.contract.race.scratch.test.tsx` · 8/10; both Complete tests failed on a harness gap (Section 7 method list gated on optionSelected, :2082)
- 19:14 · CC · [save-race] · ran the L-S2 (1) contract test (hung save, 120 s fake time) · saveQueueDeadline contract file · FAILS as expected: `AssertionError: expected [] to have a length of 1 but got +0` (line 179)
- 19:15 · CC · [save-race] · fixed the walk (Option 1 before HEPA Vacuuming), re-ran both files twice, copied files and outputs to scratchpad `session2-race/` · both scratch files · characterization 10/10 on runs 2 and 3; contract fails identically on run 2
- 19:1x · CC · CC · independent re-run of both scratch files in `~/mrc-verify`, Node v24.20.0 · (none) · confirmed: hang file 10 passed (10); contract file 1 failed (1) with the same AssertionError. `git status`: only the two untracked scratch files. Both are picked up by the vitest include glob, so a full-suite run in `~/mrc-verify` counts 1 extra failure until Michael decides their fate
  - S-Q1 result at the tag: nothing client-side bounds a save write (`client.ts:100-129` passes no fetch/signal; postgrest `signal` only via `.abortSignal()`, zero uses in `src/`). During a hang: second Save, 30 s auto-save (skips at `:4592`, does not enqueue) and Complete all issue no request; footer reads "Saving..." disabled; header Save icon stays enabled (every tap parks another save); no toast. Complete shows the non-dismissable "Generating AI Summary" overlay (`:4971-4983`) while nothing has reached the server. Release drains in order onto one id; a rejection shows "Save Failed" and the queue continues.
  - Adjacent, static only, UNVERIFIED, outside S-Q1: the PWA NetworkFirst route (`vite.config.ts:86-95`, 10 s network timeout, 1 h cache) covers GETs, so the save's pre-write snapshot SELECTs (`:4110-4133`, `:4277`) can be served from cache after a 10 s stall, feeding area reconciliation (`:4281`). Not tested; raised to Michael as a question, not a finding.
- 19:19 · CC · [dev-query-pack] · read the tag (send-email, both PDF EFs + test, render-pdf, reportPipeline, UI callers, migrations) · supabase/functions, api, src, supabase/migrations · done, nothing executed
- 19:19 · CC · [dev-query-pack] · wrote DEV query pack A–F (A4 first, A1 shape booleans, A1c) · scratchpad `session2-dev-query-pack.sql` (904 lines) · written
- 19:19 · CC · [dev-query-pack] · wrote DEV procedures + side-effect table · scratchpad `session2-dev-procedures.md` (172 lines) · written
- 19:19 · CC · [dev-query-pack] · read SF-5 write-up + DEPLOY-LOG, wrote isolation sheet G1–G7 (prepare only) · scratchpad `session2-dev-isolation-sheet.md` (370 lines) · written
- 19:2x · CC · CC · review of the pack · three files · usable, with notes:
  - SQL read in full: every block SELECT-only unless headed WRITE; the PROD ref appears only as A1's detection literal; B1 = the ruled E-Q4 query; B-APPLY byte-matches the migration.
  - Spot-checked at the tag: `api/render-pdf.ts:164` posts `previewOnly: true` (hard-save never bumps the counter); `generate-job-report-pdf/index.ts:492-499` insert sets no `generation_type`; `LeadDetail.tsx:777` "Regenerate" calls `hardSaveReport`; send-email recipient cooldown keys on recipient + `status='sent'` + `sent_at` only (`:203-216`), so the pack's exempt-template probe route in section C is valid.
  - B4c ledger form confirmed against memory `feedback_preflight_schema_verification.md:20` (Michael, 2026-09-09); its `name` value is inferred and needs Michael's OK.
  - Isolation sheet: grep finds no write statement.
- 19:2x · CC · CC · corrected `session2-step0-isolation.md`: item 2's behavioural 403 proof no longer claimed for the first insert (section C's probe rows are exempt and post nothing); the post-rotation hash is the proof, and a non-exempt insert is offered, not prepared · scratchpad · done
- 19:2x · CC · CC · run-order section added to `session2-dev-commands.md` (one path through all four sheets; D/E orders from the pack) · scratchpad · done
- 19:01 · CC · [browser-375] · hit Vercel SSO login wall on first navigation · (none) · screenshot `session2-browser/00-vercel-login-wall.png`, began 15-min poll
- 19:05 · CC · [browser-375] · SSO chain cleared by Michael; landed on the app's own login page · (none) · screenshot `01-app-login-page.png`
- 19:06 · CC · [browser-375] · fresh-state reset on the origin: unregistered 1 SW, deleted 1 cache, cleared local/sessionStorage, deleted IndexedDB `mrc-offline`; reload re-registered SW(1)/cache(1) · (none) · reset done
- 19:06–19:21 · CC · [browser-375] · polled the app login page 15 min; no credentials entered; no sign-in seen · (none) · **BLOCKED_LOGIN**; no Supabase host contacted, no forbidden call; console: two CSP blocks from the Vercel SSO wrapper/toolbar, one Sentry ingest 429 on the first load
- 19:2x · CC · CC · diagnosis: my brief ordered the storage reset "before using the app", which the agent could only do after SSO, i.e. after Michael reached the login page; a sign-in made around 19:05–19:06 would have been wiped by that reset. Brief defect, not Michael's · (none) · owned
- 19:2x · CC · [browser-375] · relaunched (sonnet, high): reset BEFORE login, then tab title "READY - log in now (MRC Session 2)" as Michael's cue, no storage clearing after that, 20-min poll, browser left open at the end · scratchpad `session2-browser/r2-*` · running
- 18:58 · CC · [decisions-1-2] · read send-email, BookJobSheet, notifications, bookingService, ViewReportPDF, LeadsManagement, email_logs migrations, fan-out trigger, useActivityTimeline, types.ts at the tag; Round 2 rulings; L-E2/P0-5/P0-14 rows; 2026-09-12 cooldown log · read-only · premise confirmed with two corrections
- 19:05 · CC · [decisions-1-2] · read generate-job-report-pdf, api/render-job-report-pdf, jobReportPipeline, three reportHash copies, photo upload/resizer paths, vercel.json, bucket migrations, PDF-CL17/T18/T19/P0-C/P2-51 rows · read-only · no lead number recorded for the 546 job; one unresized upload route (ImageUploadModal)
- 19:10 · CC · [decisions-1-2] · Supabase docs (limits, 546, shutdown reasons, log fields, retention) and Resend idempotency docs · WebFetch/WebSearch only · retention 1/7/28/90 days by plan; "wall clock" line printed on any termination
- 19:22 · CC · [decisions-1-2] · wrote `decision-1-send-email-idempotency.md` + `decision-1-prod-reads.sql` (SELECT-only) · scratchpad `session2-decisions/` · server-derived key, pending-first claim, partial unique index; ships with P0-5 after L-E2; P0-14 stays open
- 19:27 · CC · [decisions-1-2] · wrote `decision-2-pdf-cl17.md` + `decision-2-prod-reads.sql` (Logs Explorer A0–A5 + legacy block; Studio B0–B8) · scratchpad `session2-decisions/` · recommendation conditional on the shutdown line; Step A before ~17 Sep 09:58 UTC if PROD is on Pro
- 19:28 · CC · [decisions-1-2] · self-check of outputs: no PROD-ref hit, no write statements, no email-like strings · 4 files, 103,745 bytes · done
- 19:3x · CC · CC · review of the decision outputs · (none) · spot-checked at the tag:
  - `BookJobSheet.tsx:415-421` deletes every job booking for the lead before re-inserting, so a booking id changes on every submit; `:576` sends `job-booking-confirmation` on both book and reschedule; the send is fire-and-forget with `.catch(console.error)` and the success toast runs regardless; `:557` returns silently when the lead has no email.
  - `ViewReportPDF.tsx:1131` `report-approved`; `generate-job-report-pdf/index.ts:176` logs the job completion id; `docs/TODO.md:388` records the 10 Sep 19:58 546 with no lead or job number.
  - grep: no write verb at a statement start in either PROD reads file.
  - `decision-1-prod-reads.sql` read in full: five SELECT-only queries; `recipient_email` appears only inside CTEs and join conditions, never in an output column; outputs are template names, statuses, lead numbers, ids, counts, timestamps. `decision-2-prod-reads.sql` (454 lines) not read line by line: its A2 returns raw function log lines, which may carry data, so the report tells Michael to paste only lead numbers, ids and shutdown lines.
- 19:3x · CC · CC · decisions reported to Michael; time-critical: PDF-CL17 Step A must run before the 10 Sep logs age out (~17 Sep 09:58 UTC on Pro, already gone on Free) · (none) · STOPPED for Michael
- 19:24 · CC · [browser-375] · reset BEFORE login on the app login page (no SSO wall this time): 1 SW unregistered, 2 caches deleted (workbox precache, google-fonts), local/sessionStorage cleared, IndexedDB `mrc-offline` + `workbox-expiration` deleted; reload re-registered SW(1)/precache(1) · (none) · reset_done_before_login = true
- 19:25 · CC · [browser-375] · tab title "READY - log in now (MRC Session 2)"; no storage clearing after this point · `session2-browser/r2-01-ready.png` · polling
- 19:27 · CC · [browser-375] · Michael logged in (admin); every `*.supabase.co` request went to `ctppzqnysmzynkxjlzta` only; no forbidden call all session · (none) · PASS
- 19:28 · CC · [browser-375] · H+C at 375px: Stalled Inspections tile renders, count 1, MRC-2026-0002 (PASS, `r2-02-dashboard-online.png`); no horizontal scroll, scrollWidth 375 = innerWidth 375 (PASS); console on the online load: only Vercel CSP noise (PASS) · StalledInspectionsCard · done
- 19:29 · CC · [browser-375] · C-F1 live repro (offline on /admin/leads, then in-app nav to Dashboard): the Stalled tile kept its cached "1 / MRC-2026-0002", so C-F1 was NOT reproduced live (its query was cached and not refetched). Observed instead: "Leads to Assign" dropped 1 → 0 and "Today's Jobs" read 0 with no error or loading text (console-only `[Dashboard Stats] … Failed to fetch`), still 0 three seconds after going back online · `r2-03-offline-dashboard.png` · agent marked FAIL; CC reclassifies below
- 19:30 · CC · [browser-375] · S: admin "Continue Inspection" on MRC-2026-0002 opened the form with no `leads` PATCH (PASS); Save button 343 × 56 px (PASS); double-save INSERT race SKIPPED: the only reachable lead already had inspection INS-2026-0002, so no first-save INSERT could be exercised without writing to an existing DEV record · TechnicianInspectionForm · browser left open
- 19:3x · CC · CC · classification of the offline observation · (none) · the two zeroing tiles come from `src/hooks/useAdminDashboardStats.ts`, which the candidate does not touch (`git diff --stat dedd0a3 587415e` shows only `AdminDashboard.tsx` +2 for H+C; last commit to the hook `91dd58f`, 2026-07-28). The hook is a mount-time `useEffect` fetch with initial state 0 (`:31`, `:42`) and a console-only catch (`:201-202`): PRE-EXISTING silent zero, outside the candidate, contrary to the F/C rule. "No recovery after 3 s" is a short observation window, not proof of no recovery. C-F1 itself remains a static CONFIRMED finding (L-C2), unreproduced live because of cache.
- 19:3x · CC · CC · browser + decisions results reported to Michael with six questions · (none) · STOPPED for Michael
- 19:4x · Michael · — · rulings, batch 3 · (none) · recorded:
  - **403 proof:** (a) DEV's hash changes after rotation, plus (b) PROD's deployed send-slack-notification source, read by download, shows it rejects a mismatched secret. No non-exempt insert. If (b) shows no validation, stop.
  - Still owed before step 2: the trigger-source confirmation.
  - B4c name comes from the tag's migration filename.
  - Every DEV send test uses an `@example.invalid` recipient; check C1/C2.
  - Isolation-sheet additions, prepare only:
    - DEV's live Resend key + cloned bookings are the top item: a test key or a scrubbed clone before any cron points at DEV.
    - The Slack trigger is the third DEV→PROD link; add it to SF-5.
    - `SENTRY_ENVIRONMENT=dev` is prepared; Michael runs it tonight.
  - Tracker rows: LeadDetail "Regenerate" is a hard-save (P2); job-report history `generation_type` NULL (L-K2).
  - No new agents. The I, J+K and E browser surfaces are recorded as NOT VERIFIED, not deferred.
- 19:4x · CC · CC · found an error in G7 · scratchpad isolation sheet · corrected
  - The sheet marked SF-5 §5 items 2 and 3 (PROD service-role inventory; cut DEV's link incl. cron re-point) "executing tonight".
  - Cause: CC's resume brief to [dev-query-pack] said "Mark items 2 and 3 as executing tonight" without naming the list.
  - Both cells now read "prepared only — NOT tonight", with an explanatory sentence and a pointer to Step 0 items 2–4.
- 19:4x · CC · CC · trigger-source confirmation restated with citations · `session2-step0-isolation.md` §2.0 · done
  - Binding `20260527023540:79-82`.
  - pg_net call `20260823090000:263`.
  - pg_net docs quote: "not started until the transaction is committed".
  - Exception handler `:295-297`.
  - Condition: pack A1/A1c/A2 must show the same live shape.
- 19:4x · CC · CC · proof (b) prepared · `session2-step0-isolation.md` §2.3 · done
  - Michael downloads PROD's send-slack-notification (PROD ref from CLAUDE.md, function code only).
  - Expected blob `1f9c303d1c95532bb253bd806ce3e847de2e6fc7`: identical at the tag, origin/main and origin/production; last changed in `4f781d4` (2026-05-27).
  - At that blob, `:327-336` returns 403 on a mismatched secret and `:337-357` returns 401 without an Authorization header. The trigger sends no Authorization header (`20260823090000:265-269`).
  - No blob match → CC reads the downloaded file. No validation → STOP.
- 19:4x · CC · CC · B4c name check · scratchpad pack · no value change
  - `git ls-tree` at the tag → `supabase/migrations/20260912000000_email_logs_suppressed_status.sql`, name `email_logs_suppressed_status`, the sheet's value.
  - Pack B4 comment and B4c header now say "from the migration filename".
- 19:4x · CC · CC · recipients switched to `session2-probe@example.invalid` · scratchpad pack + procedures · done
  - Changed: pack C1 VALUES and section header; procedures C1 text and C2 bodies.
  - The address passes the `recipient_email` CHECK (`20251111000008:21`).
  - Option B withdrawn.
  - C2 must return 429: a `failed` row would not be trigger-exempt.
- 19:4x · CC · CC · isolation-sheet additions · scratchpad isolation sheet · done
  - A1: live Resend key + cloned bookings, the top item.
  - A2: third DEV→PROD link.
  - A3: `SENTRY_ENVIRONMENT=dev`. Command in step0 sheet item 4; reader `_shared/errorReporting.ts:159`, default `'production'`.
- ~19:5x · — · — · Claude usage limit reached mid-round; resumed ~22:4x · (none) · no work lost (22:5x check)
- 22:4x · Michael · — · rulings on CC's six · (none) · recorded:
  1. A4 first, then the run order.
  2. Silent-zero dashboard tiles: new P2 row, sibling of L-C2.
  3. Live double-save: "not verified live; covered by the unit's tests and save-race's characterization tests"; optional after Step 0 on a later DEV pass.
  4. Save-race scratch files: keep them, committed under `docs/repro/` on integration/2026-09-15 and referenced from L-S2. The service-worker stale-read is an open question on that row, not tonight.
  5. B4c name comes from the tag's migration file.
  6. Michael runs the PDF-CL17 log read before 17 Sep 09:58 UTC.
- 22:4x · Michael · — · Decision 1 §6 answers, provisional on the PROD reads · (none) · recorded:
  - 24 h rolling window.
  - Keyed sends bypass the recipient cooldown; the hourly cap stays until P0-5; the cooldown is deleted after P0-5.
  - Duplicate hits (reason `duplicate`) do not post Slack; cooldown suppressions still post.
  - Resend override is admin-only in v1, logged as a timeline event with the actor.
  - Ship order: L-E2 → P0-5 → key → delete the cooldown.
  - Key derived inside the EF from entity id + template + hash of template inputs; the unkeyed framer path is logged.
  - Then commit and end the night; nothing else launches.
- 22:4x · CC · CC · Decision 1 rulings appended · `decision-1-send-email-idempotency.md` → committed copy `docs/decisions/2026-09-15-send-email-idempotency.md` · done, with three CC notes:
  - A rolling window cannot be a static key component.
  - Reason literal: `duplicate` (ruling) vs `idempotent_duplicate` (doc).
  - **`receive-framer-lead` calls Resend directly (`index.ts:421`, `:943`), not send-email, so a key derived inside send-email never reaches the framer send.** Recorded, not decided.
- 22:5x · CC · CC · `docs/repro/` built · `~/mrc-integration/docs/repro/REPRO-save-queue-{hang,deadline-contract}-587415e.test.tsx.txt`, `REPRO-save-queue-runs-587415e.txt` · done
  - Three files; each header states provenance and results.
  - PII scan of the race sources and outputs: only `@example.com` / `@example.invalid` fixtures.
- 22:5x · CC · CC · TODO rows P2-55/56/57 were first written with line breaks inside table cells, which breaks the table · `~/mrc-integration/docs/TODO.md` · rewritten as single-line rows before commit
- 22:5x · CC · CC · status check of every sub-agent and workflow after the limit (Michael recalled one agent + three workflows unfinished) · (none) · all finished, nothing to relaunch
  - Workflow journals: `wf_4f0612d1` browser r1, `wf_56e886a8` save-race retry, `wf_9267a8e9` browser r2 and `wf_b6cc75c2` decisions each carry 1 result.
  - `wf_bcb11540`, the first save-race run, has none: it died on ENOTFOUND and was superseded by `wf_56e886a8`.
  - [dev-query-pack] completed after its resume; all three files are present.

## Codex threads

Write `codex resume <threadId>` here the moment it is printed — on stderr as `Thread ready (<id>)`, or by `node <companion> status`. The plugin SessionEnd hook deletes every job of the session, running or finished.

- (none yet)

## Review

- Target: <verbatim `Target:` line from the companion output — anything other than the pre-declared base = abort and report>
- Diff lines excl. docs/sessions/: <from `git diff --numstat origin/main...HEAD -- . ':(exclude)docs/sessions/' | awk '{a+=$1;d+=$2} END{print a+d}'`>
- Verdict: <approve | needs-attention | error>
- Findings: <count>
- codex-review-log row: <added: date + branch | pending: closing PR>

## Did

- **Candidate identity.** Tag `587415e`. Preview build `7e7cb07` carries the tag's application code (the diff is docs-only). EF blobs, `config.toml` and the lockfile are identical at tag and tip.
- **Step 0 finding.** DEV's `email_logs` Slack trigger posts to PROD's `send-slack-notification`. Evidence: the tracked source, plus live DEV `posts_to_prod = true` on 31 Aug.
  - Isolation items 1–4 prepared (`session2-step0-isolation.md`), with the trigger-source confirmation and 403 proof (b).
- **DEV sheets for Michael, all in the scratchpad; Michael runs everything:**
  - command sheet with run order
  - query pack A–F
  - procedures with a side-effect table
  - DEV-isolation sheet G1–G7 plus A1–A3 (prepare only)
- **Job-report EF tests.** `node --test` for generate-job-report-pdf at the tag: 5/5 on Node v24.20.0.
- **S-Q1 never-settles.** Characterization 10/10 green; the L-S2 contract test is red at the tag; both independently re-run. Committed to `docs/repro/`.
- **Browser phase 1, 375px on DEV:**
  - H+C tile renders (count 1, MRC-2026-0002), no horizontal scroll.
  - S form opens without a lead status change; Save button is 56 px.
  - Backend was DEV only; no forbidden call.
- **Decisions 1-2** analysed (analysis only). Decision 1 rulings recorded. PROD reads prepared for Michael.
- **Docs commit on `integration/2026-09-15`:**
  - this log, as `docs/sessions/2026-09-15-integration-2026-09-15-2.md`
  - `docs/repro/` ×3
  - `docs/decisions/2026-09-15-send-email-idempotency.md`
  - `docs/TODO.md`: P2-55/56/57, P0-6 third link, L-K2 item 6, L-S2 repro references

## Did NOT

- Apply, deploy, push, or run anything against DEV or PROD. No Supabase CLI or MCP call. No application code changed.
- **NOT VERIFIED (not deferred): browser surfaces for I, J+K and E.** They need Michael's DEV deploys and Step 0 isolation first.
  - I: inspection regenerate, hard-save, P0-A proof.
  - J+K: job-report regenerate.
  - E: suppressed-row UI and send path.
- **NOT VERIFIED LIVE: the S double-save on DEV.** It is covered by the unit's tests and save-race's characterization tests; an optional later DEV pass can check it.
- **C-F1 not reproduced live** (the query was cached). It stays a static CONFIRMED finding.
- No re-measure of tsc or full Vitest; both are inherited from Session 1 Step 2 at `587415e`.
- `gitnexus detect_changes` not run: docs-only commit, no symbol edited.
- No Codex review: no application code.

## Broke

- **Nothing known.**
- **Two CC errors, both corrected before Michael ran anything:** G7 "executing tonight" labels, and TODO rows with line breaks inside table cells.
- **One CC brief defect cost a browser run:** the storage reset ran after SSO and likely wiped Michael's login.

## Open — for Michael

- **Step 0.** A4 first; tell CC "match" or "no match", which CC records here and on P0-6. Then follow the run order at the top of `session2-dev-commands.md`.
- **PDF-CL17 Step A before 17 Sep 09:58 UTC** (`decision-2-prod-reads.sql`).
- **Decision 1 PROD reads Q1–Q4.** The rulings are provisional on them. Ruling 6's framer premise also needs a decision: the framer calls Resend directly.
- **Scratchpad at risk.** The scratchpad sheets live under `/private/tmp/…`, which a reboot clears. Only `docs/repro/`, `docs/decisions/` and this log are committed.
- **`~/mrc-verify`** (detached at the tag; its two scratch tests are now copied to `docs/repro/`): remove when convenient.
- **Push** `integration/2026-09-15`: one docs commit on top of `b1660c9`.
- **Browser phase 2 (I, J+K, E)** after the DEV deploys. The Playwright window is still logged in.

## Resume from here

<!-- resume:start -->
<!-- hook-maintained by .claude/hooks/session-resume.sh after every turn; do not hand-edit between the markers -->

- Updated: 2026-09-15 19:33 AEST · Tool: CC
- Branch: work-main @ dedd0a3 Merge pull request #172 from michaelyoussef396/docs/monitoring-intake-11sep
- Unpushed commits: none
- Uncommitted files (this log excluded):
  - `?? docs/sessions/2026-09-07-chore-workflow-scaffolding.md`
  - `?? docs/sessions/2026-09-08-chore-workflow-scaffolding-3.md`
  - `?? docs/sessions/2026-09-08-chore-workflow-scaffolding-4.md`
  - `?? docs/sessions/2026-09-08-chore-workflow-scaffolding-5.md`
  - `?? docs/sessions/2026-09-08-docs-merge-commit-review-gate-2.md`
  - `?? docs/sessions/2026-09-08-docs-merge-commit-review-gate-3.md`
  - `?? docs/sessions/2026-09-08-docs-merge-commit-review-gate.md`
  - `?? docs/sessions/2026-09-11-chore-mark-invoices-migration-applied.md`
  - `?? docs/sessions/2026-09-15-work-main-2.md`
  - `?? docs/sessions/2026-09-15-work-main.md`
  - `?? muti-session-docs/`
  - `?? template-backup-66282.html`
  - `?? template-backup-66325.html`
- Last step-log line: 19:3x · CC · CC · classification of the offline observation · (none) · the two zeroing tiles come from `src/hooks/useAdminDashboardStats.ts`, which the candidate does not touch (`git diff --stat dedd0a3 587415e` shows only `AdminDashboard.tsx` +2 for H+C; last commit to the hook `91dd58f`, 2026-07-28). The hook is a mount-time `useEffect` fetch with initial state 0 (`:31`, `:42`) and a console-only catch (`:201-202`): PRE-EXISTING silent zero, outside the candidate, contrary to the F/C rule. "No recovery after 3 s" is a short observation window, not proof of no recovery. C-F1 itself remains a static CONFIRMED finding (L-C2), unreproduced live because of cache.
- Codex threads: none
- Window: five_hour 100% used, resets 22:10 AEST (12:10 UTC)
- Next step: the first open item under "Did NOT" or "Open", else continue from the last step-log line.
<!-- resume:end -->
