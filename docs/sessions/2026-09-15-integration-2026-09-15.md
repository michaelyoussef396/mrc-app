# Session log — 2026-09-15 — integration/2026-09-15

## Header

- Date: 2026-09-15 (Tue)
- Lane: Session 1 — integration + review. Merges and reviews only; no application code authored.
- Branch: integration/2026-09-15
- Worktree: /Users/michaelyoussef/mrc-integration (created 2026-09-15 off `dedd0a3`)
- Tool: CC
- Model: claude-opus-5[1m] xhigh until ~16:30. **claude-fable-5-1, effort xhigh from Michael's rulings (16:30–16:43)** onward. Review agents: `fable`, effort `max`, set per agent in the workflow. The main loop plays the manager role.
- Baseline commit: `dedd0a3` (= `origin/main` after `git fetch origin`, 2026-09-15). `origin/production` = `bd0e984`.
- Starting tsc error lines: **100** error lines, **41** distinct after stripping `(line,col)`. Measured in this tree at `dedd0a3`, Node v24.20.0, 16:24.
- Starting test count: **79 files / 1315 tests, 1314 pass, 1 fail** (T24, named below). Measured in this tree at `dedd0a3`, Node v24.20.0, inline dummy env, 16:24.
- Session id: c330f1a7-c2b4-424b-8898-9dd105f0ee58
- Pointer stub: `~/mrc-app-1/docs/sessions/2026-09-15-work-main.md` (hook-created, untracked) points here.

## Intent

Build one integration candidate off `dedd0a3` from the six Codex-window branches in WEEK_STATE order (S > I > J+K > T > E > H+C). One code-reviewer pass per unit, then tsc + full Vitest after every merge, then tag `candidate/2026-09-15-<sha>` locally.

**STATUS 18:10 (final):** T reverted (`587415e`), Step 2 green on it, tag `candidate/2026-09-15-587415e` created (old tag retained). Michael's rulings applied to the tracker, ledger, AGENTS.md, CLAUDE.md and CODEX_WORKFLOW.md and verified read-only; one docs commit follows on top of the tag. Michael pushes branch + both tags; this session ends. Production is still gated on Michael's read of the eleven confirmed findings.

## Rulings — Michael, received between 16:30 and 16:43

- **Model:** session on Fable 5.1 xhigh. Reviews are the production gate for Codex-authored code, so reviews run at max effort. Pinned `opus` in agent definitions must be changed to `fable`/`inherit`. **Checked:** no `model:` line in `.claude/agents/code-reviewer.md`, `.claude/agents/doc-reviewer.md`, or `~/.claude/agents/{code,doc}-reviewer.md`. `general-purpose` is built in, with no file. No subagent default model in settings or env. Nothing to edit. Per-agent effort is available in the workflow (`model: 'fable', effort: 'max'`), so the main-thread `/effort` stays xhigh.
- Manager on the main thread and a disprover per finding (read-only, runs nothing): agreed.
1. **Session H: OUT of this candidate.** All four refs stay parked behind P0-2. `docs/codex-log-session-h` lands with Session H, not separately. Any P2-39(b) or P0-8 lane waits behind P0-2 too. When Session H lands, it is rebased onto the shipped SHA and re-baselined.
2. **S:** review `8e0e695` + `e15c185` together as one unit, 53 source lines, both SHAs in the log entry. S's table entry corrected to 53 (Payload below).
3. **H+C:** the two code-reviewer passes are its completed review. Michael attached a send-email instruction to "both passes": check the migration-before-deploy ordering hazard on the `suppressed` CHECK, whether a constraint failure is surfaced or swallowed, and treat "fail vs log-and-continue" as bring-to-Michael. **H+C is the stalled-inspection hook and card, not send-email. The send-email EF is unit E (`4c81afa`), so the instruction was applied to E's review as a mandatory check.** H and C got the stalled-inspection brief. Flagged to Michael.
4. `128653e` rides in the candidate; doc-reviewer pass.
5. **TODO.md line 1:** fix now, on this branch, as its own one-line docs commit before any merge; explicit path add. Done: `200b462`.
6. **Disk:** delete nothing. Produce the worktree table before Step 2; leave room for Session 2's Chromium.
- Owner message: does not mention Session H or the 94 leads (Michael raises P0-2 with Vryan directly).

## Routing (restated 16:43)

| Work | Tool | Model · effort | Concurrency |
|---|---|---|---|
| Manager | this CC main loop | Fable 5.1 · xhigh | — |
| TODO fix, merges, per-merge tsc/Vitest, Step 2, tag, disk table | main loop, Bash, no sub-agent | Fable 5.1 · xhigh | sequential, this worktree |
| Code review, 8 units (S pair, I, J, K, T, E, H, C) | workflow `wf_1970a435-17d` → `code-reviewer`, read-only | fable · max | ≤4 concurrent (in-script limiter) |
| Doc review `128653e` | same workflow → `doc-reviewer`, read-only | fable · max | same pool |
| Disprover per finding | same workflow → `general-purpose`, read-only, runs nothing | fable · max | same pool |
| Staff message draft (Glen / Clayton / Vryan) | main loop, after reviews land | Fable 5.1 · xhigh | — |

## Touching

- this log
- `docs/TODO.md` line 1: committed `200b462`
- merge commits on `integration/2026-09-15`
- `docs/codex-review-log.md`, only as the S × H+C conflict resolution at the H+C merge (keep both rows)

## Step log

- before 16:24 · CC · CC · read WEEK_STATE_2026-09-12, CODEX_WEEK_PLAN, CODEX_AUTHOR_BRIEF, TODO (all 405 lines) · (none) · done
- before 16:24 · CC · CC · `git fetch origin`; `origin/main`=`dedd0a3`, `origin/production`=`bd0e984` · (none) · matches brief
- before 16:24 · CC · CC · process check: no `claude`/`codex` CLI processes running (only the Claude desktop app) · (none) · worktrees quiet
- before 16:24 · CC · CC · Step 0 dry run (`git merge-tree --write-tree`; simulated chain commits via `commit-tree`, no refs written) · (none) · output below
- before 16:24 · CC · CC · Session H coverage check · (none) · NOT covered, see Session H
- before 16:24 · CC · CC · `git worktree add -b integration/2026-09-15 ~/mrc-integration dedd0a3` · new worktree · clean
- before 16:24 · CC · CC · `npm ci` in `~/mrc-integration` on Node v24.20.0 · node_modules · clean, typescript 5.8.3 == pinned
- 16:24 · CC · CC · baseline tsc (16:24:25–16:24:39) + full Vitest (16:24:29–16:24:40) · (none) · 100 / 41; 79 files / 1315, T24 only
- 16:27 · CC · CC · provenance + net line split per branch · (none) · see Payload
- 16:27 · CC · CC · report to Michael: routing, Session H collision risk, batched decisions · this file · STOPPED
- 16:30–16:43 · CC · CC · rulings received; checked agent `model:` pins · (none) · nothing pinned, no edit
- 16:30–16:43 · CC · CC · worktree disk table, background, read-only · scratchpad only · done
- 16:30–16:43 · CC · CC · TODO.md line 1: stripped the OSC 633 prefix with perl (bypasses format-on-save), verified numstat 1/1, 404 == 404 lines, no ESC/BEL left; `git add docs/TODO.md`; commit · docs/TODO.md · `200b462`
- 16:30–16:43 · CC · CC · confirmed all six review targets at expected tips and clean · (none) · OK
- 16:43 · CC · CC · launched review workflow `wf_1970a435-17d` (9 reviews + disprovers, ≤4 concurrent, fable/max) · (none) · running
- 16:43 · CC · CC · merge 1/6 S: `git merge --no-ff --no-commit fix/inspection-save-integrity`, then tsc set-diff vs `200b462` (docs-only over `dedd0a3`, so the `dedd0a3` measurement stands) + full Vitest · merge · `d9bcbf8`, 0 new tsc, no new failures
- 16:43–16:53 · CC · CC · merge 2/6 I, same procedure · merge · `c0958bd`, 0 new tsc, no new failures
- 16:43–16:53 · CC · CC · merge 3/6 J+K, same procedure, plus `node --test` · merge · `10680e8`, 0 new tsc, no new failures, node --test 5/5
- 16:43–16:53 · CC · CC · merge 4/6 T, same procedure · merge · `8014bd9`, 0 new tsc, no new failures
- 16:43–16:53 · CC · CC · merge 5/6 E, same procedure · merge · `45cf400`, 0 new tsc, no new failures
- 16:43–16:53 · CC · CC · merge 6/6 H+C: resolved the one-hunk review-log conflict (markers only, both rows), then same procedure · merge + docs/codex-review-log.md · `077d018`, 0 new tsc, no new failures, 85/1389
- 16:53 · CC · CC · Step 2 on `077d018` (16:53:11–16:53:50): npm ci, Vitest, tsc, build, node --test · node_modules, dist/ (ignored) · green
- 16:54 · CC · CC · Step 3: annotated local tag `candidate/2026-09-15-077d018` → `077d018` · tag · created, not pushed
- 16:55 · CC · CC · pre-push PII scan of the 2951 lines added between `dedd0a3` and `077d018`: emails, AU phones, street addresses, honorifics, "customer/client + Name", full capitalised-token list · (none) · clean. Only fixtures appear (`customer@example.com`, `recipient@example.invalid`, `Test Customer`, `0400000000`, `1 Test Street`, suburb `Coburg` as on main's existing `LeadsManagement.updateLeadStatus.test.tsx` fixture). Person names are only Michael and Claude. Lead/job numbers only: MRC-2026-0124/0127/0130/0136/0168, JOB-2026-0013.
- 16:56 · CC · CC · review progress: S, I, J, K done; T, E, H, C running; DOC queued · (none) · waiting
- 17:01 · Michael · — · pushed `integration/2026-09-15`; verified with `git ls-remote`: origin branch = `077d018` = local HEAD. Tag `candidate/2026-09-15-077d018` not yet on origin · (none) · branch pushed
- 17:01:46 · CC · CC · Michael reported "Pushed both". Re-checked with `git ls-remote --tags origin 'candidate/*'`: **no candidate tag on origin**, exit 0. The local tag exists (tag object `b5f1031` → `077d018`). Session 3 cutting from the local tag is unaffected, since worktrees share refs. Reported to Michael · (none) · tag push not visible
- 17:02:20 · Michael · — · pushed tag; verified with `git ls-remote`: origin `refs/tags/candidate/2026-09-15-077d018` = `b5f1031` (local tag object), peeled `^{}` = `077d018` · (none) · branch and tag both on origin, matching local
- 17:01 · Michael · — · format for the batch: one row per item (unit, claim, disprover verdict, failing test), duplicates merged, sorted by unit. No candidate verdict until all nine reviews are logged. Session 3 is cut from the tag and launches no sub-agents until this workflow reports finished · (none) · recorded
- 17:29 · CC · workflow `wf_1970a435-17d` · finished: 22 agents, 0 errors · (none) · results in journal.jsonl
- 17:30 · CC · CC · extracted 9 reviews + 13 verdicts from the journal into the scratchpad; first pass mis-keyed T and E (both `unit` strings begin "Unit"), re-extracted by finding-id prefix · scratchpad only · complete
- 17:30–17:36 · CC · CC · read all 22 results in full; wrote Reviews (findings with verdicts and failing tests, E mandatory check, 36 deduplicated questions, candidate verdict, owner-message draft) into this log · this file · done
- 17:36 · CC · CC · process check before handoff: no `claude`/`codex` CLI processes besides this session's own harness · (none) · quiet
- 17:37 · CC · CC · commit this log as a docs-only commit on `integration/2026-09-15` (explicit path); tag stays on `077d018` · this file · see reply for SHA
- 17:37 · CC · CC · committed this log · this file · `481dcf9`
- 17:3x–17:4x · Michael · — · rulings received (recorded under Round 2 below); one more unit of work: revert T, re-run Step 2, retag, apply rulings in one docs commit, adjust the owner message, then push and end · (none) · recorded
- 17:4x · CC · CC · `git revert -m 1 8014bd9` with the ruled message · 4 files back to `dedd0a3` · `587415e`
- 17:52 · CC · CC · Step 2 on `587415e` (17:52:12–17:52:50): npm ci, Vitest, tsc, build, node --test · node_modules, dist/ · green, figures under Round 2
- 17:52 · CC · workflow `wf_58a0796e-b29` · launched: three editors (ledger, TODO, rules) on disjoint files + one read-only verifier, fable/high (verifier max) · docs/codex-review-log.md, docs/TODO.md, AGENTS.md, CLAUDE.md, docs/CODEX_WORKFLOW.md · running
- 17:55 · CC · CC · WEEK_STATE row T annotated (T-Q5) in `~/mrc-plan` on `docs/week-2-plan` · docs/WEEK_STATE_2026-09-12.md · `d6ef1ab`, not pushed
- 17:56 · CC · CC · tag `candidate/2026-09-15-587415e` → `587415e`; `candidate/2026-09-15-077d018` retained · tag · created, not pushed
- 17:56 · CC · CC · S-Q5: corrected the S log's Touching filename and the `.ai/` step line · docs/sessions/2026-09-09-fix-inspection-save-integrity-4.md · 2 lines
- 17:56 · CC · CC · wrote Round 2 into this log · this file · done
- 18:04 · CC · workflow `wf_58a0796e-b29` · finished: 3 editors + verifier, 0 errors; verifier PASS on all six checks with four items (K-Q7 owner line, T2 label vs tracker ID T2, CODEX_WORKFLOW.md still restating the figure + stale command/base lines, editor choices) · docs/codex-review-log.md, docs/TODO.md, AGENTS.md, CLAUDE.md, docs/CODEX_WORKFLOW.md · verified
- 18:09 · CC · CC · dispositions: CODEX_WORKFLOW.md ruling 4 and §4.2 made pure pointers (no number), its stale single-total command replaced by a pointer, §4.3/§5 accept a declared same-branch base, §4.7 announces source/test + file count; AGENTS.md Workflow bullets point at the one copy; CLAUDE.md steps 1 and 4 take `<base>`; CLAUDE.md step-2 file count no longer excludes docs/sessions/ (the companion counts every file); TODO: unit-label vs tracker-ID note, T2 row's WEEK_STATE half marked done; owner draft adjusted (timeline item dropped, item 2 ending as ruled, K-Q7 sentence bracketed) · same files + this log · done
- 18:10 · CC · CC · re-checks: `150` outside gitnexus blocks appears only in AGENTS.md's Diff-limit bullet plus two historical mentions (CODEX_WORKFLOW.md:12 "~150 was judged by eye", :101 "PR #150"); no `per PR`; ledger 55 rows all 14 fields; TODO line 1 clean; PII scan of added lines: one phone-shaped token, pre-existing in a moved ledger row · (none) · clean
- 18:10 · CC · CC · docs commit, explicit paths (AGENTS.md, CLAUDE.md, docs/CODEX_WORKFLOW.md, docs/TODO.md, docs/codex-review-log.md, the S log, this log) · those files · see reply for SHA

## TODO.md line 1 — cause (commit `200b462`)

- Bytes removed from the start of line 1: `ESC ]633;E;{ sed -n '1,173p' docs/TODO.md\x3b cat /tmp/p230.txt\x3b cat /tmp/p231.txt\x3b sed -n '179,$p' docs/TODO.md\x3b } > /tmp/todo.new && mv /tmp/todo.new docs/TODO.md;6511fd6c-32b1-4b82-9893-0b1bc4ed1423 BEL ESC ]633;C BEL`. Line 1 is now `# MRC TODO — working tracker`.
- These are shell-integration markers (633;E = command line, 633;C = executed) for the grouped `sed … > /tmp/todo.new && mv` rewrite. They were captured inside the redirected output, so the command wrote itself into the file.
- **Introducing commit: `6bb528e`** (2026-09-09 16:55, "merge: resolve TODO.md conflict, keep P2-30 and P2-31"). That command inserted `/tmp/p230.txt` and `/tmp/p231.txt`, the P2-30/P2-31 rows. Its parent `399e98c` is clean (first byte `#`). The bytes were carried unchanged through `ecfc4d0`, `9f0f30d`, `14bc057` and `dedd0a3`.
- Michael's ruling attributed it to the session that produced `9f0f30d`. The byte history shows `9f0f30d` inherited it; the mechanism is as he described. Flagged.

## GitNexus

`detect_changes` is not run on these commits. `list_repos` shows only `~/.gitnexus/repos/mrc-app` (indexed 2026-05-20) and `~/mrc-cost-estimate`; neither can see `~/mrc-integration`. No symbol is edited by this session.

## Baseline

### Runtime (verbatim)

```
$ pwd && git branch --show-current
/Users/michaelyoussef/mrc-integration
integration/2026-09-15
$ source ~/.nvm/nvm.sh && nvm use 24
Now using node v24.20.0 (npm v11.19.0)
$ node --version; which node        # separate check after nvm use
v24.20.0
/Users/michaelyoussef/.nvm/versions/node/v24.20.0/bin/node
```

Default PATH outside that shell resolves `/opt/homebrew/bin/node` = **v23.7.0**, so every later command uses `/Users/michaelyoussef/.nvm/versions/node/v24.20.0/bin/node` by absolute path. Each measurement command printed `v24.20.0` before running.

### Install

- `npm ci` (npm 11.19.0): completed; only `install-scripts` allowScripts warnings. `git status --porcelain` empty afterwards (lockfile not rewritten).
- TypeScript installed 5.8.3 == pinned 5.8.3.
- Disk after install: 7.6 GiB free (96% used). Each further `npm ci` costs ~0.85 GiB.

### tsc at dedd0a3

```
$ node ./node_modules/typescript/bin/tsc -p tsconfig.app.json --noEmit
tsc exit=2
error TS lines: 100
normalised lines: 100   distinct normalised: 41
```

Matches WEEK_STATE ("100 existing diagnostic lines/41 distinct messages"). Files carrying errors: LeadBookingCard.tsx 5, invoices.ts 4, inspections.ts 4, TechnicianInspectionForm.tsx 3, InvoiceSummaryCard.tsx 3, and two or fewer each in ViewReportPDF.tsx, TechnicianJobDetail.tsx, LeadsManagement.tsx, LeadDetail.tsx, useGoogleMaps.ts, CreateNewLeadModal.tsx, htmlToPdf.ts, SyncManager.ts, fieldEditLog.ts, apiClient.ts, apiClient.test.ts, PhotoCollectionEditor.tsx, JobCompletionSummary.tsx, BookJobSheet.tsx, App.tsx, `api/render-job-report-pdf.ts` (T13 TS2339).

Gate for every merge: normalised error lines set-compared (`comm`) against the previous merge, measured fresh in this tree. If the session dies, re-measure the last green merge commit (14 s).

### Vitest at dedd0a3 (inline `VITE_SUPABASE_URL=https://localhost.invalid VITE_SUPABASE_ANON_KEY=dummy`)

```
 Test Files  1 failed | 78 passed (79)
      Tests  1 failed | 1314 passed (1315)
   Duration  10.93s
FAIL: src/lib/api/reportPipeline.test.ts > fetchVersionPdfBlob returns the file from the signed URL when it resolves | TypeError: blob.text is not a function
```

The only failure is T24 (Node-24-only). WEEK_STATE's post-merge expectation is 85 files / 1389 tests. The six branches add exactly six new Vitest files (`concurrentSave`, `versionHistory`, `sendEmail.suppression`, `ActivityTimeline`, `useStalledInspections`, `StalledInspectionsCard`), so 79 + 6 = 85 is consistent. J+K's `index.test.mjs` is a separate `node --test`.

### Step 0 — dry run, verbatim (git 2.50.1)

**A. each branch vs dedd0a3**

```
CLEAN     dedd0a3 x fix/inspection-save-integrity
CLEAN     dedd0a3 x fix/pdf-versions-generation-type
CLEAN     dedd0a3 x fix/job-report-pdf-memory
CLEAN     dedd0a3 x fix/activity-timeline-attribution
CLEAN     dedd0a3 x fix/send-email-cooldown-logging
CLEAN     dedd0a3 x feat/stalled-inspection-visibility
CLEAN     dedd0a3 x fix/lead-list-true-count
CLEAN     dedd0a3 x fix/lead-list-pagination
CLEAN     dedd0a3 x feat/lead-id-search
CONFLICT  dedd0a3 x docs/codex-log-session-h (rc=1)
    docs/codex-review-log.md
    Auto-merging docs/codex-review-log.md
    CONFLICT (content): Merge conflict in docs/codex-review-log.md
```

**B. cumulative chain, WEEK_STATE order S > I > J+K > T > E > H+C**

```
CLEAN     chain + S
CLEAN     chain + I
CLEAN     chain + JK
CLEAN     chain + T
CLEAN     chain + E
CONFLICT  chain + HC (rc=1)
    docs/codex-review-log.md
    Auto-merging docs/codex-review-log.md
    CONFLICT (content): Merge conflict in docs/codex-review-log.md
simulated six-branch tip tree: fedbe4a516c130449e59616c6627830f7bed55d0
```

**C. pairwise.** The first run indexed a bash-style array from 0 under zsh (1-indexed), so its nine `rc=129` rows had an empty first ref and `docs/codex-log-session-h` was never paired. Those rows are discarded; C′ is the redo. The 36 valid pairs:

```
CLEAN     fix/inspection-save-integrity x fix/pdf-versions-generation-type
CLEAN     fix/inspection-save-integrity x fix/job-report-pdf-memory
CLEAN     fix/inspection-save-integrity x fix/activity-timeline-attribution
CLEAN     fix/inspection-save-integrity x fix/send-email-cooldown-logging
CONFLICT  fix/inspection-save-integrity x feat/stalled-inspection-visibility (rc=1)
    docs/codex-review-log.md
    CONFLICT (content): Merge conflict in docs/codex-review-log.md
CLEAN     fix/inspection-save-integrity x fix/lead-list-true-count
CLEAN     fix/inspection-save-integrity x fix/lead-list-pagination
CLEAN     fix/inspection-save-integrity x feat/lead-id-search
CLEAN     fix/pdf-versions-generation-type x fix/job-report-pdf-memory
CLEAN     fix/pdf-versions-generation-type x fix/activity-timeline-attribution
CLEAN     fix/pdf-versions-generation-type x fix/send-email-cooldown-logging
CLEAN     fix/pdf-versions-generation-type x feat/stalled-inspection-visibility
CLEAN     fix/pdf-versions-generation-type x fix/lead-list-true-count
CLEAN     fix/pdf-versions-generation-type x fix/lead-list-pagination
CLEAN     fix/pdf-versions-generation-type x feat/lead-id-search
CLEAN     fix/job-report-pdf-memory x fix/activity-timeline-attribution
CLEAN     fix/job-report-pdf-memory x fix/send-email-cooldown-logging
CLEAN     fix/job-report-pdf-memory x feat/stalled-inspection-visibility
CLEAN     fix/job-report-pdf-memory x fix/lead-list-true-count
CLEAN     fix/job-report-pdf-memory x fix/lead-list-pagination
CLEAN     fix/job-report-pdf-memory x feat/lead-id-search
CLEAN     fix/activity-timeline-attribution x fix/send-email-cooldown-logging
CLEAN     fix/activity-timeline-attribution x feat/stalled-inspection-visibility
CLEAN     fix/activity-timeline-attribution x fix/lead-list-true-count
CLEAN     fix/activity-timeline-attribution x fix/lead-list-pagination
CLEAN     fix/activity-timeline-attribution x feat/lead-id-search
CLEAN     fix/send-email-cooldown-logging x feat/stalled-inspection-visibility
CLEAN     fix/send-email-cooldown-logging x fix/lead-list-true-count
CLEAN     fix/send-email-cooldown-logging x fix/lead-list-pagination
CLEAN     fix/send-email-cooldown-logging x feat/lead-id-search
CLEAN     feat/stalled-inspection-visibility x fix/lead-list-true-count
CLEAN     feat/stalled-inspection-visibility x fix/lead-list-pagination
CLEAN     feat/stalled-inspection-visibility x feat/lead-id-search
CLEAN     fix/lead-list-true-count x fix/lead-list-pagination
CLEAN     fix/lead-list-true-count x feat/lead-id-search
CLEAN     fix/lead-list-pagination x feat/lead-id-search
```

**C′. docs/codex-log-session-h vs the other nine**

```
CONFLICT  docs/codex-log-session-h x fix/inspection-save-integrity        (docs/codex-review-log.md)
CONFLICT  docs/codex-log-session-h x fix/pdf-versions-generation-type     (docs/codex-review-log.md)
CONFLICT  docs/codex-log-session-h x fix/job-report-pdf-memory            (docs/codex-review-log.md)
CONFLICT  docs/codex-log-session-h x fix/activity-timeline-attribution    (docs/codex-review-log.md)
CONFLICT  docs/codex-log-session-h x fix/send-email-cooldown-logging      (docs/codex-review-log.md)
CONFLICT  docs/codex-log-session-h x feat/stalled-inspection-visibility   (docs/codex-review-log.md)
CLEAN     docs/codex-log-session-h x fix/lead-list-true-count
CLEAN     docs/codex-log-session-h x fix/lead-list-pagination
CLEAN     docs/codex-log-session-h x feat/lead-id-search
```

Those six conflicts are one conflict: the log branch already conflicts with `dedd0a3`, and each of the six contains `dedd0a3`.

**Conflict hunks**

- S × H+C, `docs/codex-review-log.md`: both branches append one row at the same insertion point, after the 2026-09-09 `mrc-sentry-vis` row. S adds the `8e0e695` row (219 reviewable, 45 production / 174 test); H+C adds the `0f9d9ee` row (157 reviewable). Resolution when merging H+C: keep both rows, S's first. Docs only.
- `dedd0a3` × `docs/codex-log-session-h`: one hunk at lines 11–17 of the merged file (empty ours vs the branch's rows). Docs only.

**Summary: zero source-file conflicts anywhere among the ten refs.** Every textual conflict is `docs/codex-review-log.md`.

## Payload — what each merge actually carries

Net vs `dedd0a3`; docs exclude `docs/sessions/`.

| Branch | Commits (no merges) | Net source | Net test | Docs |
|---|---|---|---|---|
| S `fix/inspection-save-integrity` (**one review unit, 53 source, ruling 2**) | `8e0e695` impl, `5848ed5` docs, `814fb9d` logs, `e15c185` impl | **53** | 245 | 190 |
| I `fix/pdf-versions-generation-type` | `288a6db` impl, `1b407f2` + `8a2fc99` logs | 18 | 85 | 0 |
| J+K `fix/job-report-pdf-memory` | `227cd2e` impl, `8f78e7d` log, `b86fb5c` impl | 62 | 87 | 0 |
| T `fix/activity-timeline-attribution` | `4d2594c` impl, `8aefa5f` log | 22 | 80 | 0 |
| E `fix/send-email-cooldown-logging` | `4c81afa` impl (incl. new migration `20260912000000_email_logs_suppressed_status.sql`), `54c40ca` log | 46 | 92 | 0 |
| H+C `feat/stalled-inspection-visibility` | `ed9254d` impl, `a1bc597` impl, `128653e` docs | 151 | 214 | 3 |

Per commit (source / test): `8e0e695` 45/174 · `e15c185` 8/269 · `288a6db` 18/85 · `227cd2e` 58/83 · `b86fb5c` 14/12 · `4d2594c` 22/80 · `4c81afa` 46/92 · `ed9254d` 94/108 · `a1bc597` 57/106 · `128653e` 3 docs. **No commit exceeds 150 SOURCE lines.** `e15c185`, `ed9254d` and `a1bc597` exceed 150 only when test lines are counted.

Provenance (from the branch logs):

- `8e0e695` — **CC** (claude-opus-5 high, manager), 9 Sep. Codex-reviewed with a 219-line waiver granted before the run.
- H+C — **CC** code from 9 Sep (`be78c19`; Codex round 1 at `0f9d9ee`; round 2 never ran, 365 vs 260 cap, Michael ruled no raise). **Codex split it** into `ed9254d` + `a1bc597` on 12 Sep ("Split the existing implementation; do not rebuild it"). Ruling 3: the two code-reviewer passes are its completed review.
- I, J, K, T, E — Codex (GPT-6 / gpt-6-astra), 12 Sep window.
- `128653e` — adds one `AGENTS.md` rule ("A review cannot be path-scoped. The cap is managed by splitting commits…") plus two `docs/codex-review-log.md` rows; per the H+C log, Michael's 9 Sep ruling. Rides in the candidate (ruling 4).

## Session H — coverage check (brief gate)

**RULED 16:30–16:43: OUT of this candidate.** All four refs are parked behind P0-2. `docs/codex-log-session-h` lands with Session H. P2-39(b) and P0-8 lanes wait behind P0-2. When Session H lands, it is rebased onto the shipped SHA and re-baselined.

**Verdict: WEEK_STATE does NOT cover the Session H branches.** It never names any of the four. Its reconciliation covers 8 Sep onward; the Session H logs are 5–6 Sep. CODEX_WEEK_PLAN Part 4 holds P0-2/P1-15 as work but does not account for the branches that already exist.

| Ref | Tip | Pushed | Base | Files |
|---|---|---|---|---|
| `fix/lead-list-true-count` | `7784488` | yes, same SHA | `61f77e9` | `LeadsManagement.tsx` +114/−49, `PipelineTabs.tsx` +17/−14 |
| `fix/lead-list-pagination` | `0635ecd` | yes | `61f77e9`, contains true-count | adds `LeadsPagination.tsx` +84; `LeadsManagement.tsx` +164/−95 cumulative |
| `feat/lead-id-search` | `8b5939f` | yes | `61f77e9` | `leadSearch.ts` +26/−10, test +72/−13 |
| `docs/codex-log-session-h` | `0dc78a5` | yes | `a6fc2b3` | `docs/codex-review-log.md` +89 |

Collision risk as reported:

1. **Textual, source: none.** No shared source file with any of the six.
2. **Textual, docs:** `docs/codex-log-session-h` conflicts with main itself.
3. **Operational:** the P0-2 gate (Vryan conversation; about 94 uncontacted leads surface at once) is the only thing stopping a clean merge.
4. **Downstream:** P2-39(b)'s four writers and P0-8's archive handler live in `LeadsManagement.tsx`, which Session H rewrites (about 260 lines).
5. **Staleness:** 10 days old off `61f77e9`; never run against current main.

## Reviews — workflow `wf_1970a435-17d` (launched 16:43)

Per unit: reviewer = `code-reviewer` (or `doc-reviewer`), `fable`, effort `max`, read-only, no execution. One `general-purpose` disprover per finding, same model and effort, read-only, runs nothing. Findings are batched to Michael and fix nothing. A finding blocks production, not the candidate merge.

Workflow finished 17:29 (launched 16:43, 46 min): 22 agents (9 reviews + 13 disprovers), 0 errors, 588 tool uses, 3.26M sub-agent tokens. Raw structured results: `~/.claude/projects/-Users-michaelyoussef-mrc-app-1/c330f1a7-c2b4-424b-8898-9dd105f0ee58/subagents/workflows/wf_1970a435-17d/journal.jsonl` (one `result` line per agent). Nothing below was fixed, and nothing was run by any agent: every claim is a static read.

| Unit | SHA(s) | Source/test | Verdict | Findings (disprover verdict) | Questions for Michael |
|---|---|---|---|---|---|
| S | `8e0e695` + `e15c185` | 53 / 245 | findings | S-F1 low (CONFIRMED) | 5 |
| I | `288a6db` | 18 / 85 | findings | I-F1 high (CONFIRMED) | 3 |
| J | `227cd2e` | 58 / 83 | findings | J-F1 high (CONFIRMED, already recorded, corrected by K) | 4 (all about K) |
| K | `b86fb5c` | 14 / 12 | no findings | — | 5 |
| T | `4d2594c` | 22 / 80 | findings | T-F1 high (CONFIRMED), T-F2 medium (CONFIRMED) | 5 |
| E | `4c81afa` | 46 / 92 | findings | E-F1 low (CONFIRMED), E-F2 low (REFUTED) | 5 |
| H | `ed9254d` | 94 / 108 | no findings | — | 3 |
| C | `a1bc597` | 57 / 106 | findings | C-F1 medium (CONFIRMED), C-F2 low (CONFIRMED) | 3 |
| DOC | `128653e` | 3 docs | findings | F1 medium (CONFIRMED), F2 medium (CONFIRMED), F3 low (CONFIRMED), F4 low (REFUTED) | 5 |

13 findings: 11 CONFIRMED, 2 REFUTED, 0 PLAUSIBLE. None is cross-tenant, secret, auth, or data-loss. Every reviewer confirmed: the session log's Touching list matches the commit's files; no customer PII in the diff; no frozen surface touched; source/test counts match the handover; the recorded test-first/mutation claims are consistent with the assertions (statically; nothing was re-run).

### Findings, per unit (claim · disprover · failing test)

**S-F1 · low · CONFIRMED.** `TechnicianInspectionForm.tsx:4109-4133` take the three pre-write audit snapshots with the closure `currentInspectionId` (null for every save enqueued during the create window), before the latch resolves the real id at `:4223-4225`. Those saves take the UPDATE branch and write real changes, but their snapshots are null/[]/null, so `logSectionMilestone` (`fieldEditLog.ts:224-225`) returns without an `activities` row. Audit trail only; no data loss; not a regression against main (main minted a new row per save and logged nothing either). Disprover confirmed the trace and that the code comment at `:4105-4108` ("first save") does not cover a coalesced save. **Failing test:** in `concurrentSave.test.tsx`, capture `activities` inserts in the mock, run `saveThreePayloads()`, assert an insert with `activity_type === 'section_milestone'` and a change `area_name → 'second'` exists — fails today (no insert). Post-fix the harness also needs `auth.getUser` mocked and the assertion in a `waitFor`. Not recorded anywhere.

**I-F1 · high · CONFIRMED.** The EF allocates `version_number` from `inspections.pdf_version` (`generate-inspection-pdf/index.ts:2287`) and writes that counter before the history insert (`:2354-2361`). The other two writers allocate `MAX(pdf_versions.version_number)+1` and never write the counter (`api/render-pdf.ts:204-215`, `ViewReportPDF.tsx:1528-1545`). Under `unique_inspection_version UNIQUE(inspection_id, version_number)` (`20241221000000:46`), every EF regeneration that follows a hard-save or manual upload inserts a taken number → 23505 → history row lost, counter advanced, 200 with `versionHistorySaved:false`. Pre-existing; masked until now by the 23514 that this unit fixes. P0-A's acceptance ("persisted generation creates a permitted history row") is unmet in the ordinary generate → hard-save → regenerate loop. Disprover confirmed every premise, including that the collision is deterministic and that a fresh inspection whose first write is a hard-save collides on its very first EF generation. **Failing test:** in `generateInspectionPdf.versionHistory.test.ts`, keep `pdf_version: 2`, make the insert mock return `{code:'23505'}` when `row.version_number === 3`, POST `{regenerate:true}`; assert `body.versionHistorySaved === true` (gets false, `databaseCode '23505'`) and `insert` called with `version_number: 4` (gets 3). **DEV verification consequence:** pick an inspection whose `MAX(pdf_versions.version_number) <= inspections.pdf_version`, or the first regenerate returns 23505 and looks like the constraint fix failed; and a pass there does not cover hard-save-then-regenerate — run that sequence explicitly.

**J-F1 · high · CONFIRMED · already recorded and corrected by K (same merge).** At `227cd2e` all three history-failure exits are `throw`s after the Storage upload and before the parent `pdf_url` update, so the catch returns 500 and a complete HTML report is orphaned in Storage while the page shows "No Report Generated". Recorded in WEEK_STATE (J row, contradictions) and the handover ("Post-acceptance decision"); corrected by `b86fb5c` (`:487-490`, `:500-503`, `:508-515`, `:528`). **Failing test:** K's own `index.test.mjs:72-82` against the `227cd2e` handler → `500 !== 200` (recorded 12 Sep red run, 4 fail / 1 pass). Disposition already satisfied: J and K merged together in `10680e8`.

**T-F1 · high · CONFIRMED.** `useActivityTimeline.ts:128-143` resolves names with a direct `profiles ... .in('id', …)` lookup, which under the recorded live policy (`20260825141427_get_staff_names.sql` header: SELECT own-row-only, "for admins too", naming this exact hook) returns only the viewer's row. New line `:151-152` then renders `'User (name unavailable)'` for every cross-user attributed row (field_edit, section_milestone, note_added, invoice_*, job_booked …) on every timeline surface; before `4d2594c` those rows carried no label. The accepted rule's "show the known profile" clause is unmet for all cross-user rows while `get_staff_names` (the RPC `leadNotes.ts` already uses) exists. Disprover confirmed, added that `LeadDetail` is also mounted for technicians at `/technician/job/:id` (`App.tsx:350-356`) with the Activity Log not admin-gated, so technician viewers hit it under any policy; noted `docs/NOTIFICATIONS_SCHEMA_RECONCILIATION.md:176` asserts an admin read policy exists but by migration-file inference only, which the repo's own rule rejects. One Studio read settles the admin half: `select policyname, cmd, qual from pg_policies where tablename='profiles'`. **Failing test:** `ActivityTimeline.test.tsx` style: `activity({ user_id: 'user-1' })`, `profiles` mock returns `[]`, assert `queryByText(/name unavailable/)` is null — fails today, both layouts. Not recorded against this unit; the log treats the label as an edge case (line 44).

**T-F2 · medium · CONFIRMED.** The email_logs select (`useActivityTimeline.ts:101-105`) omits `sent_by` and `:195` hard-codes `actorName: null`, so every email row now renders "Actor not recorded" (`ActivityTimeline.tsx:240`), although `sent_by` is populated on every send path: the user id from the session (`notifications.ts:324-332` → `send-email/index.ts:232`) or the `SYSTEM_USER_UUID` sentinel for framer/reminder sends (`receive-framer-lead:920`, `send-inspection-reminder:253`). The sentinel needs no lookup, so the "System" half does not depend on the profiles policy. New false label; before the diff email rows had none. Disprover confirmed; caveats: `bookingService.ts:315-324` writes one `failed` skip row without `sent_by` (label true there); sentinel writes are `|| null` if the secret is unset. **Failing test:** mount an `email_logs` row with `sent_by: 'a5ae96f1-af3d-4e50-b7ec-1cab01bdec3f'`, assert `'System'` renders — fails today (`Actor not recorded`). The existing `keeps missing email_logs initiators unknown` test enshrines only the no-`sent_by` case.

**E-F1 · low · CONFIRMED.** `send-email/index.ts:193` logs the whole PostgrestError as the third `console.error` argument. For a 23514 the DETAIL is `Failing row contains (…)` with recipient_email, subject, lead_id, sent_by, so a customer address and subject line land in the EF log on every suppressed request during the misordered-deploy window, and on any future row-bearing insert failure. Disprover confirmed the shape (postgrest-js 2.76.1 passes `details` through; service_role bypasses RLS on this table — proven by P0-14's own 429 firing through service-role reads — so Postgres emits the row description). Base code never logged this object. **Failing test:** `setup('recipient', { code:'23514', details:'Failing row contains (…recipient@example.invalid…)' })`, `await send()`, `expect(JSON.stringify(logError.mock.calls)).not.toContain('recipient@example.invalid')` — fails today; same for `setup('hourly', …)`. The test injects `details` itself, so it proves only that `:193` logs the whole object.

**E-F2 · low · REFUTED.** Claim: hourly-cap rejections now write a row per request and fire the Slack/notification fan-out, removing the 100/hour bound on side effects reachable through the unauthenticated relay. Disprover: the mechanics are real (`:224-227` now writes; the fan-out trigger `20260823090000:235-238` exits early only for two template names), but the premise is wrong — on base, an outside caller already gets unbounded fan-out by sending a `from` Resend rejects (`from` is unvalidated, `:34`; 4xx is non-retryable, `:98-101`; the unconditional `:241-253` insert writes `failed`; the trigger fires FAILED/high-priority; `failed` rows are never counted by the hourly gate). No capability is gained. The residue (suppressed rows mislabelled "sent" by the fan-out; whether the hourly gate should log at all) is already recorded and is E-Q2. **New pre-existing observation for P0-5, recorded nowhere:** uncapped `failed`-row Slack/notification fan-out through the relay (E-Q5).

**C-F1 · medium · CONFIRMED.** `StalledInspectionsCard.tsx:18` reads only `{data, isLoading, isError}`. React Query 5.83 with the default `networkMode 'online'`: a fetch started while `onlineManager.isOnline()` is false is `status 'pending'`, `fetchStatus 'paused'`, so `isLoading` and `isError` are both false, and `:48-52` render a neutral grey `0` with no subtitle — byte-identical to a verified zero, for the length of the offline spell. Reachable when the window `offline` event fires in-session before the card's first successful fetch (PWA serves `/admin` offline); a cold offline open rejects → "Unavailable" instead. The card's own comment (`:12-15`) names this collapse as what it exists to prevent; Codex round-1 finding 2 fixed only the rejected route. Disprover confirmed from the library source and the repo's own precedent (`Section4AfterPhotos.tsx:120-125` gates on `fetchStatus === 'paused'`); exposure is narrow (in-session offline + no cached data + `OfflineBanner` dismissed for it to be fully silent) and self-corrects on reconnect. **Failing test:** `onlineManager.setOnline(false)` (use beforeEach/afterEach as in `Section4AfterPhotos.noInspection.test.tsx:225-231`), `respondWith(...)`, `renderCard()`, `expect(screen.queryByText('0')).toBeNull()` — fails today.

**C-F2 · low · CONFIRMED.** The count is `leadNumbers.length` (`:20-22`, `:48`), not the hook's row count; the predicate never checks `lead_number`; `leads.lead_number` is nullable (`20251028135212:272`, `types.ts:1443`), the BEFORE INSERT trigger fills only on insert and the backfill is a commented checklist item (`20251112000003:93-97`). A stalled inspection on a lead with NULL/empty `lead_number` is dropped from the count and the styling. Disprover confirmed; live precondition (any NULL row on PROD) is unverifiable by reading and the currently known stalled set is post-trigger, hence low. **Failing test:** `respondWith({ data: [{ ...stalledRow('MRC-2026-0124'), leads: { lead_number: null, archived_at: null } }] })`, `renderCard()`, `expect(await screen.findByText('1'))` — fails today (renders `0`).

**DOC-128653e-F1 · medium · CONFIRMED.** New `AGENTS.md:67` says two commits reviewed separately can land in one PR "and that is the way out of a cap, not a waiver"; the adjacent `AGENTS.md:66` still says "150 reviewable lines per PR … Above it: split", and `CLAUDE.md:159` (step 2) measures `origin/main...HEAD` (365 at this branch's tip) and demands split-or-waiver. No single action satisfies both for this branch. Disprover: the per-unit reading already dominated (`AGENTS.md:47,136,138`, `CLAUDE.md:140`, brief `:136`), so "redefines" overstates novelty — the defect is that line 66 and CLAUDE.md step 2 were left unamended; also both unit caps (210, 163) exceeded 150, so "not a waiver" is not what the same log records happening. **Check:** `sed -n '66,67p' AGENTS.md`; `sed -n '159p;162p' CLAUDE.md`; numstat `dedd0a3...a1bc597` excl. sessions = 365.

**DOC-128653e-F2 · medium · CONFIRMED.** Ledger row (`docs/codex-review-log.md:102` at `128653e`; `:103` in the integration tree) tallies findings 2 / acted 0 / rejected 0 / unclear 2 and says "carried to Michael untriaged", while the same cell narrates the triage and the session log (`:51`, `:138`) records finding 1 FILED and finding 2 FIXED — both executed in the code that rides in the candidate (`useStalledInspections.ts:58-65` comment; `StalledInspectionsCard.tsx:31-35` error state). Every other Michael-triaged row in the ledger records unclear 0. The ledger is the stated sole input for the "13 false flags to 3 real fixes" record. Disprover confirmed; the row was finalised in `30a7090` after triage and re-landed byte-identically. **Check:** `git show 128653e:docs/codex-review-log.md | sed -n '102p' | awk -F'|' '{print $7"|"$8"|"$9"|"$10}'` → ` 2 | 0 | 0 | 2 `.

**DOC-128653e-F3 · low · CONFIRMED.** The rule cites `git.mjs:279` (inline branch diff, no pathspec) as the verified mechanism, but that line runs only when the diff has ≤ 2 files (`git.mjs:8`, `:332`; the companion passes no override at `codex-companion.mjs:409`); the 3-file round-1 review ran in self-collect mode, where Codex fetches the diff itself. The rule's conclusion (no way to path-scope) is unaffected — the `:266` changed-files list is equally unscoped and `git.mjs` reads no env. Disprover: precision defect, not a false statement; the rule says "verified in the plugin source", not "executed"; the threshold is already in `CODEX_WORKFLOW.md:13-14`.

**DOC-128653e-F4 · low · REFUTED.** Claim: the ledger row carries unit-2 cap 175, superseded to 163 in the same tree before the row was committed. Disprover: the row is a dated 9 Sep record re-landed byte-identically from `30a7090` per a recorded scope decision (session log `:206`, `:250`, `:381`); the 175 also stands in the tracked `docs/CODEX_WEEK_PLAN.md:79` on `origin/main`; the ledger convention is append-not-rewrite (no prior-session row has ever been rewritten for a later cap); the session log disagrees with itself (`:128` says 175, `:172` says 163), so no ledger state satisfies the stated assertion; unit 2 is frozen at 163 lines in `a1bc597`, so the "12 lines of headroom" cannot be consumed.

### Michael's mandatory check on E (ruling 3), answered by the reviewer and re-traced by the disprover

1. **Function deployed before the migration is applied to that project's database:** only the new insert inside `suppressionResponse` (`send-email/index.ts:178-190`) fails, and only on requests that trip the recipient gate (`:213`: a `sent` row to that recipient in the last 5 min, no bypass) or the hourly gate (`:226`: ≥100 `sent` rows in the hour). Postgres rejects with SQLSTATE 23514 `new row for relation "email_logs" violates check constraint "email_logs_status_check"`; supabase-js resolves `{error}`, `:191` throws it, `:192` catches. Every other request is untouched: the normal-path insert at `:241` writes `sent`/`failed`, both permitted by the old CHECK. No row is written for the suppressed attempt, so the AFTER INSERT trigger does not fire. Reverse order (migration first) is safe: the widened CHECK accepts every value the old function writes. **The same window exists independently on PROD**; the unit's commands and migration header cover DEV only.
2. **Surfaced or swallowed:** logged loudly with the code (`:193`, `'23514'` as its own argument) — but with the whole error object, hence E-F1. Caller response byte-identical: HTTP 429, body `{error: reason}`, same headers; `sendEmail()` (`notifications.ts:339-344`) throws the same message as before. Nothing reports success; no misleading row (the INSERT failed atomically); no Slack post, no notification. The failure reaches only the Edge Function log — not the response body, not Sentry (none in this EF), not Slack, not the app. An operator relying on `email_logs` for the P0-14 audit has no signal that a row is missing.
3. **Fail vs log-and-continue:** raised as E-Q1, not resolved.

### Questions for Michael (bring-to-Michael, deduplicated, sorted by unit)

No disprover ran on these: they are questions, not claims. Each carries the premise file:line the reviewer cited.

- **S-Q1** FIFO has no timeout. Every later save, the 30 s auto-save and Complete wait behind a save whose request never settles (`:4008-4011`, `:4578`, `:4735`; `client.ts` sets no fetch timeout, no AbortSignal). Bounded only upstream (statement_timeout, Cloudflare ~100 s); cannot wedge on rejection. Before the unit an unguarded save ran independently (and minted the duplicate row). Fail-vs-continue class: indefinite wait with the footer on "Saving…" vs a client deadline in a follow-up.
- **S-Q2** Confirm "within one form instance" includes same-tab re-open of the same lead: Save (INSERT in flight), Back (confirm-discard, `:3931-3941`), re-open — the new instance's mount lookup (`:3137-3147`) misses the row and INSERTs a second one. Identical on main; scope confirmation only.
- **S-Q3** Test 2's "bind every area write to a single inspection id" assertion cannot fail: the 12 Sep mock returns a fixed `'inspection-1'` for every INSERT (`concurrentSave.test.tsx:58-62`, `:179-183`). Test 1's `inspectionInserts === 1` carries the proof. Restore distinct ids in the mock (test-only) or accept.
- **S-Q4** Pre-existing, not widened: a non-network save failure during Complete still advances the lead to `inspection_ai_summary` (`:4547-4575`, `:4735-4757`, `:4787-4798`). Fail-vs-continue class; noted so it is not later attributed to S.
- **S-Q5** Record corrections in the `-4` log: Touching names `technicianInspectionSave.test.ts (new)`, which does not exist (the file is `TechnicianInspectionForm.concurrentSave.test.tsx`); step 20:20 says the repro was "preserved in gitignored .ai/" but `5848ed5` committed it at `docs/repro/`.
- **I-Q1** On 23505 (I-F1): retry with a MAX+1 allocation like the other two writers, or keep log-and-warn? Which allocator is canonical for inspections (`index.ts:2287` vs `api/render-pdf.ts:215` vs `ViewReportPDF.tsx:1535`)? J/K already chose history-derived for the job path. Fail-vs-continue class plus allocation policy. DEV verification guidance is under I-F1.
- **I-Q2** Pre-existing, unchanged, not filed: the `pdf_versions` insert runs on the service-role client (`index.ts:1973-1975` says "SELECTs only"; `:2383` inserts) with no caller check on the default persisting path beyond gateway `verify_jwt`, which memory records as accepting the anon key. The fix makes that insert succeed again. Confirm this EF's posture is tracked (TODO lists only send-email under P0-5), or accept.
- **K-Q1** Parent `pdf_url` update failure still returns HTTP 200 `success:true` (`index.ts:517-519`, `:523-533`); every caller toasts success then refetches a stale/empty row (`LeadDetail.tsx:640-643`, `:1111-1114`; `ViewReportPDF.tsx:811-814`). Pre-existing from `dedd0a3`, recorded in the log's Open questions and TODO P1-S-5; not made worse. Fail-vs-continue class.
- **K-Q2** (merges J's item 1 and K's item 2) Degraded-path visibility: 23505 exhaustion and non-23505 errors both return 200 with `versionHistorySaved:false` (`:500-504`); the history failure is now `console.error` only (`:488`, `:502`) where J's throw wrote an `error_logs` row; `jobReportPdf.ts:43` drops the flag, so the operator sees plain success. Confirm exhaustion is deliberately inside the degraded-success policy, and whether the degraded branch should write `error_logs` until the wrapper unit lands.
- **K-Q3** (J's item 2) After a `break` on a lookup error at attempt 2+ or after exhaustion, `newVersion` still holds the last attempted, already-taken number, and `:508-515` writes it to `job_completions.pdf_version` with this generator's URL — so the v-label (`ViewReportPDF.tsx:2903`) says vN while the history switcher's vN (`:3043-3068`) opens a different document. Covered by the "not transactional" deferral, or its own row?
- **K-Q4** (J's item 3) Empty or lagging history: `newVersion = max+1` from history only (`:487`), overwriting `job_completions.pdf_version` (`:509`), so the counter can move backwards once and re-synchronise. Only reader is the label at `:2903`; reviewer recommends no change. The upload filename still embeds the pre-loop parent-derived N (`:448-449`; recorded, cosmetic).
- **K-Q5** (J's item 4) `index.test.mjs` runs only by hand: outside vitest's `include` (`vitest.config.ts:8`), no `package.json` script, no CI. Decide a `test:ef` script or a documented manual check; otherwise the P1-S-5 regression cover is a test in name only. (This session ran it at merge 3 and Step 2: 5/5.)
- **K-Q6** Response contract differs from the P0-A sibling: K returns only the boolean (`:528`); the inspection EF returns `versionHistorySaved` plus a `warning {code, databaseCode, message}` object (`generate-inspection-pdf/index.ts:2413-2418`). The one scoped-out wrapper/UI follow-up will read two shapes. Align the EF or normalise in the wrapper.
- **K-Q7** Deploy-time expectation: on a PROD row whose counter is ahead of its history rows, the next regeneration writes history-max+1, which is lower — the visible chip drops (e.g. v5 → v2) while the Storage filename keeps the old counter. By design (history is the source of truth); recorded nowhere; should not be reported as a regression.
- **T-Q1** Name-resolution path for the display half: `get_staff_names` RPC (exists, authenticated-only, SECURITY DEFINER, applied to PROD 2026-08-26, the `leadNotes.ts` pattern) vs an admin read policy on `profiles` (a new hand-applied migration that also changes what technicians can read). And when resolution itself fails: show "User (name unavailable)" (degrade) or fail the timeline query (loud). Fail-vs-continue class. Governs T-F1 and T-F2's human-name half.
- **T-Q2** Should email and notification rows carry an actor label at all in this unit? (a) no label on non-activity rows (pre-diff); (b) resolve `sent_by` (sentinel → System, id → name via T-Q1's path) and leave notifications unlabelled; (c) keep as is and accept the false claim on emails. `notifications.user_id` is the recipient, so no actor exists there; the framer `new_lead` notification carries `metadata.source='framer'`, an evidenced website origin the hook does not select.
- **T-Q3** Expectation-setting: the "Website" branch has no live rows to act on — no app-code writer of `lead_created` exists in src, the framer RPC inserts the lead only, and the trigger migration `20251112000020` is absent from PROD (P2-42). A website arrival surfaces as the `new_lead` notification, which this diff labels "Actor not recorded". Not a predicate defect.
- **T-Q4** P2-42 hazard: if `20251112000020` is ever applied as reconciliation, its triggers write `user_id = NEW.assigned_to` (`:29`, `:61`) — the assignee, not the actor — and 4d2594c would then render that person's name (wrong-human attribution, which the accepted rule forbids). It also creates an `activities` SELECT policy `USING (true)` (`:103`). Worth a note on the P2-42 row.
- **T-Q5** Record corrections: session log line 46 ("generic emails … remain unknown without evidence" — `sent_by` is the evidence and is populated), line 44 (the unavailable label is the common case under the recorded policy, not an edge case); WEEK_STATE row T ("uses known profile or evidenced origin") describes intent, not the PROD result until T-F1 is resolved.
- **E-Q1** Audit-insert failure: fail the request or log-and-continue. Decided 09:31 on 12 Sep as log-and-continue (Codex first expected a 500; Michael corrected). Shipped code does that. Trade-off: the 429 contract stays identical and a broken audit table never changes send eligibility, but the audit gap is visible only in the EF log; a 500 or a body flag would surface it to the client, but no caller reads such a flag today (ViewReportPDF would toast the wrong message; BookJobSheet console-logs either way). Confirm.
- **E-Q2** Should the global hourly gate write a suppression row at all, or only the recipient cooldown (the P0-14 case)? Logging the hourly gate too is what creates the post-cap row + Slack + fan-out per request (E-F2's residue). Options: (a) keep both, accept the noise until P0-5 closes (as shipped); (b) recipient gate only; (c) log the hourly gate without firing the trigger (trigger change = new migration).
- **E-Q3** Deploy sequencing and whether to deploy E yet. (1) Migration before function, **on each project separately**; the unit's commands cover DEV only, the header names DEV only and says nothing of order; send-email deploys are global-immediate, so a PROD function deploy before the PROD migration reproduces check (1) on live throttled requests. (2) With the fan-out trigger live as tracked (`20260823090000:251-257`, `:283-291`), every suppressed row posts Slack "`<label> sent | <recipient>`" and an in-app "`<label> sent`" notification to every admin/technician, and the timeline titles it "sent" (`useActivityTimeline.ts:180`) — for the exact P0-14 case, operators would be told the confirmation went out. WEEK_STATE 109 already says triage this before calling the rollout visible. `InspectionReportHistory` renders the raw `suppressed` text truthfully; ViewReportPDF's duplicate-send guards filter to sent/delivered, so no false duplicate dialog.
- **E-Q4** Verify the live constraint on DEV (and PROD) before applying: the migration `DROP CONSTRAINT email_logs_status_check` by name with no `IF EXISTS` and re-adds nine values taken from the tracked CREATE TABLE; the handover records the live constraint as unverified. If the name differs the apply aborts loudly inside BEGIN/COMMIT (safe); if the live value set is wider than the file, the new CHECK could reject a value a writer uses. One Studio read: `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='public.email_logs'::regclass AND contype='c'`. Then write the P2-42 ledger row; the unit's commands omit it.
- **E-Q5** New, pre-existing, for P0-5 (from the E-F2 disprover): the relay already allows uncapped Slack/notification fan-out on the base code — an unvalidated `from` that Resend rejects (`:34`, 4xx non-retryable `:98-101`) writes a `failed` row unconditionally (`:241-253`), the trigger posts "`custom FAILED | <address>`" at high priority to every admin/technician, and `failed` rows are never counted by the hourly gate. Recorded nowhere (TODO P0-5 and the backlog say only "anyone can send email as MRC"). Not a finding against E.
- **H-Q1** Predicate scope: an inspection with an `ai_summary_versions` row but no `pdf_url`, untouched 24 h+ (abandoned at the AI-review step), is never surfaced (`useStalledInspections.ts:49`; the test at `:57-61` pins this). Correct to the stated intent; a definition question against P2-43's "a job that has quietly stopped moving".
- **H-Q2** Any bulk UPDATE on `public.inspections` hides every stalled inspection for 24 h: the BEFORE UPDATE trigger (`20251028135212:917-920`) stamps `updated_at` on every touched row. The per-item migration header already avoids UPDATEs for the audit-row reason; worth one line in migration policy.
- **H-Q3** The hook is correct only for an admin caller: the zero-versions half relies on `ai_summary_versions.admins_see_all`; for a technician, `technicians_see_assigned` filters by `inspector_id` while `tech_select_own_inspections` filters by `leads.assigned_to`, so a reassigned lead reads as a false stall. Safe today (admin-only route, `App.tsx:99-103`); record before anyone reuses the hook.
- **C-Q1** What the tile should display while its read is paused offline (C-F1): "…", "Unavailable" (misleading if the admin knows they are offline), or a distinct offline state. The hook already exposes `isPaused`/`fetchStatus`. Fail-vs-continue class.
- **C-Q2** After a prior success, a failed 60 s refetch replaces the last-known lead list with "Unavailable" (`:31-43` branches on `isError` first, though React Query keeps the last `data`). Stale-but-labelled vs blank-but-honest — product call.
- **C-Q3** The card has no link target — lead numbers are plain text, no click-through, unlike the three neighbouring alert tiles. Recorded design ("non-interactive so no 48px target applies", log line 137). Confirm as the accepted v1 shape.
- **D-Q1** Which text is canon for the cap's unit (DOC-F1): "per PR" (`AGENTS.md:66`, `CLAUDE.md:159`) or "per reviewed unit" (`AGENTS.md:67`, brief `:134`, TODO P2-43)? Same ruling decides whether `--base <same-branch commit>` satisfies `CLAUDE.md:162` / `CODEX_WORKFLOW.md:59,67` "pre-declared parent of a stacked branch".
- **D-Q2** SOURCE/TEST split reporting now lives in two places (`AGENTS.md:67`, brief `:139-140`, `:185`), and the brief says "source is the number the cap actually protects" while the 9 Sep NO RAISE was applied to the total (365 vs 260; source 151). Pre-existing contradiction; this commit added the second copy.
- **D-Q3** 9 Sep step-log times do not match git committer timestamps (step "20:40 committed f8257dd" vs git 19:08; "23:00 ruled NO RAISE" vs the hook's 21:51 resume block already listing it), so the ledger's "waiver logged BEFORE the run" ordering cannot be sequenced from the log's times alone. Outside the diff; the 12 Sep mutation evidence files attest test-first independently.
- **D-Q4** Ledger rows since 2026-09-08 sit below `## Do not review` as a header-less fragment (integration tree `:98-103`); rows outside the header-bearing table do not render or parse as rows. Formatting; your call whether the ledger must parse.
- **D-Q5** For reviews touching 3+ files the companion runs self-collect and the printed `Target:` line does not say so; `git.mjs:343` already computes `inputMode`. Process choice: print it alongside Target / count / focus.
- **X-Q1** Tracker acceptance wording vs the accepted behaviour, three rows (authors could not edit TODO.md on unit branches, brief rule 5): P0-A says "injected 23514 cannot return complete success" but the implemented and accepted contract is degraded 200 + flag + warning; P1-S-5 says "commit distinct versions or return an explicit recoverable failure" but K returns 200 + `versionHistorySaved:false`; P0-14 says "at minimum failed status" but the unit uses the distinct `suppressed` status Michael authorised 09:31. Reword on merge so the rows are not read as unmet at the production gate.

### Verdict on the candidate

- **Build gate: green.** `077d018`: tsc 100/41 with 0 new and 0 gone vs `dedd0a3`; Vitest 85/1389 with only T24 failing; `npm run build` exit 0; `node --test` 5/5.
- **Review gate: all nine logged.** 11 confirmed findings, none cross-tenant, secret, auth or data-loss; 2 refuted.
- **Production: NOT cleared.** It clears only when Michael has read the eleven confirmed findings and the 36 questions. Reviewer's reading of what each unit needs, for triage, not as a decision: **T** introduces two visible truthfulness regressions on every timeline surface (T-F1, T-F2) and needs T-Q1/T-Q2 answered before it ships as-is; **I** is correct at the literal level but P0-A's acceptance is unmet in the ordinary loop (I-F1) and the DEV evidence step (DEV STATE item 3) must pick its inspection accordingly; **E** ships only after E-Q3 (per-project migration-then-deploy, and the Slack "sent" mislabel) is decided; **S, J+K, H, C** carry low/medium findings that are pre-existing, narrow, or already corrected. **DOC** has two medium factual defects in the AGENTS.md rule and the ledger row.
- Nothing was fixed. Nothing is recommended for fixing by this session.

### Owner message — DRAFT for Michael, not sent

Plain language, no lead names beyond numbers, no Session H, no lead-queue numbers. Michael sends or edits.

> **Subject: This week's app fixes — what changed and what to watch**
>
> Hi Glen, Clayton, Vryan,
>
> Six fixes from last week are being tested together as one release. None of them is live yet; I'll tell you when they are. Here's what each one does and what it means for you.
>
> **1. Inspection form — double saves.** Tapping Save more than once while a new inspection was still being created could produce two inspection records, with the rooms and readings split between them. That's what happened on MRC-2026-0124. The form now finishes the first save before applying the next, so everything lands on one record. Until it's live: tap Save once and wait for the tick.
>
> **2. Inspection report history.** Since early August, every inspection report the app generated failed to record its version in the report history. The report itself still generated, so nobody noticed. Once released, version history works again on first generation; regenerating after an edit still loses the entry — being fixed. Older reports won't get their history back.
>
> **3. Job reports.** When the app couldn't record a job report's version, it also refused to show the report at all — you'd see "No Report Generated" even though the file existed. Now the report is always shown; a missing history entry no longer hides it. [Keep this sentence only if the job-report function deploys this week:] After the update, a job report's version number may drop once (for example v5 to v2) and then count up from there — expected, not a fault.
>
> **4. Booking confirmation emails — not fixed yet.** These can fail silently. What was found: if a customer has received any email from us in the previous five minutes (for example the report approval), a job booking confirmation sent right after it is stopped by a safety limit that prevents us emailing one person too often. The customer never gets it, and nothing tells you. That's what happened on MRC-2026-0152 (job Monday 14 Sep); I'm calling them. This release only makes the block visible in the email log, as "suppressed" — the confirmation still does not go out. Interim check: when you book a job within a few minutes of sending the customer anything else, confirm the booking by phone or text, or wait ten minutes before booking. The proper fix is being decided.
>
> **5. Stalled inspections.** The admin dashboard gets a new tile counting inspections that haven't been touched for 24 hours and have no report yet, with their lead numbers. It's a prompt, not an alarm.
>
> **Also worth knowing:** a confirmation to one customer bounced on 10 Sep and the app still shows it as sent. The app doesn't check delivery at all — once the email provider accepts a message it's marked sent, and a bounce never changes that. For anything important, confirm receipt directly. This is on the list.
>
> Michael

Notes for Michael on the draft (adjusted 2026-09-15 after the rulings): the timeline item is dropped (T reverted); item 2 ends as ruled; item 3 carries K-Q7's sentence in brackets, to keep only if the job-report function deploys this week; "not fixed yet" on confirmations stays. Item 4 states the mechanism without naming the cooldown, the template or the console. The bounced-email paragraph is P0-B without the domain.

## Merges

All measured on the staged merge tree before committing (`--no-ff --no-commit`), Node v24.20.0, TypeScript 5.8.3. tsc is set-diffed against the previous merge after stripping `(line,col)`. Vitest runs with inline dummy env; failing names are set-diffed against the previous merge.

| # | Unit | Merge commit | tsc lines / distinct | tsc new · gone | Vitest files / tests | Failed | New failures | New test files |
|---|---|---|---|---|---|---|---|---|
| 0 | TODO fix | `200b462` | 100 / 41 (at `dedd0a3`) | — | 79 / 1315 | 1 (T24) | — | — |
| 1 | S | `d9bcbf8` | 100 / 41 | 0 · 0 | 80 / 1319 | 1 (T24) | none | `TechnicianInspectionForm.concurrentSave.test.tsx` |
| 2 | I | `c0958bd` | 100 / 41 | 0 · 0 | 81 / 1322 | 1 (T24) | none | `generateInspectionPdf.versionHistory.test.ts` |
| 3 | J+K | `10680e8` | 100 / 41 | 0 · 0 | 81 / 1322 | 1 (T24) | none | none; `node --test …/generate-job-report-pdf/index.test.mjs` = 5 tests, 5 pass, 0 fail |
| 4 | T | `8014bd9` | 100 / 41 | 0 · 0 | 82 / 1363 | 1 (T24) | none | `ActivityTimeline.test.tsx` (+41 tests) |
| 5 | E | `45cf400` | 100 / 41 | 0 · 0 | 83 / 1372 | 1 (T24) | none | `sendEmail.suppression.test.ts` (+9 tests); adds migration `20260912000000_email_logs_suppressed_status.sql` (not applied anywhere by merging) |
| 6 | H+C | `077d018` | 100 / 41 | 0 · 0 | **85 / 1389** | 1 (T24) | none | `useStalledInspections.test.ts`, `StalledInspectionsCard.test.tsx` (+17 tests) |

H+C conflict resolution: one hunk in `docs/codex-review-log.md` (lines 102–106). Ours was S's `8e0e695` row; theirs was H+C's `0f9d9ee` row. Verified exactly three marker lines, removed only those with perl (not the Edit tool), and staged the file by explicit path. The file is +1 line against the previous merge `45cf400` (the H+C row) and +1 line against the branch tip (the S row). Both rows are kept, S first.

After 6/6: **85 files / 1389 tests equals WEEK_STATE's arithmetic expectation exactly.** tsc is unchanged from `dedd0a3` across all six merges.

## Step 2 — candidate gate at `077d018` (16:53:11–16:53:50)

- Node v24.20.0 / npm 11.19.0, absolute v24.20.0 bin first on PATH.
- `npm ci`: exit 0, in place (npm ci replaces node_modules; nothing deleted by hand). TypeScript 5.8.3 == pinned. Tree clean afterwards.
- Vitest: **85 files / 1389 tests, 1388 passed, 1 failed** — `src/lib/api/reportPipeline.test.ts > fetchVersionPdfBlob returns the file from the signed URL when it resolves` (T24). New failures vs merge 6: none. vs `dedd0a3`: none.
- tsc `-p tsconfig.app.json --noEmit`: **100 / 41**; 0 new, 0 gone vs merge 6 and vs `dedd0a3`.
- `npm run build` (`tsc --noEmit && vite build`; the leading root `tsc` is the T7 no-op): **exit 0**, vite built in 5.86 s, PWA precache 133 entries (7624.82 KiB). `dist/` is gitignored.
- `node --test supabase/functions/generate-job-report-pdf/index.test.mjs`: **tests 5, pass 5, fail 0**, cancelled 0, skipped 0.
- Brief's expectation "roughly 85 files / 1389 tests plus 5 node tests": measured exactly that.

## Step 3 — tag

- `candidate/2026-09-15-077d018`: annotated, local, points at `077d018`. **Not pushed.** Michael pushes the branch and the tag.
- Once the reviews are logged, this session log is committed on the branch as a docs commit, so the branch tip will be the tag plus one docs commit. The tag stays on the verified commit; Session 2 verifies it and Session 3 cuts from it.

## Disk — worktree table (ruling 6; produced before Step 2, nothing deleted)

Read-only: `du -sk`, `git status --porcelain`, `git log -1`. Built 16:30–16:43.

| Path | Branch | node_modules | Uncommitted | Last commit |
|---|---|---|---|---|
| /Users/michaelyoussef/mrc-app-1 | work-main | 1.24 GiB | yes (12) | 2026-09-11 |
| /Users/michaelyoussef/mrc-app-prod | feat/area-hide-in-report-main | 0.85 GiB | yes (32) | 2026-09-05 |
| /Users/michaelyoussef/mrc-booksheet-days | fix/booksheet-equipment-days | 0.85 GiB | yes (3) | 2026-09-08 |
| /Users/michaelyoussef/mrc-cost-estimate | fix/option-stacking-equipment-days | symlink -> /Users/michaelyoussef/mrc-app-1/node_modules | no | 2026-08-31 |
| /Users/michaelyoussef/mrc-email-cooldown | fix/send-email-cooldown-logging | 0.84 GiB | no | 2026-09-12 |
| /Users/michaelyoussef/mrc-equip-days | fix/equipment-days-form | 0.85 GiB | yes (2) | 2026-09-08 |
| /Users/michaelyoussef/mrc-guard-fix | fix/guard-hook-bypasses | 0.85 GiB | yes (2) | 2026-09-07 |
| /Users/michaelyoussef/mrc-integration | integration/2026-09-15 | 0.84 GiB | yes (1: this log) | 2026-09-11 (then `dedd0a3`) |
| /Users/michaelyoussef/mrc-jobpdf | fix/job-report-pdf-memory | 0.84 GiB | no | 2026-09-12 |
| /Users/michaelyoussef/mrc-lead-list | docs/codex-log-session-h | 0.85 GiB | no | 2026-09-06 |
| /Users/michaelyoussef/mrc-merge | docs/okf-seed | 0.85 GiB | no | 2026-09-05 |
| /Users/michaelyoussef/mrc-offline | main | none | no | 2026-09-08 |
| /Users/michaelyoussef/mrc-p0-hidden | fix/inspection-save-integrity | 0.84 GiB | no | 2026-09-12 |
| /Users/michaelyoussef/mrc-pdf | fix/pdf-hard-save | 0.85 GiB | no | 2026-09-06 |
| /Users/michaelyoussef/mrc-pdf-assets-rls | fix/pdf-assets-anon-write-rls | 0.85 GiB | yes (53) | 2026-09-05 |
| /Users/michaelyoussef/mrc-pdfver | fix/pdf-versions-generation-type | 0.84 GiB | no | 2026-09-12 |
| /Users/michaelyoussef/mrc-per-item | feat/per-item-equipment-days | 0.84 GiB | no | 2026-09-09 |
| /Users/michaelyoussef/mrc-plan | docs/week-2-plan | none | no | 2026-09-12 |
| /Users/michaelyoussef/mrc-r8-cancel | fix/cancel-path-field-clearing | 0.85 GiB | no | 2026-09-05 |
| /Users/michaelyoussef/mrc-recon-1 | (detached 1551635) | none | yes (1) | 2026-09-09 |
| /Users/michaelyoussef/mrc-recon-2 | (detached 1551635) | none | yes (1) | 2026-09-09 |
| /Users/michaelyoussef/mrc-reminder-ef | fix/reminder-group-claim | 0.85 GiB | yes (54) | 2026-09-05 |
| /Users/michaelyoussef/mrc-send-email | fix/send-email-auth | 0.85 GiB | no | 2026-09-06 |
| /Users/michaelyoussef/mrc-sentry-vis | fix/sentry-prod-visibility | 0.84 GiB | no | 2026-09-09 |
| /Users/michaelyoussef/mrc-stalled-sweep | feat/stalled-inspection-visibility | 0.84 GiB | no | 2026-09-12 |
| /Users/michaelyoussef/mrc-status-enum | fix/orphaned-lead-statuses | 0.85 GiB | no | 2026-09-06 |
| /Users/michaelyoussef/mrc-timeline-attr | fix/activity-timeline-attribution | 0.84 GiB | no | 2026-09-12 |
| /Users/michaelyoussef/mrc-travel-ef | fix/travel-time-multitech | 1.22 GiB | yes (54) | 2026-09-05 |

Caches: `~/.cache` 4.35 GiB (includes `~/.cache/puppeteer` 2.94 GiB) · `~/.npm` (the npm cache) 8.62 GiB · `~/Library/Caches/ms-playwright` 4.48 GiB · disk 7.7 GiB free of 228 GiB (96% used).

## Round 2 — Michael's rulings (received 17:3x–17:4x), revert, new candidate

### Revert of T

- `git revert -m 1 8014bd9` on `integration/2026-09-15` → **`587415e`**, message "revert T: T-F1/T-F2 truthfulness regressions on every timeline surface; re-lands as T2 with T-Q1/T-Q2" (body names the two findings and the T2 plan). The four files T touched are byte-identical to `dedd0a3` again (`git diff dedd0a3 HEAD -- src/hooks/useActivityTimeline.ts src/components/dashboard/ActivityTimeline.tsx` is empty; the test file and the T session log are deleted).

### Step 2 on `587415e` (17:52:12–17:52:50, Node v24.20.0, clean `npm ci`, TypeScript 5.8.3 == pinned)

- Vitest: **84 files / 1348 tests, 1347 passed, 1 failed** — T24 only; no new failures vs `dedd0a3`; `ActivityTimeline.test.tsx` absent as expected.
- tsc: **100 / 41**; 0 new, 0 gone vs `dedd0a3`.
- `npm run build`: exit 0 (vite built in 5.79 s).
- `node --test supabase/functions/generate-job-report-pdf/index.test.mjs`: tests 5, pass 5, fail 0.
- Matches Michael's stated expectation exactly (84 / 1348, T24 only, 0 · 0, build 0, 5/5).

### Tag move

- New annotated tag **`candidate/2026-09-15-587415e`** → `587415e`. **`candidate/2026-09-15-077d018` is retained** (Session 3 cut from it); it is no longer the release candidate. Session 2 verifies the NEW tag. Session 3's worktrees are on the old tag: their base differs from the new one only by the T revert (four files), and none of lanes 1–4 touches those files.

### Rulings, verbatim (Michael, 2026-09-15)

> The F/C rule: fail when the caller can act, continue when they can't — and continue only with the failure written to error_logs or the timeline, never console-only, never shown as success.
>
> S-F1 record, ships. S-Q1 ships as-is (bounded; the alternative minted duplicates); S2 adds a per-save timeout with a visible failure and the queue continues; Session 2's race agent tests the never-settles case. S-Q2 record, not S's. S-Q3 restore distinct ids, test-only, S2. S-Q4 record as pre-existing; ruling: Complete must NOT advance status on a failed save — caller can act, so fail. S2. S-Q5 fix the log.
>
> I-F1 I ships; P0-A stays OPEN with I-F1 as the residual. I-Q1 canonical allocator is history-derived (MAX(pdf_versions)+1), same as J/K's job path; inspections.pdf_version becomes a write-back mirror updated only after a successful history insert; on 23505 retry with MAX+1. I2 lane. I-Q2 tracked as a security row beside P0-5.
>
> J-F1 closed by K; J never deploys alone — deploy note. K-Q1 not made worse; P1-S-5 stands as written; K2 delivers the explicit recoverable failure. K-Q2 yes, exhaustion is inside the policy; write error_logs on the degraded branch and propagate the flag to a toast — K2. K-Q3 own row, fixed by I2's write-back ordering. K-Q4 accept. K-Q5 add a test:ef script; deployment-captain runs it — K2. K-Q6 align in the wrapper, record. K-Q7 deploy note plus one line in the owner message.
>
> T-F1/T-F2 reverted (above). T-Q1 get_staff_names RPC — no migration, no RLS widening; on failure degrade and keep the timeline. T-Q2 (b): sentinel → System, id → name, notifications unlabelled. T2 = cherry-pick 8aefa5f onto the new tag + those two. T-Q3 expectation note. T-Q4 note on P2-42: that migration is not to be applied. T-Q5 fix the log and the WEEK_STATE row.
>
> E-F1 E ships (web); fix is log code+message only, never DETAIL — E2. E-F2 closed. E-Q1 continue, confirmed — with an error_logs write on the audit-insert failure, E2. E-Q2 (b): recipient cooldown writes rows, the hourly gate does not — it logs once per window to error_logs, E2. E-Q3 migration then function, per project, DEV then PROD; PROD EF deploy waits for E2 (timeline and the Slack trigger render 'suppressed' as suppressed — trigger change is a migration). E-Q4 I run the pg_constraint query on DEV before the migration and compare to the nine values; a wider live set stops and comes back to me; same on PROD later; then the P2-42 ledger row. E-Q5 record under P0-5; the send-email auth session moves up and takes the cooldown decision with it.
>
> H-Q1 v1 predicate stands; the second predicate is a Vryan question. H-Q2 one line in migration policy. H-Q3 record in the hook and TODO. C-F1/C-Q1 distinct offline state, never "0". C-Q2 stale-but-labelled. C-F2 count rows, not lead numbers; I'll check DEV/PROD for NULL lead_number. C-Q3 v1 confirmed, link later. All C2.
>
> D-Q1 per reviewed UNIT is canon; fix AGENTS.md:66 and CLAUDE.md step 2 to match :67; a same-branch commit declared before the run satisfies the stacked-parent rule. D-Q2 cap is 150 SOURCE lines, tests reported and unlimited, one copy in AGENTS.md; the 9 Sep NO RAISE stands as history. D-Q3 history stands; from now every waiver line carries a UTC timestamp and the SHA it precedes. D-Q4 the ledger must parse: fix the header and rows, add a reviewer column (codex | claude), log the nine passes there, ratios per reviewer. DOC-F1/F2/F3 fixed in the same commit. D-Q5 plugin edits get overwritten — record as a limitation; the lane procedure prints file count and notes ≥3 files ⇒ self-collect.
>
> X-Q1 NO rewording. Tracker acceptance is the intent. Annotate P0-A, P1-S-5 and P0-14: "interim behaviour shipped: …; acceptance unmet; owner unit: I2 / K2 / E2". P0-14 stays open until a suppressed confirmation reaches the sender as a failed status.

### Where each ruling landed

- Code rulings (S2, T2, I2, K2, E2, C2 items): `docs/TODO.md`, new section "Session plan — candidate 2026-09-15 follow-up units", plus annotations on P0-A, P1-S-5, P0-14 (X-Q1), P0-5 (E-Q5), P2-42 (T-Q4), P2-39 (T revert), P2-43 (H-Q3, C findings), P2-44 (S-Q2); new rows P0-15 (I-Q2 security), P2-54 (K-Q3), B8 (H-Q1 Vryan question); deploy notes (J never alone, K-Q7, E ordering + E-Q4 query, I-F1 DEV proof guidance).
- D-Q1/D-Q2/D-Q3/D-Q5, DOC-F1, DOC-F3, H-Q2: `AGENTS.md` (single canonical Diff-limit bullet: 150 SOURCE lines per reviewed unit, tests reported and unlimited, waiver lines carry a UTC timestamp + SHA; path-scoping bullet qualified with the ≤2-files inline threshold; new input-mode bullet; migration-policy sentence on bulk UPDATEs), `CLAUDE.md` (step 2 measures the unit's range with the source/test split and points to AGENTS.md; step 5 accepts a declared same-branch commit; STOP shape shows source/test), `docs/CODEX_WORKFLOW.md` (its two cap restatements become pointers).
- D-Q4, DOC-F2: `docs/codex-review-log.md` (header-less fragment moved into the table; `reviewer` column; stalled row tally 2/2/0/0; nine `claude` rows for these passes; ratios per reviewer).
- S-Q5: `docs/sessions/2026-09-09-fix-inspection-save-integrity-4.md` (two corrections, by this session).
- T-Q5: WEEK_STATE row T annotated in `~/mrc-plan` (`docs/week-2-plan`, commit `d6ef1ab`, not pushed — outside this branch); the T session log is out of the tree with the revert, so its lines 44/46 are fixed by T2 when it re-lands (recorded in T2's items).
- T2 cherry-pick note: `8aefa5f` modifies the session log that `4d2594c` created; cherry-picking `8aefa5f` alone onto the new tag will conflict on that file. T2 should cherry-pick `4d2594c` then `8aefa5f` (or recreate the log), then apply T-Q1 and T-Q2.
- H-Q3 "record in the hook": a comment in `src/hooks/useStalledInspections.ts` is a source edit to a reviewed unit; not done in this docs commit, listed as a C2 item.

## Codex threads

- (none — Codex is not run by this session this cycle)

## Did

- Round 1: Step 0 dry run; integration worktree; clean `npm ci`; tsc + Vitest baseline; payload/provenance; TODO.md line-1 fix (`200b462`); six merges with per-merge tsc set-diff and full Vitest in each merge message; Step 2 green at `077d018`; tag `candidate/2026-09-15-077d018`; nine reviews + thirteen disprovers, all logged; E's mandatory ordering check answered; 36 questions deduplicated; owner-message draft.
- Round 2: T reverted (`587415e`); Step 2 green on it (84 / 1348, T24 only; tsc 0 · 0; build 0; node --test 5/5); tag `candidate/2026-09-15-587415e`; rulings recorded verbatim; rulings applied to docs/TODO.md (S2/T2/I2/K2/E2/C2 units, P0-15, P2-54, B8, annotations, deploy notes), docs/codex-review-log.md (parses, reviewer column, nine claude rows, DOC-F2 tally, ratios), AGENTS.md / CLAUDE.md / docs/CODEX_WORKFLOW.md (one cap copy: 150 SOURCE lines per reviewed unit; same-branch declared base; waiver lines timestamped; self-collect note; H-Q2 migration sentence), the S log (S-Q5), WEEK_STATE row T in `~/mrc-plan` (`d6ef1ab`, unpushed); owner draft adjusted; all verified read-only.

## Did NOT

- Fix, patch or re-review any application code. Touch `~/mrc-app-1` beyond the pointer stub. Touch DEV/PROD, Vercel or Codex. Edit any agent definition (nothing was pinned).
- Add the harness's `Claude-Session:` trailer to any commit: AGENTS.md's no-attribution rule outranks it.
- Run `gitnexus detect_changes`: no symbol edited; the index cannot see this worktree.
- Put the H-Q3 comment into `src/hooks/useStalledInspections.ts` (source edit to a reviewed unit; listed as a C2 item). Fix the T session log's lines 44/46 (out of the tree with the revert; T2 item).
- Push anything.

## Broke

- nothing known

## Open — for Michael

- Push: `integration/2026-09-15` (two new commits: `587415e` revert + the docs commit) and the tag `candidate/2026-09-15-587415e`. `~/mrc-plan` `docs/week-2-plan` has one unpushed docs commit `d6ef1ab`.
- Session 2 verifies `candidate/2026-09-15-587415e`. Session 3's worktrees are on `candidate/2026-09-15-077d018`; the two bases differ only by the four T files, which lanes 1–4 do not touch.
- The count command now lives in AGENTS.md (Diff limit) and is restated in CLAUDE.md step 2 for operational use; the cap NUMBER lives only in AGENTS.md. If "one copy" is meant for the command as well, CLAUDE.md step 2 should become a pointer too.
- Owner message: the K-Q7 sentence is bracketed in item 3; keep it only if the job-report function deploys this week.
- DEV STATE item 3 (P0-A proof on DEV) must pick an inspection whose `MAX(pdf_versions.version_number) <= inspections.pdf_version`, per I-F1.

## Resume from here

<!-- resume:start -->
- Next command: none for Session 1. Michael pushes; this session ends. Session 2 starts after this session's process is gone (`ps`); Session 3 continues from its tag.
- Uncommitted files: none after the docs commit
- Untested: nothing on the candidate; the findings' failing tests are stated, not written
<!-- resume:end -->
