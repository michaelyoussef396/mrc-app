okay im going to start at the start as if i was a ustomer jsut making an summbision on the site

https://fluid-members-390105.framer.app/contact im on the fake frame website clon we have and im gonna fill out the forms
name: adam michael
date: 15/05/2026
phone 0433880403
time:9:30am
email: michaelyoussef396@gmail.com
number and address: 35 wellington street
suburb: mernda
postcode:
your message: Hi, noticed some black mould spreading along the bedroom ceiling and around the window frame in the master bedroom over the last few weeks. Getting worse and starting to smell musty. We've got two young kids so keen to get it sorted asap. Bought the place about 18 months ago, not sure if it's a moisture issue from the bathroom next door. Looking to get an inspection booked in. Thanks.

okay i have sumbbited yet but the firs tthing i would like to test is to see if these all save and if so where do they go in the databse and values and variables are they called in the database adn system the roginal values we get inlcuidng that fact that the date adn time is only eprfered here not the locked in one
i sumbbited i saw the thank you on the webwite and i also jsut go a slack message within the first 5 seconds which is good showing me this i cna see the entire form we wrote and its good to see the source get saved for website (framer) heres the screenshot for the slack notfication and heres it coppied /Users/michaelyoussef/mrc-app-1/docs/testing/new-lead-recieved slack-notification.png

mrc-system  [10:35 AM]
:house: New Lead ReceivedFull Name
adam michaelPhone
0433880403Email
michaelyoussef396@gmail.comLead Source
Website (Framer)Street Address
35 wellington streetSuburb
merndaPreferred Date
15/05/2026Preferred Time
09:30Issue Description
Hi, noticed some black mould spreading along the bedroom ceiling and around the window frame in the master bedroom over the last few weeks. Getting worse and starting to smell musty. We've got two young kids so keen to get it sorted asap. Bought the place about 18 months ago, not sure if it's a moisture issue from the bathroom next door. Looking to get an inspection booked in. Thanks.:warning: ACTION REQUIRED
:telephone_receiver: CALL LEAD AND BOOK THEM IN:link: Schedule Now: Open Leads Dashboard:date: 13/05/2026, 10:35 am  •  MRC System

the link for Open Leads Dashboard works and leads to the dashabord which is good and correct

---

## DB Verification — MRC-2026-0141

Lead `id`: `c50c4a6d-486a-46f2-8e4c-18086e7eef28` · `created_at`: 2026-05-13 00:35:22 UTC · `updated_at`: 2026-05-13 00:50:27 UTC · status `new_lead`.

| column                      | type    | submitted value (form)          | stored value (DB)                             | source field                                                  |
| --------------------------- | ------- | ------------------------------- | --------------------------------------------- | ------------------------------------------------------------- |
| `lead_number`               | varchar | (auto-gen)                      | `MRC-2026-0141`                               | system                                                        |
| `status`                    | enum    | (auto)                          | `new_lead`                                    | system                                                        |
| `full_name`                 | varchar | adam michael                    | `adam michael`                                | name                                                          |
| `email`                     | varchar | michaelyoussef396@gmail.com     | `michaelyoussef396@gmail.com`                 | email                                                         |
| `phone`                     | varchar | 0433880403                      | `0433880403`                                  | phone                                                         |
| `property_address_street`   | varchar | 35 wellington street            | `35 wellington street`                        | address line                                                  |
| `property_address_suburb`   | varchar | mernda                          | `mernda`                                      | suburb                                                        |
| `property_address_state`    | varchar | (Google Places)                 | `VIC`                                         | autocomplete                                                  |
| `property_address_postcode` | varchar | 3754                            | `3754`                                        | autocomplete (form field shown blank but Places populated it) |
| `lead_source`               | varchar | (system)                        | `website` (lowercase)                         | Edge Function default                                         |
| `issue_description`         | text    | (full message)                  | (full message verbatim)                       | message                                                       |
| `customer_preferred_date`   | date    | 15/05/2026                      | `2026-05-15` (ISO)                            | preferred_date                                                |
| `customer_preferred_time`   | text    | 9:30am                          | `09:30` (24h)                                 | preferred_time                                                |
| `urgency`                   | varchar | (not on form)                   | **NULL**                                      | dead-in-DB                                                    |
| `property_type`             | varchar | (not on form)                   | **NULL**                                      | dead-in-DB                                                    |
| `access_instructions`       | text    | (added via lead view, 10:49 am) | populated, full text                          | LeadDetail inline edit                                        |
| `special_requests`          | text    | (added via lead view, 10:49 am) | populated, full text                          | LeadDetail inline edit                                        |
| `internal_notes`            | text    | (added via lead view, 10:50 am) | single appended entry with timestamp + author | LeadDetail note appender                                      |
| `search_text`               | text    | (auto)                          | concatenated indexable string                 | DB trigger                                                    |

`internal_notes` is a single TEXT column on `leads` (NOT a separate table). New entries prepend `[DD/MM/YYYY at h:mm am/pm] {note}\n— {author}` with `\n\n---\n\n` separator before previous entries — see `src/lib/utils/internalNotes.ts:25-39`.

**Activity rows for this lead** (`SELECT * FROM activities WHERE lead_id = …`):

| activity_type  | description                  | metadata.changes                                                                       |
| -------------- | ---------------------------- | -------------------------------------------------------------------------------------- |
| `lead_updated` | Updated: access instructions | `[{ field:"access_instructions", from:null, to:"Front gate…" }]`                       |
| `lead_updated` | Updated: special requests    | `[{ field:"special_requests", from:null, to:"Please remove shoes…" }]`                 |
| `lead_updated` | Updated: internal notes      | `[{ field:"internal_notes", from:null, to:"[13/05/2026 at 10:50 am] Lead came in…" }]` |

`from: null` is technically correct (the columns were null before), but the description string tells the reader nothing about what changed, and an internal-notes append should not be diffed as a normal field update. Both are Wave 3 fixes.

**Lead source truth:** DB stores lowercase `"website"`. Slack template hardcodes `"Website (Framer)"` (`supabase/functions/receive-framer-lead/index.ts:143`). Admin lead view renders raw column. Display-normalisation question, not in current wave scope.

---

## Bug Log

### BUG-001 — Slack postcode missing from new-lead notification

- **Surface:** Slack #mrc-system, new-lead notification
- **Repro:** Submit Framer form → Slack message shows Street + Suburb, no Postcode line
- **Wave:** 1
- **Status:** pending fix

**Claude Code fix notes:**

- Root cause: `buildSlackBlocks()` template in `receive-framer-lead/index.ts` rendered Street + Suburb only. Postcode was in the inbound payload and persisted to DB but never added to the Slack block fields.
- Files changed: `supabase/functions/receive-framer-lead/index.ts`
- Commit: `3cf8718`
- Verified by: Run 2 ✅ — fresh Framer submission confirmed Slack `New Lead Received` block now includes Postcode adjacent to Street + Suburb

### BUG-002 — Property Type rendered on lead view but never populated

- **Surface:** `/admin/leads/<id>` Lead Detail page
- **Repro:** Open MRC-2026-0141 → Property Type field is blank "Set property type" prompt; no path in code writes this column from any user flow
- **Wave:** 1
- **Status:** pending fix (remove from new_lead admin view; logged as BUG-008 for future inspection-form wiring)

**Claude Code fix notes:**

- Root cause: `InlineEditField` for `property_type` rendered inside the Property Information card on `LeadDetail.tsx`, but no programmatic writer exists in the codebase. The Framer Edge Function and `CreateNewLeadModal` both omit the field on insert. Result: the field invited admin to set a value that served no downstream purpose.
- Files changed: `src/pages/LeadDetail.tsx` (removed the Property Type block; dropped now-unused `propertyTypeOptions` import)
- Commit: `3cf8718`
- Verified by: Run 2 ✅ — confirmed on mrcsystem.com production: Property Type row absent from `/admin/leads/<id>`

### BUG-003 — Urgency rendered on lead view but never populated

- **Surface:** `/admin/leads/<id>` Lead Detail page
- **Repro:** Open MRC-2026-0141 → Urgency field is blank "Set urgency" prompt; no path in code writes this column from any user flow
- **Wave:** 1
- **Status:** pending fix

**Claude Code fix notes:**

- Root cause: `InlineEditField` for `urgency` rendered inside the Lead Details card on `LeadDetail.tsx`. The column has been present in the schema for a year, but no UI surface anywhere writes it and the business does not track urgencies. Dead-in-UI.
- Files changed: `src/pages/LeadDetail.tsx` (removed the Urgency block; dropped now-unused `urgencyOptions` import)
- Commit: `3cf8718`
- Verified by: Run 2 ✅ — confirmed on mrcsystem.com production: Urgency row absent from `/admin/leads/<id>`
- Note: `leads.urgency` column remains in the schema. No migration. Dead-column cleanup is a separate future sweep.

### BUG-004 — Call button on lead detail

- **Surface:** `/admin/leads/<id>` — Call button (multiple instances)
- **Repro:** Click Call on desktop with no `tel:` handler → behaviour ambiguous; works if handler is registered, no-ops otherwise
- **Wave:** 2
- **Status:** pending fix (anchor + copy-to-clipboard fallback)

**Claude Code fix notes:**

- Root cause: `handleCall` set `window.location.href = "tel:..."` programmatically. With no OS `tel:` handler the browser could land on `about:blank` or behave inconsistently across browsers.
- Fix: removed `handleCall` JS handler. Converted all 5 Call button instances (status-specific CTAs, desktop header, mobile icon, mobile dropdown item) to `<Button asChild><a href={`tel:${lead.phone}`}>` pattern. Added a Copy icon button in the Contact Information card's phone renderReadOnly using `navigator.clipboard.writeText()` + toast.success("Phone copied").
- Files changed: `src/pages/LeadDetail.tsx`
- Commit: `eeaec8f`
- Verified by: Run 2 ✅ — confirmed on mrcsystem.com: clicking Call no longer causes about:blank, page stays put. Copy phone affordance: toast + clipboard ✅

### BUG-005 — Email button opens about:blank

- **Surface:** `/admin/leads/<id>` — Email button (multiple instances)
- **Repro:** Click Email on desktop → tab navigates to `about:blank` instead of staying on lead detail / opening mail client
- **Wave:** 2
- **Status:** pending fix (anchor-based mailto + copy fallback)

**Claude Code fix notes:**

- Root cause: `handleEmail` set `window.location.href = "mailto:..."` programmatically. With no OS `mailto:` handler the navigation resolved to `about:blank` instead of leaving the page intact.
- Fix: removed `handleEmail` JS handler. Converted all 4 Email button instances (3 status-specific CTAs, desktop header, mobile dropdown item) to `<Button asChild><a href={`mailto:${lead.email}`}>` pattern. Anchors that fail to launch a handler leave the page state unchanged. Added a Copy icon button in the Contact Information card's email renderReadOnly using `navigator.clipboard.writeText()` + toast.success("Email copied").
- Files changed: `src/pages/LeadDetail.tsx`
- Commit: `eeaec8f`
- Verified by: Run 2 ✅ — confirmed on mrcsystem.com: clicking Email no longer navigates to about:blank, page stays put. Copy email affordance: toast + clipboard ✅

### BUG-006 — Activity log entries show no diff

- **Surface:** `/admin/leads/<id>` — Activity timeline section
- **Repro:** Edit a field with an existing value → activity row description reads `"Updated: <field>"` with no old → new visible; raw snake_case rendered in user-facing description
- **Wave:** 3 (architectural)
- **Status:** pending fix (unify to canonical `field_edit` writer + FIELD_LABEL map + inline diff format)

**Claude Code fix notes:**

- Root cause: two parallel activity-log writers had diverged. `useLeadUpdate.ts` wrote `activity_type: 'lead_updated'` with `{ field, from, to }` shape + raw snake_case description and fell through to the plain-text branch in `ActivityTimeline.tsx` — no diff UI was rendered. The canonical `logFieldEdits()` writer (`activity_type: 'field_edit'` with `{ field, old, new }` shape + version + diff render) already existed for entity-scoped edits, but had no `'lead'` scope and was bypassed by `useLeadUpdate`. `handleChangeStatus` and `bookingService.bookInspection` bypassed both, hand-writing activity rows without metadata.
- Fix: created `src/lib/utils/fieldLabels.ts` (FIELD_LABEL map covering every editable lead column + `getFieldLabel()` + `formatDiffValueForDescription()`). Extended `src/lib/api/fieldEditLog.ts` with `'lead'` entity scope, `extraMetadata` pass-through, inline-diff description builder (`"{Label} changed: 'old' → 'new'"` for 1 field, `"N fields changed"` for multi-field) + new `logNoteAdded()` helper writing `activity_type: 'note_added'` with `{ note_text, author }` metadata (NOT a diff — append-only semantics). Rewrote `src/hooks/useLeadUpdate.ts` to route through canonical writers, split internal_notes append into `note_added`, preserve Slack ping for non-note changes. Rewrote `LeadDetail.handleChangeStatus` to use a single `field_edit` row covering status + reversion-cleared columns with reversion metadata via `extraMetadata` (locked decision: parallel `status_change` row dropped). Added `field_edit` diff logging to `bookingService.bookInspection` alongside the preserved `inspection_booked` milestone row. Extended `src/components/dashboard/ActivityTimeline.tsx` to render `note_added` rows as blockquote and resolve diff field names via `getFieldLabel()`. 14 unit tests (`fieldEditLog.test.ts` 9 + `fieldLabels.test.ts` 5) — all green.
- Files changed: `src/lib/utils/fieldLabels.ts` (new) · `src/lib/api/fieldEditLog.ts` · `src/hooks/useLeadUpdate.ts` · `src/pages/LeadDetail.tsx` · `src/lib/bookingService.ts` · `src/hooks/useActivityTimeline.ts` · `src/components/dashboard/ActivityTimeline.tsx` · `src/lib/api/__tests__/fieldEditLog.test.ts` (new) · `src/lib/utils/__tests__/fieldLabels.test.ts` (new)
- Commit: `d9aec0e`
- Verified by: Run 3 ✅ — production smoke on MRC-2026-0141 confirmed inline diff format, `note_added` blockquote rendering, FIELD_LABEL resolution, and version auto-increment

### BUG-007 — Activity log entries appear delayed

- **Surface:** `/admin/leads/<id>` — Activity timeline section
- **Repro:** Save a field → activity row persists to DB immediately but UI doesn't show it for up to 30 seconds
- **Wave:** 3 (architectural)
- **Status:** pending fix (invalidate `['activity-timeline']` on every writer + drop polling interval to 5s safety net)

**Claude Code fix notes:**

- Root cause: `useActivityTimeline.ts` polled at `refetchInterval: 30000` (30s) with no Postgres realtime subscription. `useLeadUpdate` invalidated `['lead', leadId]` and `['leads']` but never `['activity-timeline']`. `handleChangeStatus` and `bookInspection` didn't invalidate either. Result: a save persisted instantly to DB but the UI didn't re-render until the next 30s polling tick.
- Fix: every Wave 3 writer now calls `queryClient.invalidateQueries({ queryKey: ['activity-timeline'] })` after a successful write (`useLeadUpdate`, `LeadDetail.handleChangeStatus`, `bookingService.bookInspection`). Polling interval reduced from 30000ms to 5000ms as a defence-in-depth safety net for cases where invalidation can't reach the timeline (e.g. background tab observing server-side activity row inserts).
- Files changed: `src/hooks/useActivityTimeline.ts` · `src/hooks/useLeadUpdate.ts` · `src/pages/LeadDetail.tsx` · `src/lib/bookingService.ts`
- Commit: `d9aec0e`
- Verified by: Run 3 ✅ — production smoke confirmed activity rows appear within ~1s of save (well under the 5s polling fallback), no perceptible delay

### BUG-008 — property_type has no write path (FUTURE SWEEP — not in current scope)

- **Surface:** Inspection form (technician-side)
- **Severity:** 🟡 Major
- **Status:** Deferred to future sweep
- **Description:** `property_type` has no programmatic writer. Removed from new_lead admin view in Wave 1. Inspection form needs a write site added in a future sweep.
- **Investigation finding:** Wave 1 removes the dead admin edit affordance on the new_lead view (commit `3cf8718`). Michael's intent is that the technician sets `property_type` during the inspection form, but no such write site currently exists. This is the gap.
- **Action when picked up:** Add a `property_type` field (residential / commercial / strata / etc.) to one of the inspection-form sections (likely Section 1 Office Info or Section 2 Summary) and wire it through `updateInspection` to write `leads.property_type` at inspection time. Until that is done, the column remains null on every lead.

### BUG-009 — Lead Source renders blank in Schedule LeadInfo despite DB value present

- **Surface:** `/admin/schedule` → expanded LeadBookingCard → Lead Info section → Lead Source `<select>`
- **Repro:** Open MRC-2026-0141 in `/admin/schedule`. DB has `lead_source = "website"` (verified). LeadInfo `<select>` renders with the placeholder option `"Select lead source..."` instead of showing the source value. LeadDetail on the same lead shows the value correctly.
- **Wave:** 4 (combined with BUG-010 verification)
- **Status:** pending fix
- **Investigation finding:** The data flow is correct end-to-end — `useLeadsToSchedule.ts:68` selects `lead_source`, line 106 maps it to `leadSource`, `LeadBookingCard.tsx:129` seeds `useState(lead.leadSource || '')`. The bug is a value-vs-option mismatch in the rendered `<select>`. The DB value `"website"` (lowercase, set as the Framer Edge Function default at `receive-framer-lead/index.ts`) does not match any `<option value=...>` in `leadSourceOptions` (`src/lib/leadUtils.ts:102-124`). The option set uses display-cased values like `"Website Form"`, `"Google Search (Organic)"`, etc. When a native HTML `<select>` is given a `value` not present among its options it silently displays the empty option (line 867 of LeadBookingCard: `<option value="">Select lead source...</option>`), making the field appear blank.
- **Why LeadDetail works and Schedule doesn't:** `LeadDetail.tsx:1481-1488` uses `InlineEditField` with a `renderReadOnly` that falls back to the raw value if no option label matches (`const label = opt?.label ?? v`). Schedule uses a raw `<select>` with no fallback rendering of unknown values, so the mismatch surfaces as a blank.
- **Fix scope:** Wave 4 will normalise this from the Edge Function side — change the Framer EF default from lowercase `"website"` to a value that exists in `leadSourceOptions` (`"Website Form"`). This single source of truth fix also resolves the long-standing display inconsistency noted in BUG-001's Slack template ("Website (Framer)" vs DB "website") and the Data Map's `⚠️ Display inconsistency` caveat. Existing rows with `"website"` are out of scope for the wave (one-time data backfill is a separate decision — flagged in plan).

### BUG-010 — Internal Notes append on Schedule booking sheet (VERIFICATION ONLY)

- **Surface:** `/admin/schedule` → expanded LeadBookingCard → Internal Notes (Optional) textarea inside the booking sheet
- **Repro intent:** Confirm that internal notes typed during the booking flow follow the same prepend-with-timestamp + `note_added` activity-row pattern as LeadDetail's Internal Notes appender (Wave 3 architecture).
- **Wave:** 4 (combined with BUG-009 fix)
- **Status:** ✅ verified-clean — no fix needed
- **Investigation finding:** Wave 3 already wired the Schedule path correctly. `LeadBookingCard.tsx:119` holds the textarea state; line 397 passes `internalNotes` into `bookInspection({ ...payload, internalNotes })`. `src/lib/bookingService.ts` lines 6-7 import `appendInternalNote` from `@/lib/utils/internalNotes` and `logFieldEdits` + `logNoteAdded` from `@/lib/api/fieldEditLog`. Inside `bookInspection`: line 152 fetches `internal_notes` from the lead pre-update; lines 164-167 prepend the new entry via `appendInternalNote(leadBefore?.internal_notes ?? null, internalNotes)`; line 194 writes the `field_edit` row for status + date + time + assigned_to deltas (excluding internal_notes per Wave 3 split); lines 203-206 write the distinct `note_added` row with the appended text in `metadata.note_text` (NOT diffed). Behaviour matches LeadDetail's appender exactly.
- **Confirmation status:** No code change required. Entry retained in the bug log as a traceable verification step for future regression detection.

### BUG-011 — `leads.lead_source` dual-vocabulary divergence; Zod validator is dead code

- **Surface:** `leads.lead_source` column — every write site
- **Severity:** 🟡 Major (silent data-quality issue; not user-visible at the lead level but breaks reports aggregation)
- **Wave:** 5 (deferred — architectural; requires Vryan input on source granularity)
- **Status:** Deferred to Wave 5

**Description:** `leads.lead_source` has two coexisting vocabularies persisted to the same column:

- **System-written values** (Framer EF default at `supabase/functions/receive-framer-lead/index.ts:689`): lowercase `'website'`. Also the only value the RLS policy `allow_public_insert_leads` (`supabase/migrations/20251111000020_allow_public_lead_creation.sql:24`) permits for anonymous INSERTs.
- **Admin-written values** (`CreateNewLeadModal.tsx:404`, `LeadDetail.tsx:1489`, `LeadBookingCard.tsx:153`): Title-Case strings sourced from `leadSourceOptions` (`src/lib/leadUtils.ts:102-124`) — `'Website Form'`, `'Google Search (Organic)'`, `'Google Ads'`, `'Customer Referral'`, `'Real Estate Agent'`, etc.

The Zod validator `leadSourceSchema` at `src/lib/validators/lead-creation.schemas.ts:166-177` defines a canonical lowercase vocabulary (`'website' | 'hipages' | 'google' | 'referral' | 'repeat' | 'facebook' | 'instagram' | 'other'`) but is **dead code** — zero runtime consumers in the production codebase. Grep confirms `leadSourceSchema`, `normalLeadSchema`, and `NormalLeadSchemaType` are referenced only inside the validator file itself. Every actual write path bypasses it.

**Investigation finding:**

- All four lead_source write sites mapped (Framer EF, CreateNewLeadModal, LeadDetail inline edit, Schedule LeadInfo edit). None pass through the Zod validator before INSERT/UPDATE.
- Zero `.eq('lead_source', '<value>')` query predicates exist in the application code today (grep clean), so the divergence is currently silent at the read side except in reports aggregation.
- Reports impact: `src/hooks/useReportsData.ts:96-104` source-config table only buckets lowercase keys (`'website'`, `'hipages'`, `'google'`, `'referral'`, `'repeat'`, `'facebook'`, `'instagram'`, `'other'`). Line 221 (`const source = lead.lead_source || 'other'`) drops every admin-created lead with a Title-Case value into the `'other'` bucket. The "Source breakdown" chart on `/admin/reports` is therefore inaccurate proportional to the share of admin-created leads.
- Future filtering, segmentation, or attribution work hits the same divergence — any predicate keyed on the lowercase vocabulary will miss the Title-Case rows.

**Why this didn't surface before:** the BUG-009 Schedule `<select>` rendering symptom is what exposed the underlying vocabulary divergence. Without that symptom, the issue would have remained invisible until the next reports-accuracy audit or attribution-keyed filter.

**Why Wave 4 (BUG-009 narrow fix) does not resolve this:** Wave 4 adds a first-class `{ label: 'Website', value: 'website' }` entry to `leadSourceOptions` so the canonical lowercase value renders correctly in the Schedule `<select>`. It does not reconcile the broader vocabulary divergence, does not wire the Zod validator into any write path, and does not backfill historical admin-created rows with Title-Case sources.

**Recommended fix scope (Wave 5):**

1. **Vocabulary decision (input from Vryan required — marketing/sales attribution granularity):** either
   - (a) align UI `leadSourceOptions` to the lowercase-terse vocabulary used by Framer EF + RLS + reports (collapse the 21 Title-Case options to the 8 Zod enum values), OR
   - (b) widen the canonical vocabulary to include the granular Title-Case values and update `useReportsData.ts` source-config + Zod enum + RLS policy comment to match.
2. **Wire the chosen vocabulary through every write site** (`CreateNewLeadModal`, `LeadDetail.saveField('lead_source', …)`, `LeadBookingCard.handleSaveLeadInfo`, `receive-framer-lead/index.ts` insert). Either consume the existing dead Zod validator or remove the validator and rely on `leadSourceOptions` as the single source of truth.
3. **Backfill historical rows:** `UPDATE leads SET lead_source = '<canonical>' WHERE lead_source IN (<Title-Case mismatches>)`. System-attributed migration using `SET LOCAL app.acting_user_id = SYSTEM_USER_UUID` (audit trigger captures the sentinel; Wave 3 `field_edit` rows are application-only and unaffected by raw SQL UPDATE).
4. **Delete the dead validator module** if no longer needed after the wiring decision in step 2.

**Action when picked up:** sequence the four steps above as a single Wave 5 PR. RLS policy (`allow_public_insert_leads`) constrains public/anonymous INSERTs to `lead_source = 'website'` and must remain authoritative for the Framer/public path regardless of which vocabulary direction is chosen.

---

## Data Map — new_lead stage

The canonical reference for every field captured during the `new_lead` stage of the pipeline. Source-of-truth derived from the DB Verification table above (lead `MRC-2026-0141`, `c50c4a6d-486a-46f2-8e4c-18086e7eef28`) plus the Framer Edge Function (`supabase/functions/receive-framer-lead/index.ts`) and LeadDetail inline-edit affordances.

### Framer form submission → `leads` row

| source (form field)          | DB table | DB column                   | type    | related table | transformation notes                                                                                                                                         |
| ---------------------------- | -------- | --------------------------- | ------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `name`                       | `leads`  | `full_name`                 | varchar | —             | trimmed; passed to Slack `Full Name` block (line 140 of EF)                                                                                                  |
| `phone`                      | `leads`  | `phone`                     | varchar | —             | cleaned via `cleanPhoneNumber()` only on later inline-edit (not on initial insert)                                                                           |
| `email`                      | `leads`  | `email`                     | varchar | —             | lowercased validation on form; stored verbatim                                                                                                               |
| `address line`               | `leads`  | `property_address_street`   | varchar | —             | stored verbatim                                                                                                                                              |
| `suburb`                     | `leads`  | `property_address_suburb`   | varchar | —             | drives `property_zone` re-calc on later inline-edit via `calculatePropertyZone()`                                                                            |
| (Google Places autocomplete) | `leads`  | `property_address_state`    | varchar | —             | populated by Places API even if form field appears blank to the user                                                                                         |
| `postcode`                   | `leads`  | `property_address_postcode` | varchar | —             | populated by Places autocomplete (form field shown blank but lookup fills it); rendered in Slack postcode block (Wave 1 fix BUG-001)                         |
| `preferred_date`             | `leads`  | `customer_preferred_date`   | date    | —             | converted from `DD/MM/YYYY` (form) → ISO `YYYY-MM-DD` (DB) by EF; **never cleared on status reversions** (locked: customer's stated preference is immutable) |
| `preferred_time`             | `leads`  | `customer_preferred_time`   | text    | —             | converted from `h:mmam/pm` (form) → 24h `HH:MM` (DB) by EF; same never-cleared rule                                                                          |
| `message`                    | `leads`  | `issue_description`         | text    | —             | stored verbatim, no length cap on insert; UI textarea caps at 1000 chars on later edit                                                                       |

### System-set on insert (Framer Edge Function)

| source                   | DB table | DB column     | type        | related table                                                                          | transformation notes                                                                                                                                                                                                                                |
| ------------------------ | -------- | ------------- | ----------- | -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `gen_random_uuid()`      | `leads`  | `id`          | uuid        | FK target for activities/inspections/calendar_bookings/job_completions/invoices/photos | primary key                                                                                                                                                                                                                                         |
| EF lead-number generator | `leads`  | `lead_number` | varchar     | —                                                                                      | format `MRC-YYYY-NNNN`, monotonic per year                                                                                                                                                                                                          |
| EF default               | `leads`  | `status`      | enum        | (drives status flow)                                                                   | always `new_lead` on Framer insert                                                                                                                                                                                                                  |
| EF default               | `leads`  | `lead_source` | varchar     | —                                                                                      | always lowercase `"website"`. **⚠️ Display inconsistency:** Slack template hardcodes `"Website (Framer)"` at `supabase/functions/receive-framer-lead/index.ts:143`; admin LeadDetail renders raw `"website"` lowercase. Surface to normalise later. |
| `NOW()`                  | `leads`  | `created_at`  | timestamptz | —                                                                                      | UTC; rendered as `DD/MM/YYYY` via `formatDate()` in AU/Melbourne                                                                                                                                                                                    |
| `NOW()`                  | `leads`  | `updated_at`  | timestamptz | —                                                                                      | DB trigger refreshes on every UPDATE                                                                                                                                                                                                                |
| DB trigger               | `leads`  | `search_text` | text        | —                                                                                      | concatenated indexable string (name + email + phone + address + source + description) for `LeadsManagement` search                                                                                                                                  |

### Implicit-null on insert (populated later in pipeline or never)

| source                  | DB table | DB column                                                                                  | type        | populated when            | notes                                                                                                                                                     |
| ----------------------- | -------- | ------------------------------------------------------------------------------------------ | ----------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none on Framer insert) | `leads`  | `urgency`                                                                                  | varchar     | NEVER (BUG-003)           | Dead-in-DB column. Removed from LeadDetail admin view in Wave 1. Schema preserved, no migration.                                                          |
| (none on Framer insert) | `leads`  | `property_type`                                                                            | varchar     | NEVER currently (BUG-008) | Intended to be set by technician in inspection form, but no write path exists. Removed from LeadDetail admin view in Wave 1. Logged as future-sweep work. |
| LeadDetail admin edit   | `leads`  | `assigned_to`                                                                              | uuid        | on booking                | FK → `auth.users.id`; cleared on rank<1 reversion                                                                                                         |
| `bookInspection()`      | `leads`  | `inspection_scheduled_date`                                                                | date        | on booking                | cleared on rank<1 reversion                                                                                                                               |
| `bookInspection()`      | `leads`  | `scheduled_time`                                                                           | text        | on booking                | cleared on rank<1 reversion                                                                                                                               |
| `bookInspection()`      | `leads`  | `booked_at`                                                                                | timestamptz | on booking                | cleared on rank<1 reversion                                                                                                                               |
| inspection completion   | `leads`  | `inspection_completed_date`                                                                | date        | later                     | cleared on rank<2 reversion                                                                                                                               |
| later stages            | `leads`  | `quoted_amount`, `invoice_amount`, `job_*_date`, `payment_received_date`, `report_pdf_url` | varies      | later                     | each cleared on corresponding rank reversion                                                                                                              |

### Admin inline-edit additions on `/admin/leads/<id>` at new_lead stage

| source (LeadDetail component)              | DB table | DB column             | type | related table | transformation notes                                                                                                                                                                                                                              |
| ------------------------------------------ | -------- | --------------------- | ---- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `InlineEditField` (Customer Requests card) | `leads`  | `access_instructions` | text | —             | full-overwrite; first non-null write triggers `field_edit` activity row (post-Wave-3)                                                                                                                                                             |
| `InlineEditField` (Customer Requests card) | `leads`  | `special_requests`    | text | —             | full-overwrite; same activity-log behaviour                                                                                                                                                                                                       |
| Internal Notes textarea + Add button       | `leads`  | `internal_notes`      | text | —             | **append-only via `appendInternalNote()`** — prepends `[DD/MM/YYYY at h:mm am/pm] {note}\n— {author}\n\n---\n\n` to existing log. Post-Wave-3: writes a `note_added` activity row (NOT a diff), full appended entry text in `metadata.note_text`. |

### Activity rows produced at new_lead stage (post-Wave-3 architecture)

| trigger                                      | activities table row                                                                                                                              | metadata shape                                                                                                    | rendered by                                                                   |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| LeadDetail inline-edit (non-notes)           | `activity_type='field_edit'` · title `"vN — edited {Label}"` · description `"{Label} changed: 'old' → 'new'"` (1 field) or `"N fields changed"`   | `{ version, entity_type: 'lead', entity_id, changes: [{ field, old, new }] }`                                     | `ActivityTimeline.tsx` diff render with FIELD_LABEL resolution + version chip |
| LeadDetail Internal Notes append             | `activity_type='note_added'` · title `"Note added"`                                                                                               | `{ note_text, author }`                                                                                           | `ActivityTimeline.tsx` blockquote render                                      |
| LeadDetail status change (manual transition) | `activity_type='field_edit'` — single row covering `status` + reversion-cleared columns (locked Wave 3 decision: no parallel `status_change` row) | `{ version, entity_type: 'lead', entity_id, changes, reverted?, cancelled_booking_ids?, preserved_booking_ids? }` | same `field_edit` diff render                                                 |

### External notification surfaces fired on Framer submission

| event                       | surface                 | message contents                                                                                                                                                                       | code reference                                                     |
| --------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Slack `new_lead` block      | `#mrc-system` channel   | Full Name, Phone, Email, Lead Source ("Website (Framer)" hardcoded), Street, Suburb, **Postcode** (Wave 1 added), Preferred Date/Time, Issue Description, Action banner, Schedule link | `receive-framer-lead/index.ts:108-198`                             |
| customer confirmation email | Resend → customer email | branded "we received your request" template                                                                                                                                            | `receive-framer-lead/index.ts:204+` (`buildConfirmationEmailHtml`) |
| `email_logs` row            | `email_logs` table      | per-send delivery record (recipient, subject, template_name, status, sent_at, `sent_by = SYSTEM_USER_UUID` per the Phase 2 audit canon)                                                | `send-email` EF                                                    |

### okay so michael is back testing now ive testing all the changes and i approve of them and now im gonna contuine

ive gone intoo the shceudle button and gone to that page to shduel the fake lead now like im on call with them

TEST DELETE 2026-05-13 R2
mernda • Property• 4h ago

Prefers 14/05 at 10:00 AM

adam michael test
mernda • Property• 5h ago

Prefers 15/05 at 9:30 AM

Customer's Preferred Time
Date
15/05/2026
Time
9:30 AM
Not yet confirmed — call to schedule

Internal Notes
[13/05/2026 at 3:57 pm] intenral notes 2 test
— michael youssef

---

[13/05/2026 at 10:50 am] Lead came in via website form. Spoke to Adam over the phone briefly — sounds genuine, wants this sorted quickly because of the kids. Confirmed mould visible in master bedroom ceiling + around window. Mentioned bathroom is on shared wall so likely a moisture transfer issue. Property purchased 18 months ago, hasn't had any prior mould work done. Mid-range job most likely. Booked for Friday morning — Glen heading out.
— michael youssef

Address Confirmed
35 wellington street, mernda, VIC, 3754

mernda

Notes from Enquiry
Hi, noticed some black mould spreading along the bedroom ceiling and around the window frame in the master bedroom over the last few weeks. Getting worse and starting to smell musty. We've got two young kids so keen to get it sorted asap. Bought the place about 18 months ago, not sure if it's a moisture issue from the bathroom next door. Looking to get an inspection booked in. Thanks.

Lead Info
Full Name
adam michael test
Phone
0433880403
Email
michaelyoussef396@gmail.com
Brief Description
Hi, noticed some black mould spreading along the bedroom ceiling and around the window frame in the master bedroom over the last few weeks. Getting worse and starting to smell musty. We've got two young kids so keen to get it sorted asap. Bought the place about 18 months ago, not sure if it's a moisture issue from the bathroom next door. Looking to get an inspection booked in. Thanks.

#### i approve all this in the to shcudle page but Lead Source this is left emapty this is the first bug that the lead soruce is just left unaissgned when its usppose to be prefilled and i also wna tot check that Internal Notes (Optional) if saved and filled then it should create another seciton automaticly like we build it to od jsut wnated to double check other than that seems good once those 2 things get fixed ill get back to testing and atuall shduling it in prorpely
